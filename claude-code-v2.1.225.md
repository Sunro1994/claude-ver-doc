# Claude Code v2.1.225

> 작성일: 2026-08-11

---

# 📋 요약본

## 🎉 신기능 (4건)
- **게이트웨이 지출 한도 경고** — 사용량 경고에 게이트웨이 지출 한도를 반영한다. 한도 도달 메시지에 한도값·초기화 시각·운영자 메시지가 함께 표시된다. 게이트웨이도 2.1.225 이상이어야 동작한다.
- **`claude agents` 워크스페이스 신뢰 확인** — 신뢰되지 않은 디렉터리에서 `claude agents`를 실행하면 신뢰 확인 프롬프트가 뜬다. `claude` 본체와 동일한 동작이다.
- **Remote Control 사진 직접 전달** — Claude 앱에서 첨부한 사진을 별도 도구 호출로 디스크에서 읽지 않고 Claude에게 바로 보여준다.
- **`SendMessage`로 원격 세션에 먼저 말 걸기** — 다른 머신의 Remote Control 세션을 이름으로 지목해 대화를 시작할 수 있다. 상대가 먼저 메시지를 보내야 답장만 가능했던 제약이 사라졌다.
  - `ListAgents`가 해당 세션들을 `name [ref]` 형식으로 보여준다.

## 🛠️ 개선/수정 (10건)
- **`CLAUDE_CODE_OAUTH_TOKEN` 덮어쓰기 수정** — 일시적인 401이 장기 토큰을 저장된 로그인의 단기 토큰으로 교체해, 재시작 전까지 headless 세션이 깨지던 문제를 고쳤다.
- **macOS MCP OAuth 401 폭주 수정** — 키체인 읽기가 타임아웃되면 인증한 적 없는 것처럼 401이 연속 발생하던 문제를 고쳤다.
- **auto 모드 안전 필터 오집계 수정** — 자체 권한 확인이 안전 필터에 거부된 것을 연속 차단 한도에 세던 문제를 고쳤다. 해당 작업은 여전히 거부되지만, 모델에게 재시도하지 말고 넘어가라고 알린다.
- **세션 간 메시지 방치 수정** — headless 세션과 시작 시점에 교차 세션 메시지가 알림도 만료도 없이 멈춰 있던 문제를 고쳤다.
- **Remote Control 재개 시 히스토리 손상 수정** — 아주 큰 대화가 압축된 뒤 Remote Control 세션을 재개하면 대화 기록이 깨지던 문제를 고쳤다.
- **에이전트 목록 hover 부작용 수정** — 목록에서 다른 프로젝트 세션에 마우스를 올리면 다음 에이전트의 시작 디렉터리가 바뀌던 문제를 고쳤다.
- **`claude self-hosted-runner` 조기 실패 처리** — `--base-dir`를 만들 수 없거나 쓸 수 없을 때 등록만 하고 모든 세션이 실패하던 문제를 고쳤다. 이제 시작 시점에 명확한 에러를 내고 종료한다.
- **웹 세션 멈춤 오탐 수정** — Claude Code on the web 세션이 멈춘 것으로 잘못 보고되며 재연결마다 이벤트 백로그를 계속 늘려 재전송하던 문제를 고쳤다.
- **[VSCode] Focus 뷰 접힘 수정** — 최신 할 일 목록, 대기 중인 질문의 맥락, 확정된 답변이 접혀 사라지던 문제를 고쳤다. 생각만 있는 접힘은 "Thought for Ns"로 표시되고 해당 턴이 끝나면 다시 접힌다.
- **`SendMessage` 수신자 오치환 방지** — 이미 확인한 Remote Control 수신자는, 그쪽 목록을 확인할 수 없더라도 같은 이름의 로컬 세션으로 바뀌지 않는다.

## 🔑 이번 버전의 핵심 키워드
**"원격·헤드리스 세션의 인증과 연결을 신뢰할 수 있게 만든 버전"** — 토큰 교체·키체인 타임아웃·재연결 백로그 같은 조용한 실패를 걷어내고, Remote Control 세션을 이름으로 먼저 호출할 수 있게 했다.

---

# 📜 원문 (한글 번역본)

> 원문 ChangeLog를 원래 순서 그대로 한 줄도 빠짐없이 번역한 문서입니다.

- Claude Code의 사용량 경고에 게이트웨이 지출 한도(spend-limit) 지원을 추가했다. 한도 도달 메시지가 한도값, 초기화 시각, 운영자 메시지를 함께 알려준다 (게이트웨이가 2.1.225여야 함)
- 신뢰되지 않은 디렉터리에 대해 `claude agents`에 워크스페이스 신뢰 프롬프트를 추가했다. `claude`의 동작과 일치한다
- 일시적인 401이 장기 유효 `CLAUDE_CODE_OAUTH_TOKEN`을 저장된 로그인의 단기 토큰으로 교체해, 재시작할 때까지 headless 세션이 깨지던 문제를 고쳤다
- macOS에서 키체인 읽기가 타임아웃된 뒤, MCP OAuth 서버가 인증된 적 없는 것처럼 401 에러를 연달아 내며 간헐적으로 실패하던 문제를 고쳤다
- auto 모드가 자신의 권한 확인이 안전 필터에 거부된 것을 연속 차단 한도에 포함해 세던 문제를 고쳤다. 해당 작업은 여전히 거부되지만, 이제 모델에게 재시도하지 말고 넘어가라고 알린다
- headless 세션과 시작 과정에서 교차 세션(cross-session) 메시지가 알림도 만료도 없이 멈춰 있던 문제를 고쳤다
- 아주 큰 대화가 압축(compact)된 뒤 Remote Control 세션을 재개할 때 대화 기록이 깨지던 문제를 고쳤다
- 에이전트 목록에서 다른 프로젝트의 세션에 마우스를 올리면 다음 에이전트가 시작할 디렉터리가 바뀌던 문제를 고쳤다
- `claude self-hosted-runner`가 `--base-dir`를 생성하거나 쓸 수 없을 때 등록은 되고 모든 세션이 실패하던 문제를 고쳤다. 이제 시작 시점에 명확한 에러와 함께 종료한다
- Claude Code on the web 세션이 멈춘 것으로 잘못 보고되어, 재연결마다 점점 커지는 이벤트 백로그를 다시 보내던 문제를 고쳤다
- Remote Control 개선: Claude 앱에서 첨부한 사진을 별도 도구 호출로 디스크에서 읽는 대신 Claude에게 직접 보여준다
- [VSCode] Focus 뷰가 최신 할 일 목록, 대기 중인 질문의 맥락, 확정된 답변을 접어 감추던 문제를 고쳤다. 생각만 있는 접힘은 "Thought for Ns"로 표시되고 해당 턴이 완료되면 다시 접힌다
- `SendMessage`가 이제 다른 머신의 Remote Control 세션과 이름으로 대화를 시작할 수 있다 (`ListAgents`가 `name [ref]` 형태로 보여준다). 상대가 먼저 메시지를 보낸 뒤에만 답장하던 방식에서 바뀌었다
- `SendMessage`: 이미 확인한 Remote Control 수신자는, 그 목록을 확인할 수 없었더라도 이 머신의 같은 이름 세션으로 절대 교체되지 않는다

---

## 🎯 챌린지

이번 버전에서 내 환경에 적용해볼 만한 항목입니다.

### 1. `CLAUDE_CODE_OAUTH_TOKEN` 사용 여부 점검
- **파일**: `~/.claude/settings.json`
- **근거**: 이번 버전은 일시적 401이 장기 토큰을 단기 토큰으로 갈아치우던 버그를 고쳤다. `env` 블록이나 셸 프로필에 `CLAUDE_CODE_OAUTH_TOKEN`을 쓰고 있다면 이 버전으로 올리는 것만으로 headless(`claude -p`) 실행이 중간에 죽는 사고가 사라진다. 현재 `settings.json`에는 `env` 설정이 없으니 토큰을 어디서 주입하는지 확인하고 필요하면 `env`로 모아 둔다.
- **난이도**: ★☆☆ (약 10분)

### 2. `claude -p` 자동화 실패 알림 추가
- **파일**: `~/.claude/settings.json` (`hooks.Stop`)
- **근거**: `claude-changelog-sync`는 매일 08:00 cron으로 `claude -p --model opus`를 돌린다. headless 세션 인증 실패·교차 세션 메시지 방치 같은 조용한 실패가 이번 버전에서 고쳐졌지만, 실패 자체를 알아채는 장치는 아직 없다. 기존 `Stop` hook의 `osascript` 알림 옆에 종료 상태를 구분하는 알림을 붙여 실패를 눈에 보이게 만든다.
- **난이도**: ★★☆ (약 15분)

### 3. VSCode Focus 뷰 재확인
- **파일**: VSCode 확장 (설정 파일 변경 없음) — 확장 업데이트 후 Focus 뷰 확인
- **근거**: Focus 뷰가 최신 할 일 목록과 대기 중인 질문 맥락을 접어 감추던 문제가 고쳐졌다. `effortLevel: high` 설정으로 긴 thinking이 자주 발생하는 환경이라 "Thought for Ns" 접힘 동작 개선 효과가 크다. 확장을 최신으로 올리고 긴 작업 한 건을 돌려 할 일 목록이 유지되는지 본다.
- **난이도**: ★☆☆ (약 5분)

### 4. `enabledPlugins` 정리
- **파일**: `~/.claude/settings.json` (`enabledPlugins`, `statusLine`)
- **근거**: `token-optimizer` 플러그인은 `false`로 꺼져 있는데 `statusLine`은 여전히 그 플러그인 캐시 경로(`plugins/cache/alexgreensh-token-optimizer/.../statusline.js`)를 가리킨다. 플러그인 캐시가 정리되면(`cleanupPeriodDays: 30`) statusLine이 깨진다. 플러그인을 다시 켜거나 스크립트를 `~/.claude/` 아래로 복사해 경로를 고정한다.
- **난이도**: ★☆☆ (약 10분)
