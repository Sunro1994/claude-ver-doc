# Claude Code v2.1.227

> 작성일: 2026-08-11

---

# 📋 요약본

## 🛠️ 개선/수정 (5건)
- **구독 등급 인식 오류 수정** — 만료된 로그인 토큰으로 세션을 시작하면 사용자의 구독 등급 없이 기능 플래그가 평가됐다. 그 결과 Max 플랜 사용자에게 Fable용 usage credits를 켜라고 잘못 안내하는 일이 있었고, 이제 고쳐졌다.
- **`claude-code-action` Bash 실패 수정** — GitHub 호스팅 러너에서 `allowed_non_write_users` 옵션을 쓰면 모든 Bash 명령이 실패하던 문제를 고쳤다.
- **`/tui` 되감기 동작 수정** — 첫 메시지 이전으로 되감은(rewind) 대화를 `/tui`가 다시 불러오던 문제를 고쳤다.
- **슬래시 명령 메뉴 개선** — 파란색은 이제 선택된 행에만 쓴다. 검색어와 일치한 글자는 색을 바꾸는 대신 굵게 표시한다. 이모지나 악센트가 들어간 명령 이름도 글자가 깨지지 않는다.
- **성능 개선** — 파일 없음 제안(file-not-found suggestions)과 at-mention 크기 검사에서 이벤트 루프가 멈추는 횟수를 줄였다.

## 🔑 이번 버전의 핵심 키워드
**"잘못된 플랜 안내와 CI Bash 실패를 잡은 수리 릴리스"** — 신기능 없이 오탐·실패·표시 문제만 정리했다.

---

# 📜 원문 (한글 번역본)

> 원문 ChangeLog를 원래 순서 그대로 한 줄도 빠짐없이 번역한 문서입니다.

- 만료된 로그인 토큰으로 세션이 시작될 때 사용자의 구독 등급 없이 기능 플래그가 평가되던 문제를 수정했다. 이로 인해 Max 플랜 사용자에게 Fable용 usage credits 활성화를 잘못 요구할 수 있었다.
- GitHub 호스팅 러너에서 `allowed_non_write_users`를 설정한 `claude-code-action` 실행 시 모든 Bash 명령이 실패하던 문제를 수정했다.
- `/tui`가 첫 메시지 이전으로 되감긴 대화를 다시 되살리던 문제를 수정했다.
- 슬래시 명령 메뉴 개선: 파란색은 이제 선택된 행만 표시하고, 일치한 글자는 색을 바꾸는 대신 굵게 표시하며, 이모지나 악센트가 포함된 이름도 글리프를 유지한다.
- 성능 개선: 파일 없음 제안과 at-mention 크기 검사에서 이벤트 루프 정체를 줄였다.

---

## 🎯 챌린지

이번 버전에서 내 환경에 적용해볼 만한 항목입니다.

### 1. 로그인 토큰 만료 여부 확인 후 재로그인
- **파일**: `~/.claude/settings.json` (변경 없음) / 터미널에서 `claude` 실행 후 `/status`
- **근거**: 이번 버전은 만료된 토큰으로 세션을 시작하면 구독 등급이 빠진 채 기능 플래그가 평가되는 버그를 고쳤다. 설정에 `"model": "claude-opus-4-8[1m]"`처럼 상위 플랜 모델을 고정해 두었으므로, 토큰이 만료된 상태였다면 모델·크레딧 안내가 어긋났을 수 있다. `/status`로 로그인 상태와 플랜을 확인하고 필요하면 `/login`으로 갱신한다.
- **난이도**: ★☆☆ (약 5분)

### 2. `claude-code-action` 워크플로에서 `allowed_non_write_users` 점검
- **파일**: 각 프로젝트의 `.github/workflows/*.yml`
- **근거**: GitHub 호스팅 러너 + `allowed_non_write_users` 조합에서 모든 Bash 명령이 실패하던 버그가 고쳐졌다. 해당 옵션을 쓰는 워크플로가 있으면 액션 버전을 최신으로 올리고, 그동안 우회하려고 넣어둔 임시 조치(Bash 스텝 비활성화 등)를 되돌린다. 없으면 이 항목은 건너뛴다.
- **난이도**: ★★☆ (약 15분)

### 3. 슬래시 명령 이름에서 이모지 제거 여부 재검토
- **파일**: `~/.claude/skills/*/SKILL.md`, `~/.claude/commands/*.md`
- **근거**: 이번 버전에서 이모지·악센트가 들어간 명령 이름도 글리프가 깨지지 않게 됐다. 다만 MEMORY.md의 "산출물에 이모지 금지" 기준은 그대로다. 명령 목록을 훑어 이름·`description`에 남은 이모지를 텍스트 태그로 정리하면 메뉴 표시와 문서 규칙이 함께 맞는다.
- **난이도**: ★★☆ (약 15분)
