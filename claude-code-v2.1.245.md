# Claude Code v2.1.245

> 작성일: 2026-08-28

---

# 📋 요약본

## 🛠️ 개선/수정 (1건)
- **Linux 시작 크래시 수정** — glibc 2.44를 탑재한 리눅스 배포판에서 Claude Code 실행 즉시 죽던 문제를 고쳤다. 해당 배포판은 Arch Linux, CachyOS, Fedora Rawhide다. 최신 glibc를 쓰는 롤링 릴리스 계열이 주 대상이었다.

## 🔑 이번 버전의 핵심 키워드
**"최신 glibc 리눅스 실행 불가 해소"** — 기능 추가 없이 Arch·CachyOS·Fedora Rawhide의 시작 크래시만 잡은 핫픽스 릴리스.

---

# 📜 원문 (한글 번역본)

> 원문 ChangeLog를 원래 순서 그대로 한 줄도 빠짐없이 번역한 문서입니다.

- glibc 2.44를 제공하는 리눅스 배포판(예: Arch Linux, CachyOS, Fedora Rawhide)에서 시작 시 발생하던 크래시를 수정했다.

---

## 🎯 챌린지

이번 버전에서 내 환경에 적용해볼 만한 항목입니다.

### 1. macOS 환경이라 직접 영향 없음을 확인하고 버전만 올리기
- **파일**: 터미널에서 `claude update` 실행 (설치 경로 `~/.claude/`)
- **근거**: 이번 버전은 리눅스 전용 크래시 수정이다. 현재 환경은 darwin(macOS)이라 이 크래시와 무관하다. 다만 누적 수정을 받으려면 최신 버전 유지가 유리하다. 업데이트 후 `claude --version`으로 `2.1.245` 확인.
- **난이도**: ★☆☆ (약 5분)

### 2. 원격 리눅스 서버의 Claude Code 버전 점검
- **파일**: EOS 외부개발서버 등 접속 후 `claude --version`, `ldd --version`
- **근거**: 메모리상 EOS 외부개발서버(svr-02) 등 리눅스 호스트를 다룬다. 그 호스트가 롤링 릴리스 계열이고 glibc 2.44라면 이전 버전에서 시작 크래시가 났을 수 있다. `ldd --version`으로 glibc를 확인하고 2.44면 즉시 `2.1.245`로 올린다.
- **난이도**: ★☆☆ (약 10분)

### 3. Stop hook 알림에 버전 표기 추가
- **파일**: `~/.claude/settings.json` 의 `hooks.Stop`
- **근거**: 현재 Stop hook은 "작업이 완료되었습니다"만 띄운다. 이런 핫픽스 릴리스는 버전 차이를 인지하기 어렵다. 알림 title에 실행 중인 버전을 넣어두면 여러 머신·세션에서 어떤 버전이 도는지 즉시 보인다. `with title "Claude Code"` 부분을 버전 포함 문자열로 바꾼다.
- **난이도**: ★★☆ (약 15분)
