# Claude Code v2.1.190

> 작성일: 2026-06-25

---

# 📋 요약본

## 🛠️ 개선/수정 (1건)
- **버그 수정 및 안정성 개선** — 세부 항목이 공개되지 않은 유지보수 릴리스. 신규 기능 없이 기존 동작의 결함 수정과 안정성 향상에 집중했다.

## 🔑 이번 버전의 핵심 키워드
**"조용한 안정화 릴리스"** — 새 기능 없이 버그 수정과 신뢰성 개선만 담은 유지보수 버전이다.

---

# 📜 원문 (한글 번역본)

> 원문 ChangeLog를 원래 순서 그대로 한 줄도 빠짐없이 번역한 문서입니다.

- 버그 수정 및 안정성 개선

---

## 🎯 챌린지

이번 버전에서 내 환경에 적용해볼 만한 항목입니다.

### 1. 2.1.190 으로 업데이트 후 hook 회귀 점검
- **파일**: `~/.claude/settings.json` + `~/agent-infra/hooks/`
- **근거**: 이 버전은 안정성 개선이 핵심이라 hook 실행 경로에 영향이 갈 수 있다. `claude update` 후 자작 hook 7종(`deploy-guard.sh`, `persona-drift-warn.sh`, `session-end-retro.sh` 등)이 정상 트리거되는지 한 번씩 smoke 확인한다. hook 의존도가 높은 환경에서 회귀를 조기 차단한다.
- **난이도**: ★★☆ (약 15분)

### 2. `cleanupPeriodDays` 재검토
- **파일**: `~/.claude/settings.json`
- **근거**: 현재 `99999` 로 설정돼 transcript 가 사실상 영구 누적된다. 안정성 릴리스에 맞춰 디스크·세션 안정성 관점에서 보존 기간을 현실값(예: `90`)으로 조정할지 판단한다. 무한 누적은 장기적으로 reliability 리스크다.
- **난이도**: ★☆☆ (약 5분)

### 3. UserPromptSubmit hook 의 실패 처리 검증
- **파일**: `~/.claude/settings.json`
- **근거**: `measure.py` 호출이 `|| echo 0` 로 폴백하는데, 이 버전의 hook 처리 안정성 변경과 맞물려 컨텍스트 사용률 측정이 조용히 0 으로 떨어질 수 있다. 실제로 `python3 measure.py quality current --json` 를 직접 실행해 정상 출력되는지 확인한다.
- **난이도**: ★☆☆ (약 10분)
