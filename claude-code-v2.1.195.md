# Claude Code v2.1.195

> 작성일: 2026-06-28

---

# 📋 요약본

## 🎉 신기능 (1건)
- **`CLAUDE_CODE_DISABLE_MOUSE_CLICKS`** — 전체화면(fullscreen) 모드에서 마우스 클릭/드래그/호버를 비활성화하면서 휠 스크롤은 그대로 유지하는 환경변수. 터미널 네이티브 텍스트 선택·복사를 되살리면서도 스크롤 편의는 잃지 않는다.

## 🛠️ 개선/수정 (11건)
- **hook matcher 정확 일치** — 하이픈 포함 식별자(예: `code-reviewer`, `mcp__brave-search`)가 부분 문자열로 잘못 매칭되던 문제 수정. 이제 정확 일치(exact-match)한다. 하이픈 포함 MCP 서버의 모든 도구를 매칭하려면 `mcp__brave-search__.*` 를 써야 한다.
- **macOS 음성 받아쓰기 무음 캡처 수정** — 장시간 세션에서 기본 입력 장치가 바뀐 뒤 무음만 잡히던 문제 해결.
- **공백 없는 언어 자동 제출 수정** — 일본어·중국어·태국어처럼 단어 사이 공백이 없는 언어에서 음성 받아쓰기 자동 제출이 동작하지 않던 문제 해결.
- **외부 플러그인 설치 동의 강제** — 프로젝트 `.claude/settings.json` 으로만 활성화된 외부 플러그인이 일부 로더 경로에서 명시적 설치 동의를 건너뛰던 문제 수정.
- **`/plugin` Enable/Disable 수정** — 플러그인 `plugin.json` 의 `name` 이 마켓플레이스 항목 이름과 다를 때 활성/비활성이 동작하지 않던 문제 해결.
- **백그라운드 작업 유실 수정** — 더 최신 버전의 Claude Code 가 기록한 백그라운드 작업이 `claude agents` 에서 사라지거나 데이터를 잃던 문제 해결.
- **크래시 작업 재오픈 빈 화면 수정** — 크래시된 백그라운드 작업을 다시 열 때 재시작 화면 대신 최대 5초간 빈 화면이 뜨던 문제 해결.
- **백그라운드 에이전트 데몬 도달 불가 수정** — 컨트롤 소켓 시작 실패 시 데몬이 도달 불가 상태로 떠 재시작을 막던 문제 해결.
- **Linux 음성 모드 개선** — SoX 는 설치돼 있으나 오디오 캡처 장치가 없을 때, "마이크 없음" 과 "SoX 미설치" 를 구분해 표시한다.
- **`claude agents` 완료 목록 개선** — 남는 세로 공간을 채우도록 표시. 짧은 터미널에서는 헤더가 압축돼 라이브 세션이 가려지지 않는다.
- **Remote 세션 시작 개선** — 컨테이너 기동 동안 프로비저닝 체크리스트를 표시한다.

## 🔑 이번 버전의 핵심 키워드
**"백그라운드 에이전트·음성·플러그인 안정화, 그리고 hook matcher exact-match 정정"** — 신기능 한 건을 빼면 대부분 누적된 결함을 다듬은 안정화 릴리스다.

---

# 📜 원문 (한글 번역본)

> 원문 ChangeLog를 원래 순서 그대로 한 줄도 빠짐없이 번역한 문서입니다.

- 전체화면 모드에서 마우스 클릭/드래그/호버를 비활성화하되 휠 스크롤은 유지하는 `CLAUDE_CODE_DISABLE_MOUSE_CLICKS` 추가
- 하이픈 포함 식별자(예: `code-reviewer`, `mcp__brave-search`)를 가진 hook matcher 가 실수로 부분 문자열 매칭되던 문제 수정 — 이제 정확 일치한다. 하이픈 포함 MCP 서버의 모든 도구를 매칭하려면 `mcp__brave-search__.*` 를 사용하라.
- macOS 음성 받아쓰기가 기본 입력 장치 변경 후 장시간 세션에서 무음을 캡처하던 문제 수정
- 공백 없이 쓰는 언어(일본어, 중국어, 태국어)에서 음성 받아쓰기 자동 제출이 전혀 동작하지 않던 문제 수정
- 프로젝트 `.claude/settings.json` 으로만 활성화된 외부 플러그인이 모든 로더 경로에서 명시적 설치 동의를 요구하지 않던 문제 수정
- 플러그인 `plugin.json` 의 `name` 이 마켓플레이스 항목 이름과 다를 때 `/plugin` Enable/Disable 이 동작하지 않던 문제 수정
- 더 최신 버전의 Claude Code 가 기록할 때 백그라운드 작업이 `claude agents` 에서 사라지거나 데이터를 잃던 문제 수정
- 크래시된 백그라운드 작업을 다시 열 때 재시작 대신 최대 5초간 빈 화면이 표시되던 문제 수정
- 컨트롤 소켓 시작 실패 시 백그라운드 에이전트 데몬이 도달 불가 상태로 실행되어 재시작을 막던 문제 수정
- Linux 음성 모드 개선: SoX 가 설치돼 있으나 오디오 캡처 장치가 없을 때 "마이크 없음" 과 "SoX 미설치" 를 구분한다
- `claude agents` 완료 목록이 가용한 세로 공간을 채우도록 개선; 짧은 터미널에서는 헤더가 압축돼 라이브 세션이 계속 보인다
- 컨테이너가 시작되는 동안 프로비저닝 체크리스트를 보여주도록 Remote 세션 시작 개선

---

## 🎯 챌린지

이번 버전에서 내 환경에 적용해볼 만한 항목입니다.

### 1. 전체화면 마우스 클릭 비활성화로 터미널 복사 되살리기
- **파일**: `~/.claude/settings.json` (`env` 블록 추가)
- **근거**: 이번 버전 신규 `CLAUDE_CODE_DISABLE_MOUSE_CLICKS` 항목. `"env": { "CLAUDE_CODE_DISABLE_MOUSE_CLICKS": "1" }` 를 추가하면 휠 스크롤은 유지하면서 클릭/드래그가 Claude Code 에 가로채이지 않아, 터미널 네이티브 텍스트 선택·복사가 정상 동작한다. statusLine·hook 출력을 자주 드래그 복사하는 환경에 직접적 편의.
- **난이도**: ★☆☆ (약 5분)

### 2. hook matcher exact-match 변경 점검
- **파일**: `~/.claude/settings.json` (`hooks` 블록)
- **근거**: 이번 버전에서 hook matcher 가 하이픈 식별자에 한해 부분 문자열 매칭을 멈추고 정확 일치로 바뀌었다. 현재 matcher 는 `Read`·`Bash`·`Write`·`Edit` 로 모두 하이픈 없는 정확한 도구명이라 영향이 없음을 확인하고, 향후 MCP 서버(예: `playwright`) 전체 도구를 매칭하는 hook 을 추가할 땐 `mcp__playwright__.*` 패턴을 써야 함을 메모. 다수 hook 을 운용하는 환경이라 회귀 여부를 한 번 검증할 가치가 있다.
- **난이도**: ★☆☆ (약 5~10분)
