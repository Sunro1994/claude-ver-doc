# Claude Code v2.1.197

> 작성일: 2026-07-01

---

# 📋 요약본

## 🎉 신기능 (1건)
- **Claude Sonnet 5 출시 — Claude Code 기본 모델 전환** — 신규 모델 Claude Sonnet 5가 Claude Code의 기본 모델이 된다. 네이티브 `1M-token` 컨텍스트 윈도우를 제공해 한 세션에 훨씬 많은 코드·문서를 담을 수 있다.
  - 8월 31일까지 `Mtok당 $2/$10` 프로모션 가격 적용 (입력 $2 / 출력 $10).
  - 접근하려면 `2.1.197` 이상으로 업데이트해야 한다.

## 🔑 이번 버전의 핵심 키워드
**"기본 모델이 Sonnet 5로 바뀌고 컨텍스트가 1M으로 넓어졌다"** — 업데이트만 하면 더 큰 컨텍스트와 프로모 가격을 쓸 수 있는 모델 교체 릴리스.

---

# 📜 원문 (한글 번역본)

> 원문 ChangeLog를 원래 순서 그대로 한 줄도 빠짐없이 번역한 문서입니다.

- Claude Sonnet 5 출시: 이제 Claude Code의 기본 모델이며, 네이티브 `1M-token` 컨텍스트 윈도우와 8월 31일까지 적용되는 `Mtok당 $2/$10` 프로모션 가격을 제공합니다. 접근하려면 버전 `2.1.197`로 업데이트하세요. https://www.anthropic.com/news/claude-sonnet-5

---

## 🎯 챌린지

이번 버전에서 내 환경에 적용해볼 만한 항목입니다.

### 1. settings.json에 `model` 명시 고정
- **파일**: `~/.claude/settings.json`
- **근거**: 이번 버전이 Sonnet 5를 Claude Code 기본 모델로 전환한다. 모델을 명시하지 않으면 업데이트 후 세션·서브에이전트가 자동으로 Sonnet 5로 바뀐다. CLAUDE.md §5는 신뢰성 우선 Opus 고정 방침이므로, 의도치 않은 기본 전환을 막으려면 `"model"` 키를 명시해 고정한다.
- **난이도**: ★☆☆ (약 5분)

### 2. 버전 업데이트로 1M 컨텍스트 확보
- **파일**: (CLI 실행) `claude update` 또는 패키지 매니저로 `2.1.197` 이상 설치 후 `claude --version` 확인
- **근거**: Sonnet 5와 `1M-token` 컨텍스트는 `2.1.197` 이상에서만 접근 가능하다. Poplus 30개 마이크로서비스 모노레포처럼 컨텍스트가 큰 학습·탐색 작업에서 1M 윈도우가 직접적 이득이다.
- **난이도**: ★☆☆ (약 5분)

### 3. `claude-changelog-sync` 생성 모델 재검토 (프로모 8/31 한정)
- **파일**: `claude-changelog-sync` 스킬의 `claude -p --model opus` 명령
- **근거**: 이 스킬은 매일 cron으로 문서를 생성한다. 8월 31일까지 Sonnet 5 프로모 가격이 적용되고 1M 컨텍스트로 전체 ChangeLog를 한 번에 처리할 수 있어, 요약 생성 같은 비치명 작업은 Sonnet 5 전환 시 비용이 크게 준다. 단 CLAUDE.md §5(신뢰성 우선 Opus)와 충돌하므로 적용 여부는 사용자가 판단한다.
- **난이도**: ★☆☆ (약 10분)
