# Claude Code v2.1.210

> 작성일: 2026-07-19

---

# 📋 요약본

## 🎉 신기능 (4건)
- **접힌 툴 요약에 실시간 경과 시간 카운터** — 오래 걸리는 툴 호출이 멈춘 것처럼 보이지 않고, 요약 줄에서 경과 시간이 눈에 띄게 째깍째깍 흐른다.
- **위험 권한 규칙 시작 경고** — `Write(path)`·`NotebookEdit(path)`·`Glob(path)` 권한 규칙에 시작 시 경고를 띄운다. 대신 `Edit(path)`·`Read(path)`를 쓰라고 안내한다.
- **스크린 리더 권한 모드 음성 안내** — Shift+Tab으로 권한 모드를 순환할 때 바뀐 모드를 소리로 읽어준다.
- **에이전트 푸터 대기 개수 표시** — 입력을 기다리는 백그라운드 에이전트 수를 푸터에 보여주고, 수가 바뀌면 잠깐 색으로 강조한다.

## 🛠️ 개선/수정 (29건)
- **워크트리 서브에이전트 격리** — `isolation: 'worktree'` 서브에이전트가 자기 워크트리가 아니라 메인 저장소에 git 변경 명령을 실행하던 위험을 막았다.
- **ultracode 오발동 차단** — 웹훅 페이로드·중계된 PR 코멘트 등 사람이 아닌 입력에서 `ultracode` opt-in이 켜지던 문제를 고쳤다.
- **훅 타임아웃 오보고** — 훅 콜백 타임아웃을 사용자 거부로 잘못 보고해 무인 세션이 멈추고 대기하던 문제를 고쳤다.
- **`cd` 백그라운드 가정** — 명령이 백그라운드로 넘어간 뒤 Claude가 `cd`가 적용됐다고 착각하던 문제를 고쳤다. 이제 디렉터리 미변경을 명시한다.
- **플러그인 MCP 종료** — 세션 중 MCP 서버를 재동기화할 때 플러그인이 제공한 MCP 서버가 꺼지던 문제를 고쳤다.
- **플랜 승인 덮어쓰기** — 편집 없는 플랜 승인이 "(edited by user)"로 표시되고 오래된 스냅샷으로 플랜 파일을 덮어쓰던 문제를 고쳤다.
- **Grep 오탐** — 결과 끝을 넘어 페이지를 넘길 때 "No matches found"로 잘못 뜨던 문제를 고쳤다.
- **`$1`/`$2` 자리표시자 보존** — 스킬·커맨드에서 매칭 안 된 위치 자리표시자가 조용히 사라지던 문제를 고쳐 이제 그대로 남긴다.
- **워크트리 잠금 스윕** — 종료된 백그라운드 세션이 남긴 `git worktree lock`을 주기적 스윕이 해제한다.
- **Agent 툴 인젝션 방어** — 서브에이전트가 읽은 내용을 통한 간접 프롬프트 인젝션에 대해 Agent 툴을 강화했다.
- **auto 모드 분류기 Sonnet 5** — 외부 세션 권한 분류기가 이제 Sonnet 5를 기본으로 쓰고, 첫 요청에서 검증돼 세션 동안 고정된다.
- **MEMORY.md 초과 오류** — 인덱스가 읽기 한도를 넘기는 메모리 쓰기는 조용한 잘림 대신 명시적 오류를 낸다.
- **`claude attach` 안정화** — 세션 전환 중 "job not found"·"agent is still starting"으로 실패하던 attach가 데몬 안정까지 대기하도록 고쳤다.
- **Fable 어드바이저 일시 중단** — 서버 측 문제 수정 동안 Fable이 어드바이저 선택기에서 사용 불가로 표시된다.

## 🔑 이번 버전의 핵심 키워드
**"백그라운드 에이전트·워크트리·MCP 격리를 다지고, 대량의 안정성 버그를 정리한 릴리스"** — 화려한 신기능보다 무인·병렬 운영의 안정성에 집중했다.

---

# 📜 원문 (한글 번역본)

> 원문 ChangeLog를 원래 순서 그대로 한 줄도 빠짐없이 번역한 문서입니다.

- 오래 실행되는 툴 호출이 멈춘 것처럼 보이지 않고 눈에 띄게 시간이 흐르도록, 접힌 툴 요약 줄에 실시간 경과 시간 카운터를 추가했다.
- `Write(path)`, `NotebookEdit(path)`, `Glob(path)` 권한 규칙에 대해 시작 시 경고를 추가했다 — 대신 `Edit(path)` 또는 `Read(path)`를 사용하라.
- `isolation: 'worktree'` 서브에이전트가 자기 격리 워크트리가 아니라 메인 저장소 체크아웃에 대해 git을 변경하는 명령을 실행할 수 있던 문제를 수정했다.
- `ultracode` 키워드 opt-in이 웹훅 페이로드나 중계된 PR 코멘트 같은 사람이 아닌 입력에서 발동되던 문제를 수정했다.
- UI 컴포넌트가 스타일 텍스트 요소 밖의 내용을 반환할 때 렌더링된 텍스트 조각이 크래시 텔레메트리에 새어 들어가던 문제를 수정했다.
- Claude Code에서 연 외부 에디터로 붙여넣기 마커가 새어 나가, 붙여넣은 텍스트 주위에 이상한 È/É 문자로 나타날 수 있던 문제를 수정했다.
- 세션 전환 중 `claude attach`가 때때로 "job not found" 또는 "agent is still starting" 오류로 실패하던 문제를 수정했다 — 이제 attach는 데몬이 안정될 때까지 기다리며, 느린 attach 도중의 터미널 리사이즈는 완료 후 한 번에 적용된다.
- 툴의 결과 렌더러가 UI 요소 대신 숫자 bigint 값이나 일반 텍스트를 반환할 때 세션이 크래시되던 문제를 수정했다.
- 훅 콜백 타임아웃이 모델에게 사용자 거부로 잘못 보고되어, 무인 세션이 멈추고 대기하게 만들던 문제를 수정했다.
- 명령이 백그라운드로 옮겨진 뒤 Claude가 `cd`가 적용된 것으로 가정하던 문제를 수정했다 — 이제 툴 결과가 작업 디렉터리가 변경되지 않았음을 명시한다.
- MCP 서버를 세션 중간에 재동기화할 때 플러그인이 제공한 MCP 서버가 종료되던 문제를 수정했다.
- 편집 없는 플랜 승인이 "(edited by user)"로 표시되고 오래된 스냅샷으로 플랜 파일을 덮어쓰던 문제를 수정했다.
- Bedrock, Vertex, Foundry에서 auto 모드가 더 이상 opt-in이 필요 없는데도 `/doctor`가 auto-mode-default 제안을 건너뛰던 문제를 수정했다.
- Grep 콘텐츠 모드가 결과 끝을 넘어 페이지를 넘길 때 "No matches found"라고 잘못 표시하던 문제를 수정했다.
- 스킬과 커맨드에서 매칭되지 않은 `$1`/`$2` 위치 자리표시자가 조용히 제거되던 문제를 수정했다 — 이제 그대로 보존된다.
- 플러그인 캐시 쓰기가 실패 시 임시 파일을 남기고, Windows·네트워크 파일시스템에서 잠긴 파일 이름 변경에 실패하던 문제를 수정했다.
- 클라이언트가 백그라운드 서비스로의 연결을 리셋할 때 백그라운드 워커가 크래시 루프에 빠지던 문제를 수정했다.
- `claude agents --effort ultracode` 값이 디스패치된 세션에 전달되지 않고 조용히 버려지던 문제를 수정했다.
- ←를 눌러 에이전트 뷰를 열면 세션으로 돌아왔을 때 작업 트래커가 사라지던 문제를 수정했다.
- 에이전트 대시보드가 세션 삭제 후에도 버려진 답장 초안의 붙여넣은 이미지를 유지하던 문제를 수정했다.
- 종료된 백그라운드 세션이 영구적인 `git worktree lock`을 남기던 문제를 수정했다 — 이제 주기적 스윕이 소유 프로세스가 사라진 잠금을 해제한다.
- `initialize` 컨트롤 요청으로 등록된 SDK MCP 서버가 다음 턴까지 연결을 시작하지 못하고 대기하던 문제를 수정했다.
- `CLAUDE_CODE_DISABLE_ALTERNATE_SCREEN=1` 환경에서 세션에서 에이전트 뷰로 돌아갈 때 겹치는 잔상 프레임이 남던 문제를 수정했다.
- 뒤늦게 나타난 `.claude/*` 심링크가 샌드박스 쓰기 금지 목록에 반영되지 않던 문제를 수정했다.
- 서브에이전트가 읽은 내용을 통한 간접 프롬프트 인젝션에 대해 Agent 툴을 강화했다.
- 명령이 타임아웃에 걸려 자동으로 백그라운드로 전환될 때의 Bash/PowerShell 툴 메시지를 개선해, 모델이 멈춤(hang)과 명시적 백그라운드 요청을 구분할 수 있게 했다.
- auto 모드 개선: 권한 분류기가 외부 세션에 대해 이제 Sonnet 5를 기본으로 사용하며, 세션의 첫 요청에서 검증되고 세션 동안 고정된다.
- 번들된 dataviz 스킬의 차트 색상 검증을 지각적 OKLab 색차로 개선하고 색맹 임계값을 재보정했다.
- MEMORY.md 인덱스가 읽기 한도를 초과하게 만드는 메모리 쓰기는 이제 조용한 잘림 대신 명시적 오류를 낸다.
- 스크린 리더 모드가 이제 Shift+Tab으로 권한 모드를 순환할 때 권한 모드 변경을 소리로 알린다.
- 에이전트 푸터 힌트가 이제 몇 개의 백그라운드 에이전트가 입력을 기다리는지 표시하며, 개수가 바뀌면 잠깐 색으로 강조한다.
- 에이전트 뷰: ←를 눌러 빠져나온 세션이 마우스 호버나 화살표 키로 선택이 옮겨간 뒤에도 눈에 띄게 표시된 상태로 유지된다.
- Fable 어드바이저 실패를 유발하는 서버 측 문제가 수정되는 동안, Fable이 어드바이저 선택기에서 일시적으로 사용 불가로 표시된다.

---

## 🎯 챌린지

이번 버전에서 내 환경에 적용해볼 만한 항목입니다.

### 1. 권한 규칙 감사 — `Write/NotebookEdit/Glob` → `Edit/Read`
- **파일**: `~/.claude/settings.json`, 프로젝트 `.claude/settings.json`
- **근거**: 이번 버전(원문 2번)이 `Write(path)`·`NotebookEdit(path)`·`Glob(path)` 권한 규칙에 시작 경고를 추가했다. 이 규칙들은 의도대로 동작하지 않으니 `Edit(path)`·`Read(path)`로 바꿔야 한다. 훅·권한을 촘촘히 쓰는 환경이라 시작마다 뜨는 경고 노이즈를 미리 없앨 수 있다.
- **난이도**: ★☆☆ (약 5분)

### 2. 스킬·커맨드 `$1`/`$2` 자리표시자 점검
- **파일**: `~/.claude/skills/*/SKILL.md`, `~/.claude/commands/*`, 프로젝트 `.claude/commands/*`
- **근거**: 원문 15번으로 매칭 안 된 `$1`/`$2` 위치 자리표시자가 이제 조용히 제거되지 않고 그대로 보존된다. `/kship [PR 제목]`처럼 인자를 받는 커스텀 스킬·커맨드가 있으니, 자리표시자가 의도대로 렌더되는지 한 번 재확인한다.
- **난이도**: ★★☆ (약 10분)

### 3. 오래된 워크트리 잠금 정리
- **파일**: `/Users/leeseonro/Document/BE` (명령 `git worktree list --porcelain` → `git worktree prune`)
- **근거**: 원문 21번으로 종료된 백그라운드 세션이 남긴 `git worktree lock`을 주기적 스윕이 해제한다. feat/worktree 워크플로우를 상시 쓰는 환경이라, 과거에 쌓여 남은 잠금이 있는지 한 번 훑고 prune해 정리한다.
- **난이도**: ★☆☆ (약 5분)

### 4. `MEMORY.md` 길이 점검
- **파일**: `/Users/leeseonro/.claude/projects/-Users-leeseonro-Document-BE/memory/MEMORY.md`, 글로벌 `~/.claude/MEMORY.md`
- **근거**: 원문 29번으로 MEMORY.md가 읽기 한도를 넘기면 이제 메모리 쓰기가 조용한 잘림이 아니라 명시적 오류로 실패한다. 글로벌 인덱스가 이미 20줄 가까이 쌓여 있으니, 한도 초과로 저장이 막히기 전에 오래된 항목을 정리·분할한다.
- **난이도**: ★★☆ (약 15분)
