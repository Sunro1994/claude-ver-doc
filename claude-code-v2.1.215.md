# Claude Code v2.1.215

> 작성일: 2026-07-19

---

# 📋 요약본

## 🛠️ 개선/수정 (1건)
- **`/verify`·`/code-review` 자동 실행 중단** — 이제 Claude가 스스로 `/verify`와 `/code-review` 스킬을 돌리지 않는다. 필요할 때 사용자가 직접 `/verify` 또는 `/code-review`로 호출해야 한다. 검증·리뷰가 "알아서 되겠지"에 기대던 흐름은 명시적 호출로 바꿔야 한다.

## 🔑 이번 버전의 핵심 키워드
**"검증은 이제 수동 호출"** — Claude가 검증·리뷰를 대신 트리거해주지 않으니, 필요한 시점에 직접 불러야 한다.

---

# 📜 원문 (한글 번역본)

> 원문 ChangeLog를 원래 순서 그대로 한 줄도 빠짐없이 번역한 문서입니다.

- Claude는 더 이상 `/verify`와 `/code-review` 스킬을 스스로 실행하지 않는다. 사용하고 싶을 때 `/verify` 또는 `/code-review`로 직접 호출하라.

---

## 🎯 챌린지

이번 버전에서 내 환경에 적용해볼 만한 항목입니다.

### 1. 검증 등급 규칙에 "수동 호출" 명시
- **파일**: `/Users/leeseonro/.claude/CLAUDE.md` (§7.1 검증 등급)
- **근거**: §7.1은 치명 등급에서 `/integrity-review`·`/verify` 활용을 전제하지만, 이번 버전부터 Claude가 `/verify`·`/code-review`를 자동 실행하지 않는다. "치명 등급에서는 `/verify`·`/code-review`를 사용자가 직접 호출한다"는 한 줄을 추가해 자동 실행에 기대던 공백을 막는다.
- **난이도**: ★☆☆ (약 5분)

### 2. `/kship` 순서에 `/code-review` 수동 단계 삽입
- **파일**: `/Users/leeseonro/Document/BE/CLAUDE.md` (슬래시 커맨드 → /kship 동작 순서)
- **근거**: `/kship`은 테스트 → 커밋 → 푸시 → PR로 이어지는데, 커밋 전 코드 리뷰가 자동으로 끼어들 여지가 사라졌다. 4단계(테스트)와 5단계(커밋) 사이에 "`/code-review` 수동 실행" 항목을 넣어 PR 전 리뷰 누락을 방지한다.
- **난이도**: ★★☆ (약 10분)
