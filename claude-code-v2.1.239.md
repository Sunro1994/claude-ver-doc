# Claude Code v2.1.239

> 작성일: 2026-08-22

---

# 📋 요약본

## 🎉 신기능 (11건)
- **`/claude-api upgrade`** — Python 프로젝트를 `anthropic` 0.x에서 1.x로 마이그레이션하는 명령이 추가됐다. 스킬의 Python 레퍼런스도 1.x 기준으로 갱신 — 타임아웃은 `httpx.Timeout`이 아니라 `anthropic.Timeout`을 쓴다.
- **전체화면 렌더러 제안 확대** — Bedrock·Vertex·Foundry 등 그동안 제외됐던 환경에도 1회성 전체화면 제안이 뜬다. 해당 환경의 신규 설치는 전체화면으로 시작한다.
- **비용 추정에 US-only 추론 프리미엄 반영** — `/cost`, 상태줄, `--max-budget-usd`가 데이터 레지던시 워크스페이스의 1.1배 가산을 포함해 계산한다.
- **Cloud 세션 플러그인 동기화** — claude.ai에서 동기화된 플러그인은 `name@synced`로 표시되고 `claude plugin enable/disable <name>@synced`로 제어된다. 직접 설치한 동명 플러그인을 덮어쓰지 않는다.
- **Alpine/musl 빌드 애드온 지원** — 네이티브 이미지 붙여넣기, 클립보드, 오디오 캡처 애드온이 정상 로드된다. glibc 대신 musl로 빌드된 바이너리를 쓴다.
- **Windows 세션 간 메시징** — 여러 머신의 Claude Code 세션이 `SendMessage`로 서로 메시지를 보내고 `ListAgents`로 서로를 찾는다. macOS·Linux와 동일해졌다.
- **`ListAgents` 자기 이름 인지** — 세션이 자기 이름(피어가 메시지 보낼 때 쓰는 이름)을 알려준다. 자기 이름으로 `SendMessage`하면 "그런 에이전트 없음" 대신 자기 자신임을 알린다.
- **`ListAgents`·`/list-agents` 팀메이트 표시** — 살아있는 팀메이트가 목록에 나온다. 이전엔 서브에이전트와 다른 세션만 보여서 연결 가능한 팀메이트가 없는 것처럼 보였다.
- **`keybindingFlavor: "readline"` Bash 단어 키 일치** — Alt+F와 Ctrl/Option+→가 단어 끝에서 멈추고, Alt+D가 거기까지 삭제한다(Ctrl+Y로 되붙이기). 구두점이 단어 구분자로 동작한다.
- **Claude in Chrome 탭 그룹 정리** — `/clear`가 세션의 Chrome 탭 그룹을 닫는다. 빈 그룹은 `/resume` 시점과 Claude Code 종료 시 닫힌다.
- **원격 세션 모바일 이미지 경로** — 모바일에서 업로드한 이미지가 저장 파일 경로를 함께 전달한다. Claude가 만드는 파일로 그 이미지를 복사할 수 있다.

## 🛠️ 개선/수정 (48건)
- **사용량 한도 메시지 보강** — 월 지출 한도를 이미 다 쓴 경우, 세션·주간 한도가 언제 초기화되는지도 알려준다.
- **Bedrock 스트리밍 프록시 이중 청구 수정** — 응답 `Content-Type` 헤더를 지우는 프록시 뒤에서 매 턴을 비스트리밍으로 재실행해 청구 API 호출이 조용히 2배가 되던 문제를 고쳤다.
- **Bedrock + SSO + `awsAuthRefresh` 시작 멈춤 수정** — HTTPS 프록시 뒤에서 시작 시 멈추던 문제. 자격증명 사전 체크가 `HTTPS_PROXY`를 따른다.
- **없어진 디렉터리에서 시작 시 크래시 덤프 수정** — 원시 크래시 덤프 대신 명확한 메시지를 출력한다.
- **JetBrains 터미널 Edit·Write 지연 수정** — Claude Code 플러그인이 붙어 있을 때 약 5초씩 멈추던 문제.
- **Esc + 대기 프롬프트 경쟁 상태 수정** — 다음 턴이 조기 종료되어 Claude는 아직 작업 중인데 세션이 유휴로 보이고, 이후 재제출이 같은 동작을 반복하던 문제.
- **WebFetch 만료 콘텐츠 잔류 수정** — 의도한 15분이 아니라 세션 전체 동안 만료된 페이지 내용을 메모리에 붙들고 있던 문제.
- **Cloud 세션 plan 모드 해제 수정** — 워커가 유휴 재시작된 뒤 plan 모드에서 빠져나온 상태로 재개되던 문제(웹·데스크톱·모바일).
- **MCP elicitation 폼 잘림 수정** — 터미널보다 긴 폼이 전체화면에서 잘렸다. 이제 창에 맞게 표시되고 숨은 필드는 스크롤로 접근, Accept/Decline은 항상 보인다.
- **원격 MCP 서버 5xx 복구 수정** — 세션 중 재연결 시 일시적 5xx 이후 계속 실패 상태로 남던 문제(cloud 세션·SDK `setMcpServers()`).
- **커스텀 세션 제목 유실 수정** — 이름 변경 후 대화가 약 64KB 넘게 쌓이면 `/resume`에서 제목이 사라지던 문제.
- **`claude -c`/resume 디렉터리 오인 수정** — `_`, `-`, `.` 같은 문자만 다른 경로의 다른 디렉터리 세션을 집어오던 문제.
- **`/resume`·에이전트 뷰 정렬 수정** — 파일만 건드려졌거나 단순히 다시 열었을 때 최근 변경으로 표시·재정렬되던 문제.
- **`/resume` 전체 프로젝트 모드 삭제 디렉터리 수정** — 삭제된 워크트리 등으로 `cd`하라고 안내하던 문제. 이제 현재 디렉터리에서 재개된다.
- **`dark-ansi` 테마 가독성 수정** — 전체화면에서 펼친 도구 결과가 배경과 같은 색으로 렌더링되던 문제.
- **전체화면 렌더러 제안 반복 수정** — 답할 수 없는 상황에서 매 실행마다 다시 뜨던 문제. 3회 노출 후 중단된다.
- **`.worktreeinclude` 패턴 수정** — `**/`로 시작하는 패턴이 gitignore된 디렉터리 안의 대상에 대해 조용히 아무것도 매칭하지 않던 문제.
- **UTF-8 BOM 파일 무시 수정** — `.md` 파일이 BOM으로 시작하는 에이전트·스킬·커맨드가 조용히 무시되던 문제.
- **`/insights` 태그 노출 수정** — 일부 모델에서 응답에 리터럴 `<message>` 태그를 그대로 뱉던 문제.
- **마켓플레이스 `metadata.pluginRoot` 수정** — 아무 효과가 없었다. 이제 문서대로 맨 플러그인 소스 이름이 그 아래에서 해석된다.
- **브라우저 터미널 마우스 이벤트 수정** — 마우스 리포트가 쪼개져 도착하면 `"35;150;7M"` 같은 텍스트가 프롬프트에 끼어들던 문제.
- **effort/ultracode 상태 배지 커스텀 테마 수정** — 색 오버라이드가 무시되던 문제.
- **OpenTelemetry 트레이스 분절 수정** — `PreToolUse` 훅으로 지연된 도구 실행이 새 트레이스를 시작하지 않고 원래 턴의 트레이스로 이어진다.
- **에이전트 뷰 vim 모드 수정** — Escape가 NORMAL 모드로 전환되며 입력한 텍스트를 지우지 않고 유지한다.
- **`selection:copy` 키바인딩 수정** — Shift+화살표로 확장한 선택이 조용히 버려지던 문제.
- **`/voice` 시작 팁 수정** — `voice.enabled` 설정으로 음성 입력을 켠 뒤에도 계속 떴던 문제.
- **셸 모드(`!`) Tab 완성 수정** — `./script` 경로에서 `./`가 떨어져 셸이 실행할 수 없는 명령이 남던 문제.
- **전체화면 포커스 클릭 오작동 수정** — 창을 다시 포커스하려고 클릭했을 뿐인데 권한 프롬프트에 응답하거나 버튼이 눌리던 문제.
- **전체화면 슬래시 커맨드 패널 가림 수정** — `/config`, `/model` 같은 패널이 최신 메시지를 덮던 문제. 대화가 패널 위에 고정된다.
- **`/workflows` 상세 대화창 오버플로 수정** — Claude가 응답 중일 때 열면 터미널을 넘쳐 헤더가 화면 밖으로 나가던 문제.
- **Linux 샌드박스 git 수정** — 존재하지 않는 `.git/config.worktree`를 읽을 수 없게 만들어 `extensions.worktreeConfig`가 설정된 레포에서 모든 샌드박스 git 명령이 깨지던 문제.
- **훅 `posix_spawn ENOENT` 수정** — 세션 작업 디렉터리가 삭제된 뒤 훅이 실패하던 문제. 이제 프로젝트 루트나 홈 디렉터리에서 실행된다.
- **`claudeMdExcludes` 심볼릭 링크 수정** — 패턴이 대상이 아니라 rules 디렉터리나 심볼릭 링크 자체를 지목할 때 심볼릭 링크된 `.claude/rules` 파일이 제외되지 않던 문제.
- **Remote Control 세션 제목 폭주 동기화 수정** — 두 Claude Code 프로세스가 하나의 백그라운드 작업 상태를 공유할 때 발생(2.1.232 회귀). 제목 갱신이 중복 제거·레이트 제한된다.
- **`/`로 시작하는 제목 세션 수정** — `SendMessage`로 지정할 수 없고 `ListAgents`에서 "(untitled)"로 보이던 문제.
- **붙여넣기 플레이스홀더 깨짐 수정** — 커서가 `[Pasted text #N]` 안에 있을 때 Ctrl+W·Ctrl+U·Ctrl+K·Option+Backspace·Option+D와 vim `df`/`dt`가 깨진 플레이스홀더를 남기던 문제.
- **마스킹 입력 유출 수정** — 로그인 코드 입력란 같은 비밀번호형 입력의 텍스트가 다른 곳에서 Ctrl+Y로 붙여넣어지거나 Esc 두 번으로 지울 때 프롬프트 히스토리에 저장되던 문제.
- **검색창 Ctrl+Backspace 수정** — 단어 대신 한 글자만 지우던 문제.
- **조직 정책 거부 재전송 수정** — 조직 정책 체크에서 거부된 요청이 거부 표시 전에 재전송되던 문제.
- **컴팩션 후 리마인더 개선** — 스킬의 원래 인수가 새 요청으로 재실행되지 않는다.
- **긴 파일 경로 중간 생략** — 도구 사용 행의 긴 경로를 중간에서 잘라 한 줄로 유지한다.
- **원격 세션 keep-alive 유지** — 긴 `SessionStart`·`Setup` 훅이 도는 동안에도 keep-alive를 보내 훅 중간에 컨테이너가 유휴 회수되지 않는다.
- **`/goal` 체크인 백오프** — 장시간 백그라운드 작업의 반복 체크인이 30분 고정이 아니라 30분 → 1시간 → 2시간 간격으로 늘어난다.
- **`/goal` 목표 복원** — `claude --resume` 선택기로 세션을 재개하면 활성 목표가 복원된다.
- **`CLAUDE_CODE_RETRY_WATCHDOG` 즉시 실패** — 조직 지출 한도·크레딧 소진 오류에서 초기화를 무한정 기다리지 않고 바로 실패한다.
- **Claude Code on the web 프록시 경유** — Bash 등 도구가 non-API anthropic.com 호스트(www, docs 등)로 보내는 요청이 세션 네트워크 프록시를 통과한다. 환경의 허용 도메인이 적용된다.
- **Remote Control 안내 개선** — 계정에 Remote Control이 활성화되지 않은 경우 메시지와 `claude doctor` 문구가 명확해졌다.
- **[VSCode] 사용량 배너 정렬** — 사용량 한도 배너의 "View usage"가 배너 중간에 떠 있지 않고 경고 텍스트와 같은 줄에 놓인다.

## 🔑 이번 버전의 핵심 키워드
**"전체화면·재개·세션 간 통신의 대청소"** — 신기능은 적고, 전체화면 렌더링·`/resume` 세션 식별·세션 간 메시징에 쌓인 결함을 48건 규모로 걷어낸 안정화 릴리스다.

---

# 📜 원문 (한글 번역본)

> 원문 ChangeLog를 원래 순서 그대로 한 줄도 빠짐없이 번역한 문서입니다.

- 비용 추정(`/cost`, 상태줄, `--max-budget-usd`)이 데이터 레지던시 워크스페이스의 1.1배 US-only 추론 프리미엄을 포함한다
- Bedrock·Vertex·Foundry 및 그 외 그동안 제외됐던 환경에 1회성 전체화면 렌더러 제안을 추가했다. 해당 환경의 신규 설치는 이제 전체화면으로 시작한다
- Python 프로젝트를 `anthropic` 0.x에서 1.x로 마이그레이션하는 `/claude-api upgrade`를 추가하고, 스킬의 Python 레퍼런스를 1.x 기준으로 갱신했다(타임아웃은 `httpx.Timeout`이 아니라 `anthropic.Timeout`을 쓴다)
- Cloud 세션: claude.ai에서 동기화된 플러그인이 `name@synced`로 표시되고, `claude plugin enable/disable <name>@synced`로 동작하며, 직접 설치한 동명 플러그인을 덮어쓰지 않는다
- Alpine/musl 빌드: 네이티브 이미지 붙여넣기, 클립보드, 오디오 캡처 애드온이 로드된다(런타임이 거부하던 glibc 바이너리 대신 musl로 빌드된 바이너리 사용)
- 월 지출 한도를 이미 다 썼을 때 표시되는 사용량 한도 메시지가 세션 한도·주간 한도의 초기화 시점도 함께 알려준다
- 응답 `Content-Type` 헤더를 제거하는 프록시 뒤에서의 Bedrock 스트리밍을 고쳤다. 매 턴을 비스트리밍으로 재실행해 청구되는 API 호출이 조용히 2배가 되던 문제다
- Bedrock을 SSO 프로필과 `awsAuthRefresh`로 쓸 때 HTTPS 프록시 뒤에서 Claude Code가 시작 시 멈추던 문제를 고쳤다 — 자격증명 사전 체크가 `HTTPS_PROXY`를 따른다
- 더 이상 존재하지 않는 디렉터리에서 Claude Code를 시작할 때 나던 원시 크래시 덤프를 고쳤다. 이제 명확한 메시지를 출력한다
- Claude Code 플러그인이 연결된 JetBrains IDE 터미널에서 Edit·Write 호출이 약 5초간 멈추던 문제를 고쳤다
- 프롬프트가 대기 중일 때 Esc를 누르면 다음 턴이 조기 종료될 수 있던 경쟁 상태를 고쳤다. Claude가 아직 작업 중인데 세션이 유휴 상태가 되고, 이후 재제출이 동작을 반복하게 만들던 문제다
- WebFetch가 의도한 15분이 아니라 세션 전체 동안 만료된 페이지 콘텐츠를 메모리에 유지하던 문제를 고쳤다
- Cloud 세션(Claude Code on the web, 데스크톱·모바일 앱)이 워커의 유휴 재시작 후 plan 모드를 벗어난 상태로 재개되던 문제를 고쳤다
- 터미널보다 높은 MCP elicitation 폼이 전체화면 모드에서 잘리던 문제를 고쳤다. 이제 폼이 창에 맞고, 숨은 필드는 스크롤로 접근할 수 있으며, Accept/Decline은 항상 보인다
- cloud 세션이나 SDK `setMcpServers()`를 통한 세션 중 재연결에서 일시적 5xx 이후 원격 MCP 서버가 계속 실패 상태로 남던 문제를 고쳤다
- 이름을 변경한 뒤 대화가 약 64KB 넘게 기록되면 커스텀 세션 제목이 `/resume`에서 사라지던 문제를 고쳤다
- `claude -c`/resume이 `_`, `-`, `.` 같은 문자만 다른 경로의 다른 디렉터리 세션을 집어오던 문제를 고쳤다
- `/resume`과 에이전트 뷰가 파일만 건드려졌거나 단순히 다시 열렸을 뿐인 세션을 최근 변경으로 표시(및 재정렬)하던 문제를 고쳤다
- `/resume` 전체 프로젝트 모드가 삭제된 디렉터리(예: 제거된 워크트리)로 `cd`하라고 안내하던 문제를 고쳤다. 그런 세션은 이제 현재 디렉터리에서 재개된다
- `dark-ansi` 테마가 전체화면 모드에서 펼쳐진 도구 결과를 배경과 같은 색 텍스트로 렌더링하던 문제를 고쳤다
- 답할 수 없는 상황에서 전체화면 렌더러 프롬프트가 실행마다 다시 뜨던 문제를 고쳤다. 이제 3회 노출 후 중단된다
- `**/`로 시작하는 `.worktreeinclude` 패턴이 대상이 gitignore된 디렉터리에 있을 때 조용히 아무것도 매칭하지 않던 문제를 고쳤다
- `.md` 파일이 UTF-8 BOM으로 시작하는 에이전트·스킬·커맨드가 조용히 무시되던 문제를 고쳤다
- 일부 모델에서 `/insights`가 응답에 리터럴 `<message>` 태그를 그대로 내보내던 문제를 고쳤다
- 마켓플레이스 `metadata.pluginRoot`가 아무 효과가 없던 문제를 고쳤다. 이제 맨 플러그인 소스 이름이 문서에 설명된 대로 그 아래에서 해석된다
- 브라우저 기반 터미널에서 마우스 리포트가 여러 쓰기로 쪼개져 도착할 때 마우스 이동이 `"35;150;7M"` 같은 텍스트를 프롬프트에 삽입하던 문제를 고쳤다
- effort/ultracode 상태 배지 색상에 대한 커스텀 테마 오버라이드가 무시되던 문제를 고쳤다
- OpenTelemetry 트레이스 분절을 고쳤다. `PreToolUse` 훅으로 지연된 도구 실행이 새 트레이스를 시작하지 않고 원래 턴의 트레이스에서 이어진다
- 에이전트 뷰의 vim 모드를 고쳤다. Escape가 프롬프트를 지우지 않고 NORMAL 모드로 전환하며 텍스트를 유지한다
- `selection:copy` 키바인딩이 Shift+화살표 키로 확장된 텍스트 선택을 조용히 버리던 문제를 고쳤다
- `voice.enabled` 설정으로 음성 입력을 활성화한 뒤에도 `/voice` 시작 팁이 계속 표시되던 문제를 고쳤다
- 셸 모드(`!`) Tab 완성이 `./script` 경로에서 `./`를 떨어뜨려 셸이 실행할 수 없는 명령을 남기던 문제를 고쳤다
- 전체화면 모드에서 창을 다시 포커스하려고 터미널을 클릭했을 뿐인데 권한 프롬프트에 응답하거나 버튼을 누르던 문제를 고쳤다
- 전체화면 모드의 슬래시 커맨드 패널(예: `/config`, `/model`)이 최신 메시지를 덮던 문제를 고쳤다. 이제 대화가 패널 위에 고정된다
- Claude가 아직 응답 중일 때 열면 `/workflows` 상세 대화창이 터미널을 넘쳐 헤더가 화면 밖으로 사라지던 문제를 고쳤다
- Linux 샌드박스가 존재하지 않는 `.git/config.worktree`를 읽을 수 없게 만들어, `extensions.worktreeConfig`가 설정된 레포에서 모든 샌드박스 git 명령이 깨지던 문제를 고쳤다
- 세션의 작업 디렉터리가 삭제된 뒤 훅이 "posix_spawn ENOENT"로 실패하던 문제를 고쳤다. 이제 프로젝트 루트 또는 홈 디렉터리에서 실행된다
- 패턴이 대상이 아니라 rules 디렉터리나 심볼릭 링크 자체를 지목할 때 `claudeMdExcludes`가 심볼릭 링크된 `.claude/rules` 파일을 제외하지 않던 문제를 고쳤다
- 두 Claude Code 프로세스가 하나의 백그라운드 작업 상태를 공유할 때 Remote Control로 세션 제목 동기화가 폭주하던 문제(2.1.232 회귀)를 고쳤다. 제목 갱신이 중복 제거되고 레이트 제한된다
- 제목이 `/`로 시작하는 세션이 `SendMessage`로 지정 불가능하고 `ListAgents`에서 "(untitled)"로 표시되던 문제를 고쳤다
- 커서가 `[Pasted text #N]` 안에 있을 때 Ctrl+W, Ctrl+U, Ctrl+K, Option+Backspace, Option+D와 vim `df`/`dt`가 깨진 플레이스홀더를 남기던 문제를 고쳤다
- 로그인 코드 입력란 같은 마스킹(비밀번호형) 입력의 텍스트가 다른 곳에서 Ctrl+Y로 붙여넣어지거나, Esc 두 번으로 지웠을 때 프롬프트 히스토리에 저장되던 문제를 고쳤다
- 검색창에서 Ctrl+Backspace가 단어 대신 한 글자를 지우던 문제를 고쳤다
- 조직 정책 체크에서 거부된 요청이 거부가 표시되기 전에 재전송되던 문제를 고쳤다
- 컴팩션 후 표시되는 리마인더를 개선해 스킬의 원래 인수가 새 요청으로 재실행되지 않게 했다
- 도구 사용 행의 긴 파일 경로가 한 줄을 유지하도록 중간에서 잘린다
- 원격 세션이 긴 `SessionStart` 또는 `Setup` 훅이 실행되는 동안 keep-alive를 계속 보내므로, 훅 중간에 컨테이너가 유휴 회수되지 않는다
- `/goal`: 장시간 백그라운드 작업의 반복 체크인이 30분마다 반복되지 않고 백오프한다(30분, 그다음 1시간, 이후 2시간마다)
- `/goal`: `claude --resume` 선택기로 세션을 재개하면 그 세션의 활성 목표가 복원된다
- `ListAgents`가 세션에 자기 이름(피어가 메시지를 보낼 때 쓰는 이름)을 알려주고, 자기 이름으로 `SendMessage`하면 "no agent named …" 대신 자기 자신임을 알린다
- `ListAgents`와 `/list-agents`가 살아 있는 팀메이트를 목록에 표시한다(이전엔 서브에이전트와 다른 세션만 나와서 연결 가능한 팀메이트가 없는 것처럼 보였다)
- `keybindingFlavor: "readline"`이 단어 키에서도 Bash와 일치한다. Alt+F와 Ctrl/Option+→가 단어 끝에서 멈추고, Alt+D가 거기까지 삭제하며(Ctrl+Y로 되붙이기), 구두점이 단어를 구분한다
- 영구 재시도 모드(`CLAUDE_CODE_RETRY_WATCHDOG`)가 조직 지출 한도·크레딧 소진 오류에서 초기화를 무한정 기다리지 않고 즉시 실패한다
- Claude in Chrome: `/clear`가 세션의 Chrome 탭 그룹을 닫고, 빈 그룹은 `/resume` 시점과 Claude Code 종료 시 닫힌다
- 원격 세션: 모바일에서 업로드한 이미지가 저장된 파일 경로를 함께 포함하므로, Claude가 만드는 파일로 그 이미지를 복사할 수 있다
- Claude Code on the web: Bash와 그 외 도구가 non-API anthropic.com 호스트(예: www, docs)로 보내는 요청이 세션의 네트워크 프록시를 경유하므로, 환경의 허용 도메인이 적용된다
- Remote Control: 계정에 Remote Control이 활성화되지 않았을 때의 메시지와 `claude doctor` 문구가 명확해졌다
- Windows: 세션 간 메시징을 지원한다. macOS·Linux와 마찬가지로 여러 머신의 Claude Code 세션이 `SendMessage`로 서로 메시지를 보내고 `ListAgents`로 서로를 찾을 수 있다
- [VSCode] 사용량 한도 배너의 "View usage"가 배너 중간에 떠 있지 않고 경고 텍스트와 같은 줄에 놓인다

---

## 🎯 챌린지

이번 버전에서 내 환경에 적용해볼 만한 항목입니다.

### 1. 스킬·에이전트 파일 BOM 검사
- **파일**: `~/.claude/skills/`, `~/.claude/agents/`, `~/.claude/commands/`
- **근거**: 이번 버전에서 UTF-8 BOM으로 시작하는 `.md`가 조용히 무시되던 버그가 고쳐졌다. 즉 이전 버전에서는 BOM이 붙은 스킬이 로드되지 않고도 아무 경고가 없었다는 뜻이다. `claude-changelog-sync`·`report-writer`·`update-repos` 등 커스텀 스킬을 여러 개 쓰고 있으니 `grep -rlI $'\xef\xbb\xbf' ~/.claude/skills ~/.claude/agents ~/.claude/commands` 로 한 번 훑고, 걸리면 `sed -i '' '1s/^\xEF\xBB\xBF//'` 로 제거한다.
- **난이도**: ★☆☆ (약 5분)

### 2. 훅 스크립트를 절대 경로 기준으로 고정
- **파일**: `~/.claude/hooks/deploy-guard.sh`, `~/.claude/settings.json`
- **근거**: 훅이 삭제된 작업 디렉터리에서 `posix_spawn ENOENT`로 죽던 문제가 고쳐졌고, 이제 프로젝트 루트나 홈에서 실행된다. 워크트리를 만들고 지우는 작업 흐름(feat 브랜치 로컬 전용 정책)에서는 훅의 실행 디렉터리가 사라지는 상황이 실제로 발생한다. `deploy-guard.sh` 안에서 상대 경로나 `$PWD` 가정을 쓰는 부분이 있는지 확인하고 `$HOME` 기준 절대 경로로 바꾼다.
- **난이도**: ★★☆ (약 15분)

### 3. `keybindingFlavor: "readline"` 적용
- **파일**: `~/.claude/settings.json`
- **근거**: 이번 버전에서 readline 플레이버가 단어 단위 키(Alt+F, Ctrl/Option+→, Alt+D, Ctrl+Y)까지 Bash와 동일하게 동작하도록 맞춰졌다. 현재 `settings.json`에 이 키가 없어 기본 동작을 쓰고 있다. zsh를 쓰는 환경이라 터미널 편집 습관과 프롬프트 편집이 일치하면 긴 프롬프트 수정이 빨라진다. `"keybindingFlavor": "readline"` 한 줄 추가로 끝난다.
- **난이도**: ★☆☆ (약 5분)

### 4. `claudeMdExcludes` 심볼릭 링크 패턴 점검
- **파일**: `~/.claude/settings.json`, `~/.claude/rules`
- **근거**: 패턴이 심볼릭 링크나 rules 디렉터리를 지목할 때 제외가 먹지 않던 버그가 고쳐졌다. 지금은 `settings.json`에 `claudeMdExcludes`가 없으니, `CLAUDE.md`가 4KB 한도에 근접하게 커진 상황(§6 Deploy 정책·§7 페르소나 라우팅이 대부분 차지)에서 프로젝트별로 안 쓰는 rules 파일을 제외해 컨텍스트를 줄일 여지가 있다. 먼저 `.claude/rules` 존재 여부와 심볼릭 링크인지 확인하고, 있으면 제외 패턴을 대상 파일 경로로 지정한다.
- **난이도**: ★★☆ (약 15분)
