# Claude Code v2.1.250

> 작성일: 2026-08-29

---

# 📋 요약본

## 🛠️ 개선/수정 (1건)
- **버그 수정 및 안정성 개선** — 세부 항목이 공개되지 않은 유지보수 릴리스. 개별 기능 변경 없이 기존 동작의 오류와 불안정성을 정리했다.

## 🔑 이번 버전의 핵심 키워드
**"조용한 안정화 릴리스"** — 새 기능 없이 버그 수정과 신뢰성 개선만 담긴 버전이다.

---

# 📜 원문 (한글 번역본)

> 원문 ChangeLog를 원래 순서 그대로 한 줄도 빠짐없이 번역한 문서입니다.

- 버그 수정 및 신뢰성 개선

---

## 🎯 챌린지

이번 버전에서 내 환경에 적용해볼 만한 항목입니다.

### 1. 비활성 플러그인 정리
- **파일**: `~/.claude/settings.json`
- **근거**: `enabledPlugins` 에 `false` 로 꺼둔 플러그인이 6개 쌓여 있다. 안정화 릴리스는 설정 잔여물을 정리하기 좋은 시점이다. 쓰지 않는 항목과 대응하는 `extraKnownMarketplaces` 를 함께 제거하면 시작 시 마켓플레이스 조회가 줄어든다.
- **난이도**: ★☆☆ (약 10분)

### 2. statusLine 경로 검증
- **파일**: `~/.claude/settings.json`
- **근거**: `statusLine` 이 `token-optimizer` 플러그인의 `5.8.8` 버전 경로를 절대 경로로 하드코딩하고 있는데, 정작 해당 플러그인은 `false` 로 비활성 상태다. 플러그인 캐시가 정리되면 매 턴 statusline 실행이 실패한다. 경로 존재를 확인하고 없으면 `statusLine` 을 제거한다.
- **난이도**: ★☆☆ (약 5분)

### 3. 업데이트 후 hook 동작 확인
- **파일**: `~/.claude/hooks/deploy-guard.sh`
- **난이도**: ★★☆ (약 15분)
- **근거**: 안정성 수정이 hook 실행 경로에 영향을 줄 수 있다. `feat/test-guard` 같은 임시 브랜치에서 `git push origin feat/test-guard` 를 시도해 차단이 여전히 동작하는지 직접 확인한다. 이 hook은 원격에 작업 브랜치가 올라가는 사고를 막는 마지막 방어선이라 버전 업 직후 한 번은 확인할 값어치가 있다.
