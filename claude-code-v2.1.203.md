# Claude Code v2.1.203

> 작성일: 2026-07-08

---

# 📋 요약본

## 🎉 신기능 (3건)
- **로그인 만료 경고** — 로그인이 곧 만료되기 전에 경고를 띄운다. 백그라운드 세션이 끊기기 전에 미리 재인증할 수 있다.
- **수동 권한 모드 ⏸ 배지** — 수동 권한 모드일 때 하단 푸터에 회색 ⏸ 배지를 표시해 현재 모드를 항상 볼 수 있게 했다.
- **MCP `roots/list`에 추가 작업 디렉터리 반영** — 세션의 추가 작업 디렉터리를 MCP `roots/list`에 포함하고, 디렉터리 집합이 바뀌면 `notifications/roots/list_changed`를 보낸다.

## 🛠️ 개선/수정 (34건)
- **macOS 백그라운드 세션 stall 수정** — 잘못된 저메모리 감지로 세션 열기·전환이 15~20초 멈추던 문제(2.1.196 회귀)를 해결.
- **백그라운드 세션 복구력 강화** — 데몬의 세션 토큰이 낡아 attach·응답·중지가 영구 먹통 되던 문제(이제 자동 복구), 작업 디렉터리가 삭제·교체·무효화됐을 때의 crash-loop, 데몬 자동 업그레이드가 실행 중 세션을 조용히 죽이던 문제를 수정.
- **subagent 작업 이어받기** — `claude agents`로 돌아갈 때 실행 중 subagent를 조용히 멈추고 프롬프트를 처음부터 다시 돌리던 문제 수정. 작업이 그대로 이어진다.
- **subagent 재위임 억제** — 에이전트가 자기 작업 전체를 또 다른 subagent에 떠넘기는 경향 완화.
- **context 사용량 표시기 CPU/메모리 회귀 수정** — 매 턴 전체 트랜스크립트를 재분석하던 문제 해결.
- **worktree 격리 정확도** — 격리된 subagent가 부모 체크아웃에서 shell 명령을 돌리던 문제, 중첩 저장소 거부 문제, 비-git 디렉터리·`WorktreeCreate` hook 환경에서 편집 불가 문제 수정.
- **환경변수 전달 수정** — 백그라운드 에이전트가 낡은 `PATH`를 물려받던(Windows) 문제, shell로 export한 `ANTHROPIC_BASE_URL`을 잃고 기본 엔드포인트로 API 키를 보내 401 나던 문제 수정.
- **Bash "argument list too long" 수정** — git worktree가 많은 저장소에서 실패하던 문제 해결.
- **`TaskStop`/`TaskOutput` 수정** — 다른 에이전트가 띄운 백그라운드 에이전트를 못 찾던 문제. 이제 실행 중 에이전트를 id·설명으로 나열한다.
- **`claude agents` UI 안정화** — 컴포저가 입력 메시지를 버리던 문제, 이미 열린 대화를 다시 열 때 크래시, 답변 후에도 "Needs input" 표시, 시작 실패가 실제 에러 대신 "exit_with_message"만 보이던 문제, 빈 뷰 정리.
- **설정 반영 수정** — forked 백그라운드 세션이 `effortLevel` 변경을 무시하던 문제, attach된 세션이 `CLAUDE_CODE_DISABLE_MOUSE`/`CLAUDE_CODE_DISABLE_MOUSE_CLICKS` opt-out을 무시하던 문제 수정.
- **터미널 렌더링 개선** — 긴 히스토리 스크롤 시 내용 튐, bash 모드 입력 시 깜빡임·튐, 재attach 시 `^[[I`/`^[[O` 이스케이프 코드 출력, 긴 응답 스트리밍 시 전체 화면 재렌더 문제 해결.
- **LSP 전용 플러그인 오탐 수정** — 언어 서버가 진단·탐색을 처리 중인데도 미사용으로 잘못 플래그되던 문제 해결.
- **기타** — Windows에서 `/clear` 후 백그라운드 출력이 빈 파일로 대체되던 문제, `/exit`가 완료된 에이전트에도 경고하던 문제, `@` 디렉터리 선택기가 worktree를 안 보이던 문제 수정. 좌측 화살표로 뷰 닫기 제거(Esc 사용), 시작 경고를 `/doctor`·`/status`로 이동, 바이너리 ~7MB·시작 메모리 ~7MB 감소, [VSCode] "Enable Remote Control for all sessions" 토글 추가.

## 🔑 이번 버전의 핵심 키워드
**"백그라운드·subagent 세션 안정성 대청소"** — 멈춤·먹통·작업 유실·환경변수 손실을 걷어내 오래 돌리는 에이전트 워크플로우를 믿고 맡길 수 있게 한 버전.

---

# 📜 원문 (한글 번역본)

> 원문 ChangeLog를 원래 순서 그대로 한 줄도 빠짐없이 번역한 문서입니다.

- 로그인이 곧 만료될 때 경고를 추가해, 백그라운드 세션이 끊기기 전에 재인증할 수 있게 했다.
- 수동 권한 모드일 때 푸터에 회색 ⏸ 배지를 추가해 현재 활성 모드가 항상 보이게 했다.
- 세션의 추가 작업 디렉터리를 MCP `roots/list`에 추가하고, 집합이 바뀌면 `notifications/roots/list_changed`를 전송하도록 했다.
- macOS에서 잘못된 저메모리 감지로 백그라운드 에이전트 세션 열기·전환이 15~20초 멈추던 문제(2.1.196 회귀)를 수정했다.
- 데몬의 세션 토큰이 낡았을 때 백그라운드 세션이 attach·응답·중지에 영구 응답하지 않던 문제를 수정했다 — 이제 세션이 자동으로 복구된다.
- `claude agents`로 돌아갈 때 실행 중이던 subagent를 조용히 중지하고 프롬프트를 처음부터 다시 실행하던 문제를 수정했다 — 이제 작업이 그대로 이어진다.
- 인터랙티브 세션의 메모리·턴당 CPU 회귀를 수정했다: context 사용량 표시기가 더 이상 매 턴마다 전체 트랜스크립트를 재분석하지 않는다.
- 백그라운드 에이전트가 디스패치한 shell 대신 데몬의 낡은 `PATH`를 물려받아 Windows에서 도구가 누락되던 문제를 수정했다.
- 백그라운드·에이전트 뷰 세션이 shell로 export된 `ANTHROPIC_BASE_URL`을 잃어버려 API 키를 기본 엔드포인트로 보내 401로 실패하던 문제를 수정했다.
- git worktree가 많은 저장소에서 Bash가 "argument list too long"으로 실패하던 문제를 수정했다.
- worktree로 격리된 subagent가 자기 worktree 대신 부모 체크아웃에서 shell 명령을 실행하던 문제를 수정했다.
- 다중 저장소 워크스페이스에서 worktree 생성이 중첩 저장소를 거부해, 백그라운드 세션이 격리·편집을 못 하던 문제를 수정했다.
- 백그라운드 에이전트가 작업 디렉터리가 삭제·파일로 교체·무효 경로가 됐을 때 crash-loop 하던 문제를 수정했다 — 이제 명확한 에러와 함께 한 번만 실패한다.
- 백그라운드 데몬 자동 업그레이드 실패가 실행 중인 모든 백그라운드 세션을 조용히 죽이던 문제를 수정했다.
- `TaskStop`과 `TaskOutput`이 다른 에이전트가 띄운 백그라운드 에이전트를 못 찾던 문제를 수정했다 — 이제 에러가 실행 중 에이전트를 id·설명으로 나열한다.
- `claude agents` 컴포저가 슬래시 명령을 쓸 수 없는 곳에서 입력한 메시지를 버리던 문제를 수정했다.
- 이미 다른 세션에서 대화가 열려 있는 중지된 세션을 열 때 에이전트 목록이 크래시하던 문제를 수정했다.
- 질문에 이미 답한 뒤에도 에이전트 목록에서 백그라운드 세션이 "Needs input"으로 표시되던 문제를 수정했다.
- 백그라운드 에이전트 시작 실패가 실제 에러 대신 "exit_with_message"만 보여주던 문제를 수정했다.
- 데몬을 통해 forked될 때 백그라운드 세션이 settings.json의 `effortLevel` 변경을 무시하던 문제를 수정했다.
- attach된 백그라운드 세션이 `CLAUDE_CODE_DISABLE_MOUSE`·`CLAUDE_CODE_DISABLE_MOUSE_CLICKS` opt-out을 무시하던 문제를 수정했다.
- 이름 붙은 에이전트가 모두 완료된 뒤에도 `/exit`이 실행 중인 백그라운드 에이전트가 있다고 잘못 경고하던 문제를 수정했다.
- 비-git 디렉터리에서 시작된 백그라운드 세션이 `WorktreeCreate` hook이 설정돼 있을 때 파일을 편집하지 못하던 문제를 수정했다.
- `claude agents`의 `@` 디렉터리 선택기가 등록된 git worktree를 보여주지 않던 문제를 수정했다.
- Windows에서 `/clear` 이후 백그라운드 작업 출력이 빈 파일로 영구히 대체되던 문제를 수정했다.
- 긴 트랜스크립트 히스토리를 위로 스크롤할 때 내용이 튀던 문제를 수정했다.
- bash 모드에서 shell 히스토리 제안이 표시된 상태로 입력할 때 터미널이 깜빡이고 튀던 문제를 수정했다.
- 백그라운드 세션에 재attach할 때 리터럴 `^[[I`/`^[[O` 이스케이프 코드가 출력되던 문제를 수정했다.
- 언어 서버가 진단을 전달하거나 탐색 요청에 응답하고 있을 때도 LSP 전용 플러그인이 미사용으로 잘못 플래그되던 문제를 수정했다.
- 긴 응답이 스트리밍되는 동안의 반응성을 개선했다: 라이브 프리뷰 업데이트가 더 이상 전체 화면을 재렌더하지 않는다.
- subagent 동작을 개선했다: 에이전트가 자기 작업 전체를 또 다른 subagent에 재위임할 가능성을 낮췄다.
- 크게 번들되던 의존성을 인라인 대신 지연 로딩하도록 바꿔 바이너리 크기를 ~7MB, 시작 메모리를 ~7MB 줄였다.
- 좌측 화살표가 더 이상 백그라운드 작업·diff·워크플로우 상세 뷰를 닫지 않도록 변경했다 — 대신 Esc를 누른다.
- 빈 `claude agents` 뷰가 항상 정리된 섹션(Needs input / Working / Completed)을 설명과 함께 보여주도록 변경했다.
- 시작 시 뜨던 "claude command missing or broken" 경고를 제거했다 — 대신 `/doctor`와 `/status`에 나타난다.
- `claude agents` 푸터의 중복된 내비게이션 힌트를 제거했다.
- [VSCode] "Enable Remote Control for all sessions" 설정 토글을 추가했다.

---

## 🎯 챌린지

이번 버전에서 내 환경에 적용해볼 만한 항목입니다.

### 1. Claude Code 2.1.203으로 업그레이드
- **파일/명령**: `claude update` (또는 설치 방식에 맞는 업그레이드 명령)
- **근거**: 당신은 macOS 사용자이면서 매 프롬프트마다 `measure.py`로 context fill을 재는 `UserPromptSubmit` hook을 돌린다. 이 버전은 macOS 세션 15~20초 stall 회귀와 "context 사용량 표시기가 매 턴 전체 트랜스크립트를 재분석하던" CPU/메모리 회귀를 함께 고쳤다 — 둘 다 지금 환경에서 체감되는 부하다. 시작 메모리 ~7MB 감소도 덤.
- **난이도**: ★☆☆ (약 5분)

### 2. `~/.claude/settings.json`에 `effortLevel` 명시
- **파일**: `/Users/leeseonro/.claude/settings.json`
- **근거**: 당신은 모든 subagent를 Opus로 돌리고 백그라운드·병렬 디스패치를 자주 쓴다. 이번 버전은 데몬을 통해 forked된 백그라운드 세션이 `effortLevel`을 무시하던 버그를 고쳤다. `effortLevel`을 명시해두면 forked·백그라운드 세션까지 동일한 추론 깊이가 보장돼, "작은 실패의 downstream 전파"를 줄이려는 신뢰성 우선 원칙(§5)과 맞는다.
- **난이도**: ★☆☆ (약 5~10분)

### 3. 모노레포 형제 디렉터리를 `/add-dir`로 세션에 등록
- **파일/명령**: 세션 내 `/add-dir <경로>` (Poplus 30개 프로젝트 루트·형제 디렉터리)
- **근거**: 이번 버전은 세션의 추가 작업 디렉터리를 MCP `roots/list`에 반영하고 변경 시 알림까지 보낸다. 당신은 다수의 `claude.ai` MCP 서버를 붙인 채 30개 프로젝트 마이크로서비스 모노레포를 학습 중이므로, 관련 디렉터리를 추가 등록하면 MCP 도구들이 전체 프로젝트 루트를 인지해 탐색·참조 범위가 넓어진다.
- **난이도**: ★★☆ (약 10~15분)
