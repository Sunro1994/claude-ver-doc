# Claude Code v2.1.185

> 작성일: 2026-06-21

---

# 📋 요약본

## 🛠️ 개선/수정 (1건)
- **스트림 정체(stream-stall) 힌트 문구·타이밍 변경** — API 응답 지연 시 표시되는 안내가 `"No response from API · Retrying in …"`에서 `"Waiting for API response · will retry in …"`로 바뀌었다. 표현이 "응답 없음(재시도)"에서 "응답 대기 중(재시도 예정)"으로 완화됐다. 또한 표시 트리거가 무응답 10초에서 20초로 늦춰져, 짧은 지연에는 힌트가 뜨지 않는다.

## 🔑 이번 버전의 핵심 키워드
**"응답 지연 안내, 덜 놀라게 더 늦게"** — 스트림 정체 힌트의 문구를 완화하고 노출 시점을 10초에서 20초로 미뤘다.

---

# 📜 원문 (한글 번역본)

> 원문 ChangeLog를 원래 순서 그대로 한 줄도 빠짐없이 번역한 문서입니다.

- 스트림 정체 힌트가 이제 `"No response from API · Retrying in …"` 대신 `"Waiting for API response · will retry in …"`로 표시되며, 침묵 10초 후가 아니라 20초 후에 트리거된다.

---

## 🎯 챌린지

이번 버전에서 내 환경에 적용해볼 만한 항목입니다.

### 1. 컨텍스트 사용률 차단 임계값 점검
- **파일**: `~/.claude/settings.json`
- **근거**: 이번 버전은 "짧은 지연엔 굳이 알리지 않는다"는 방향으로 노출 임계값을 10초→20초로 늦췄다. 현재 `UserPromptSubmit` 훅의 컨텍스트 사용률 차단선이 `80%`로 고정돼 있는데, 같은 관점에서 이 값이 실제 작업 흐름을 끊지 않는 적정선인지 한번 재검토할 만하다. `>= 80` 숫자만 조정하면 된다.
- **난이도**: ★☆☆ (약 5분)

### 2. Stop 알림 문구를 작업 맥락 포함으로 개선
- **파일**: `~/.claude/settings.json`
- **근거**: 이번 버전이 안내 문구를 더 정확하게 다듬은 것과 같은 맥락이다. 현재 `Stop` 훅의 `osascript` 알림이 항상 `"작업이 완료되었습니다"` 고정 문구라, 여러 세션을 동시에 돌릴 때 어느 작업인지 구분되지 않는다. 알림 제목·본문에 디렉터리명 등 맥락을 추가하면 식별이 쉬워진다.
- **난이도**: ★★☆ (약 10분)
