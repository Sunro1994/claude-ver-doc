# Claude Code v2.1.228

> 작성일: 2026-08-14

---

# 📋 요약본

## 🎉 신기능 (2건)
- **claude.ai 동기화 스킬 보안 강화** — claude.ai에서 내려받은 스킬이 로컬 명령어나 MCP 프롬프트를 덮어쓰지 못한다. 설명문은 정제·표시되고, 본문의 `!` 명령 실행과 `@` 파일 확장이 로컬에서 차단된다.
- **크로스 세션 메시지 표시 개선** — 보낸 사람과 본문이 접힌 한 줄 대신 그대로 펼쳐져 보인다. 다른 기기의 Remote Control 세션으로 보낸 메시지는 발신자에 내 Remote Control 세션 이름이 표시된다.

## 🛠️ 개선/수정 (16건)
- **화면 멈춤 수정** — 드문 내부 레이아웃 오류 뒤 프로세스는 살아 있는데 화면만 다시 안 그려지던 문제를 고쳤다.
- **Windows `git` 탐지** — git 설치 폴더의 상위 폴더에서 Claude Code를 실행하면 `git` / Git Bash를 못 찾던 문제를 고쳤다.
- **`/tui` 모델 되돌림** — 마지막 응답 이후 `/model`을 바꿨는데 `/tui`가 이전 모델로 되돌리던 문제를 고쳤다.
- **첫 세션 inbox 누락** — 설치·업그레이드 직후 첫 세션에서 크로스 세션 메시지가 inbox 없이 시작되던 문제를 고쳤다.
- **Remote Control `/resume` 정보 유출** — 연결 중 `/resume` 시 복원된 대화의 제목·기록이 연결된 세션으로 새던 문제를 고쳤다.
- **self-hosted runner `checkout` 실패** — 세션이 push하지 않는 레포에서 `checkout` 훅이 실패하면 모든 새 runner 세션이 죽던 문제를 고쳤다. 이제 그 레포는 경고와 함께 건너뛴다.
- **self-hosted runner 조기 종료** — 백그라운드 작업이 끝나고 후속 턴이 시작되기 전 틈에서 세션이 종료되던 문제를 고쳤다.
- **메모리 폴더 삭제** — 세션 정리가 프로젝트 memory 폴더 안 내용을 지우던 문제를 고쳤다.
- **플러그인 캐시 오삭제** — 유일한 버전이 심볼릭 링크된 개발 체크아웃일 때 백그라운드 정리가 그 캐시를 지우던 문제를 고쳤다.
- **설정 병합 오류** — 상위 우선순위 설정 계층에서 재정의된 marketplace 항목이 다른 계층의 커스텀 헤더를 물려받던 문제를 고쳤다. 이제 marketplace 항목은 항목 전체 단위로 병합된다.
- **deferred-tools 알림 중복** — 스킬 호출 후 모델에 두 번 전달되던 문제를 고쳤다.
- **Vertex AI 자격 증명 처리** — 만료·누락된 Google Cloud 자격 증명이 몇 분간 재시도하지 않고 몇 초 안에 실패한다.
- **compaction 진행 표시** — 진행 바만 보이던 것에서, 재시도 카운트다운과 지연 힌트가 compaction 중에도 표시된다.
- **터미널 제목 스피너** — busy 스피너 글리프를 바꿔 일부 터미널의 탭 바 흔들림을 줄였다.
- **Write 도구 규칙 완화** — 최신 모델은 이번 세션에 읽지 않은 기존 파일도 덮어쓸 수 있다. Edit 도구 규칙과 동일하다. 구버전 모델은 여전히 먼저 읽어야 한다.
- **auto 모드 비용 안내 제거** — Pro·Max·Team 플랜 최초 사용 안내에서 "auto 모드 세션은 비용이 조금 더 든다"는 낡은 문구를 뺐다.

## 🔑 이번 버전의 핵심 키워드
**"삭제 사고와 정보 유출을 막는 안정화 릴리스"** — 메모리 폴더·플러그인 캐시 오삭제, Remote Control 기록 유출, runner 조기 종료를 한 번에 정리했다.

---

# 📜 원문 (한글 번역본)

> 원문 ChangeLog를 원래 순서 그대로 한 줄도 빠짐없이 번역한 문서입니다.

- 드문 내부 레이아웃 오류 이후 프로세스는 계속 실행되지만 대화형 세션이 화면 갱신을 완전히 멈추던 문제를 수정
- git 설치 위치의 상위 폴더에서 Claude Code를 실행할 때 Windows에서 `git` / Git Bash를 찾지 못하던 문제를 수정
- 마지막 응답 이후 `/model`이 변경된 경우 `/tui`가 세션을 이전 모델로 되돌리던 문제를 수정
- 설치 또는 업그레이드 후 첫 세션에서 크로스 세션 메시징이 inbox 없이 시작되는 경우가 있던 문제를 수정
- 연결 상태에서의 Remote Control `/resume`이 복원된 대화의 제목이나 기록을 연결된 세션으로 유출하던 문제를 수정
- 세션이 push하지 않는 레포지토리에 대해 `checkout` 훅이 실패하면 모든 신규 runner에서 `claude self-hosted-runner` 세션이 실패하던 문제를 수정. 이제 해당 레포지토리는 경고와 함께 건너뛴다
- 백그라운드 작업 종료와 후속 턴 시작 사이의 틈에서 self-hosted runner가 세션을 끝내던 문제를 수정
- 세션 정리가 프로젝트 memory 폴더 내부의 내용을 삭제하던 문제를 수정
- 플러그인의 유일한 버전이 심볼릭 링크된 개발 체크아웃일 때 백그라운드 플러그인 캐시 정리가 그 캐시를 삭제하던 문제를 수정
- 상위 우선순위 설정 계층에서 재정의된 marketplace 항목이 다른 계층의 커스텀 헤더를 상속받을 수 있던 설정 병합 문제를 수정. 이제 marketplace 항목은 항목 전체 단위로 병합된다
- 스킬 호출 후 deferred-tools 알림이 모델에 두 번 전송되는 경우가 있던 문제를 수정
- claude.ai에서 동기화된 스킬을 강화: 로컬 명령이나 MCP 프롬프트를 더 이상 가리지 않으며, 설명은 정제·표시되고, 사용자 기기에서 스킬 본문이 `!` 명령을 실행하거나 `@` 파일을 확장하지 않는다
- 크로스 세션 메시지 개선: 보낸 사람과 본문이 접힌 줄 대신 인라인으로 표시되고, 다른 기기의 Remote Control 세션으로 보내는 메시지는 발신자로 내 Remote Control 세션 이름을 표시한다
- Vertex AI 자격 증명 처리 개선: 만료되었거나 없는 Google Cloud 자격 증명이 몇 분간 재시도하지 않고 몇 초 안에 실패한다
- compaction 진행 표시 개선: 진행 바만 나오던 것에서, 재시도 카운트다운과 지연 힌트가 compaction 중에 표시된다
- 일부 터미널의 탭 바 흔들림을 줄이기 위해 터미널 제목의 busy 스피너 글리프를 변경
- Write 도구를 변경해 최신 모델은 이번 세션에 읽지 않은 기존 파일을 덮어쓸 수 있게 함 (Edit 도구 규칙과 동일). 구버전 모델은 여전히 먼저 읽어야 한다
- Pro, Max, Team 플랜의 최초 사용 안내에서 auto 모드 세션이 비용이 조금 더 든다는 낡은 문구를 제거

---

## 🎯 챌린지

이번 버전에서 내 환경에 적용해볼 만한 항목입니다.

### 1. memory 폴더 백업 스냅샷 남기기
- **파일**: `/Users/leeseonro/.claude/projects/-/memory/`
- **근거**: 이번 버전이 "세션 정리가 memory 폴더 내부를 삭제"하던 버그를 고쳤다. 내 memory에는 MEMORY.md 인덱스와 20개 이상의 기억 파일이 쌓여 있어 유실 시 복구가 어렵다. 이 폴더를 git repo로 만들거나 `tar` 스냅샷을 한 번 떠 두면 같은 종류의 사고에 대비된다.
- **난이도**: ★☆☆ (약 7분)

### 2. `cleanupPeriodDays` 값 재검토
- **파일**: `/Users/leeseonro/.claude/settings.json`
- **근거**: 세션 정리 관련 삭제 버그가 두 건(memory 폴더, 플러그인 캐시) 고쳐졌다. 현재 `cleanupPeriodDays: 30`으로 자동 정리가 돌고 있으니, 업그레이드가 실제로 반영됐는지 `claude --version`으로 확인한 뒤 값을 유지할지 늘릴지 정한다.
- **난이도**: ★☆☆ (약 5분)

### 3. 심볼릭 링크 플러그인 체크아웃 점검
- **파일**: `/Users/leeseonro/.claude/plugins/cache/`
- **근거**: 유일한 버전이 심볼릭 링크된 개발 체크아웃인 플러그인의 캐시가 지워지던 버그가 고쳐졌다. 내 환경엔 marketplace 4개·플러그인 8개가 등록돼 있으니 `ls -l`로 캐시 안에 심볼릭 링크가 있는지, 비활성 플러그인 캐시가 남아 있는지 한 번 훑고 정리한다.
- **난이도**: ★☆☆ (약 8분)

### 4. marketplace 항목 계층 중복 정리
- **파일**: `/Users/leeseonro/.claude/settings.json`
- **근거**: marketplace 항목이 이제 항목 전체 단위로 병합된다. `extraKnownMarketplaces`에 4개(`karpathy-skills`, `alexgreensh-token-optimizer`, `understand-anything`, `claude-plugins-official`)가 있는데, 프로젝트별 `.claude/settings.json`에 같은 이름이 재정의돼 있으면 이번 변경으로 병합 결과가 달라진다. 중복 정의를 찾아 한 계층으로 모은다.
- **난이도**: ★★☆ (약 15분)
