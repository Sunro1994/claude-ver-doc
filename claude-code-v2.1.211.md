# Claude Code v2.1.211

> 작성일: 2026-07-19

---

# 📋 요약본

## 🎉 신기능 (1건)
- **`--forward-subagent-text` / `CLAUDE_CODE_FORWARD_SUBAGENT_TEXT`** — 서브에이전트의 텍스트와 thinking을 stream-json 출력에 포함시킨다. 헤드리스·스크립트 환경에서 서브에이전트가 실제로 무슨 판단을 했는지 관측할 수 있다.

## 🛠️ 개선/수정 (36건)
- **서브에이전트 모델 override 회귀 수정** — 명시적 모델로 띄운 서브에이전트가 resume·후속 메시지 시 부모 모델로 되돌아가던 문제를 고쳤다.
- **PreToolUse 훅 `ask` 존중** — auto 모드가 unsandboxed Bash에서 훅의 `ask` 결정을 덮어쓰지 않고 최소 프롬프트로 고정한다.
- **권한 미리보기 유니코드 무력화** — 채팅 채널 승인 메시지에서 양방향 재정의·zero-width·유사 따옴표 문자를 중화해 도구 입력이 승인문을 시각적으로 조작하지 못하게 했다.
- **병렬 세션 동시 로그아웃 수정** — credential store를 공유하는 세션들이 절전 해제 후 한꺼번에 로그아웃되던 문제를 고쳤다.
- **중첩 `.claude/rules/*.md` 로딩 수정** — 설정 소스가 프로젝트 설정을 제외해도 규칙 파일이 로드되던 문제를 막았다.
- **프롬프트 캐싱 과금 회귀 수정** — Bedrock·Vertex·Mantle·Foundry에서 후행 시스템 블록을 매 요청 새 입력 토큰으로 청구하던 문제를 고쳤다.
- **백그라운드 에이전트 결과 보고 개선** — 실행 중인 에이전트 상태를 보고하고 결과를 지어내지 않으며 실제 완료를 기다린다.
- **권한 규칙 저장소 루트 저장** — 워크트리에서 부여한 "always allow"가 세션·워크트리 전반에 유지된다.
- **정수 환경변수 표기 확장** — 타임아웃·토큰 예산·재시도 횟수가 `1e6`·`64_000` 표기를 허용한다.
- **메모리 인덱스 경고 정확화** — frontmatter·HTML 주석을 빼고 로드된 콘텐츠만 측정한다.
- **`/clear` 비용 카운터 초기화** — statusline 비용이 `/clear` 후 $0에서 시작한다.
- **MCP 재연결·`/loop`·백그라운드 세션** 등 자잘한 신뢰성 결함 다수 수정.

## 🔑 이번 버전의 핵심 키워드
**"서브에이전트·백그라운드 에이전트·훅·과금의 신뢰성을 조용히 다지는 안정화 릴리스"** — 새 기능은 하나뿐, 나머지는 에이전트 실행과 청구의 정확성을 메우는 수정이다.

---

# 📜 원문 (한글 번역본)

> 원문 ChangeLog를 원래 순서 그대로 한 줄도 빠짐없이 번역한 문서입니다.

- `--forward-subagent-text` 플래그와 `CLAUDE_CODE_FORWARD_SUBAGENT_TEXT` 환경변수를 추가해, 서브에이전트의 텍스트와 thinking을 stream-json 출력에 포함할 수 있게 했다.
- 채팅 채널로 전달되는 권한 미리보기가 양방향 재정의(bidirectional-override)·zero-width·유사 따옴표 문자를 무력화하지 못하던 문제를 수정 — 이제 도구 입력이 승인 메시지를 시각적으로 조작할 수 없다.
- auto 모드가 unsandboxed Bash에 대한 PreToolUse 훅의 `ask` 결정을 덮어쓰던 문제를 수정 — 이제 훅의 `ask`는 결정을 최소 프롬프트(prompt)로 고정한다(floor).
- 여러 세션이 하나의 credential store를 공유할 때, 절전 해제 후 병렬 Claude Code 세션이 모두 동시에 로그아웃되던 문제를 수정.
- 유휴 상태의 웹 세션이 깨어난 뒤 플러그인 MCP 서버가 재연결되지 않아, 다음 메시지 전까지 MCP 호출이 계속 실패하던 문제를 수정.
- Vertex와 Bedrock의 Claude Code가 모델을 명시적으로 설정했는데도 시작 시 기본 Opus 모델을 시도하고 잘못된 fallback 알림을 출력하던 문제를 수정.
- 명시적 모델 override로 생성된 서브에이전트가 resume되거나 후속 메시지를 받으면 부모의 모델로 되돌아가던 문제를 수정.
- 설정 소스가 프로젝트 설정을 제외해도 중첩된 `.claude/rules/*.md` 파일이 로드되던 문제를 수정.
- 파일 업로드 검증 수정: DOS 장치 접미사(`.prn`)나 끝의 점(trailing dot)으로 끝나는 파일명을 이제 허용하고, 하드 링크가 여러 개인 파일은 거부한다.
- 원격·CLI 세션에서 Chrome의 Claude로 파일을 업로드하는 기능을 수정.
- 입력을 "?"로 남기는 편집이 조용히 삼켜지며 단축키 패널을 토글하던 문제를 수정.
- Claude in Chrome 확장이 활성화됐지만 Chrome이 실행 중이 아닐 때 시작 시 멈추던(hang) 문제를 수정.
- 비동기 콘텐츠(설정 탭, Stats, diff 뷰 및 기타 로딩 상태)를 드러내는 300ms 지연을 수정.
- agents 뷰에서 방금 중지된 백그라운드 세션을 다시 여는 것이 같은 세션 id로 빈 대화를 시작하던 문제를 수정.
- `/loop`이 한 번 사용 후 해당 세션을 `/resume`에서 숨기던 문제를 수정.
- 스크린 리더 사용자가 `/terminal-setup`이나 온보딩 터미널 설정 후 들리는 터미널 벨을 잃던 문제를 수정.
- LLM 게이트웨이 인증(`ANTHROPIC_AUTH_TOKEN` + `ANTHROPIC_BASE_URL`)의 백그라운드 작업이 데몬 재생성(respawn) 후 "Not logged in"으로 돌아오던 문제를 수정.
- git이 워크트리를 더 이상 인식하지 못할 때 `claude agents` 작업이 영구히 삭제 불가가 되던 문제를 수정 — 이제 해당 행이 조용히 다시 나타나는 대신 삭제가 거부된 이유를 표시한다.
- `/clear`가 세션 비용 카운터를 초기화하지 않던 문제를 수정 — 이제 `/clear` 후 statusline의 비용이 $0에서 시작한다.
- Windows에서 Claude in Chrome 설정 페이지가 브라우저에서 열리지 않던 문제를 수정.
- Windows에서 stdin을 읽을 수 없을 때 헤드리스 print 모드 세션이 크래시하거나 조용히 종료되던 문제를 수정.
- 프롬프트에 링크가 포함될 때 agents 뷰의 백그라운드 세션 제목이 명명(naming) 모델의 거부 텍스트를 표시하던 문제를 수정.
- 사용자가 종료한 백그라운드 에이전트가 자동 재생성되고, 되살아난 에이전트가 옛 세션의 오래된 프롬프트를 다시 실행하던 문제를 수정.
- 스케줄이 없는 routine이 다음 실행 시각을 서기 1년(year 1)으로 보고하던 문제를 수정.
- Windows에서 동기화된 skill/plugin 디렉터리 명명을 강화하고, `/clear` 후에도 CCR 웹 fetch/search 프록시가 계속 동작하도록 유지.
- 터미널 레이아웃과 렌더링 성능 개선.
- 백그라운드 에이전트 결과 보고 개선 — Claude가 이제 아직 실행 중인 에이전트의 상태를 보고하고, 결과를 지어내는 대신 실제 완료를 기다린다.
- 메모리 인덱스 초과 경고를 개선해, frontmatter와 HTML 주석을 제외한 로드된 콘텐츠만 측정한다.
- 정수형 환경변수(타임아웃, 토큰 예산, 재시도 횟수)가 `1e6`·`64_000` 같은 과학적 표기와 자릿수 구분 표기를 허용하도록 업데이트.
- 문서 링크를 현재 문서 사이트로 업데이트.
- "always allow" 권한 규칙을 저장소 루트에 저장하도록 변경 — git 워크트리에서 부여한 승인이 세션과 워크트리 전반에 유지된다.
- `/usage-credits`가 조직 관리자에게 요청을 보내기 전에 확인을 묻도록 변경.
- Vim 모드의 `s`·`S`(문자/줄 치환)가 NORMAL 모드에서 동작하도록 변경 — vim 동작과 일치.
- [VSCode] Remote Control 배너가 그 기능을 설명하도록 업데이트.
- Claude in Chrome: 파일 업로드 경로 검증을 강화.
- Claude in Chrome: 스크린샷 액션의 `save_to_disk`가 이제 이미지를 디스크에 쓰고 경로를 반환한다 — 이전에는 아무 동작도 하지 않았다.
- Bedrock·Vertex·Mantle·Foundry에서 후행 시스템 컨텍스트 블록을 매 요청마다 새 입력 토큰으로 청구하던 프롬프트 캐싱 회귀(regression)를 수정.

---

## 🎯 챌린지

이번 버전에서 내 환경에 적용해볼 만한 항목입니다.

### 1. 서브에이전트 텍스트/thinking 스트림 노출
- **파일**: `~/.claude/settings.json` (`env` 블록에 `"CLAUDE_CODE_FORWARD_SUBAGENT_TEXT": "1"` 추가)
- **근거**: 이번 버전 신기능. 당신은 모든 subagent를 Opus로 돌리고 CLAUDE.md §5에서 "subagent 출력을 검증하고 행동하라"고 규정한다. 이 값을 켜면 stream-json 출력에 서브에이전트의 판단 과정이 실려, downstream 전파 전에 무엇을 근거로 판단했는지 확인할 수 있다.
- **난이도**: ★☆☆ (약 5분)

### 2. 워크트리 간 permission 규칙 저장소 루트로 통합
- **파일**: `.claude/settings.json` (저장소 루트)
- **근거**: "always allow 규칙을 저장소 루트에 저장" 변경 덕에 워크트리에서 부여한 승인이 이제 전 워크트리에 유지된다. 당신은 `superpowers:using-git-worktrees`로 격리 작업을 자주 한다 — 워크트리별 `.claude/settings.local.json`에 흩어진 allow 규칙이 있으면 루트로 옮겨 매 워크트리마다 다시 승인하는 낭비를 없앤다.
- **난이도**: ★★☆ (약 15분)

### 3. MEMORY.md 인덱스 길이 재점검
- **파일**: `~/.claude/MEMORY.md`
- **근거**: 메모리 인덱스 초과 경고가 이제 frontmatter·HTML 주석을 빼고 로드된 콘텐츠만 정확히 측정한다. 당신의 MEMORY.md는 이미 4KB 표시 한도에서 잘릴 만큼 길다 — 개선된 경고가 주는 정확한 신호로 실제 로드량을 확인하고, 오래된 포인터를 정리해 매 세션 컨텍스트 부담을 줄인다.
- **난이도**: ★★☆ (약 15분)
