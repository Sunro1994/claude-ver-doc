# Claude Code v2.1.265

> 작성일: 2026-09-10

---

# 📋 요약본

## 🎉 신기능 (5건)
- **`--plugin-dir`에 플러그인 폴더 지정 지원** — 하위 폴더마다 매니페스트가 있으면 각각 플러그인으로 로드된다. 실행 중 하위 폴더를 추가·삭제해도 반영된다.
- **디스크 저장 도구 결과에 1GB 상한** — 상한을 넘겨 잘린 경우 대화 내 미리보기에 잘렸다고 표시된다.
- **Claude Desktop·Cowork 텔레메트리에 `user.email`·`user.groups` 추가** — Claude apps 게이트웨이를 통해 전송되며 터미널 세션과 동일한 필드를 갖는다.
- **[VSCode] 비활성 세션 자동 보관** — 일정 기간 쓰지 않은 세션을 자동으로 아카이브한다. "Archive inactive sessions" 설정 신설, 기본 14일.
- **`--worktree` 병렬 체크아웃** — 대형 레포에서 새 worktree를 병렬로 체크아웃해 시작이 빨라진다(git 2.32+).

## 🛠️ 개선/수정 (44건)
- **프롬프트 캐시 재사용 깨짐 3종 수정** — ①foreground로 띄운 subagent를 resume하면 도구 목록·시스템 프롬프트 앞부분이 바뀌던 문제 ②에이전트 팀메이트·resume된 subagent가 이후 턴에서 `SubagentStart` 훅 컨텍스트와 미리 로드한 스킬을 프롬프트 앞부분 밖으로 밀어내던 문제를 고쳤다.
- **도구 실행 중 프로세스가 죽은 뒤 resume 수정** — 마지막 프롬프트를 다시 쓰지 않고, 중단된 도구 호출을 남긴 채 "중단됨"으로 표시한다.
- **`/model opusplan[1m]`이 "Model not found"로 거부되던 문제 수정**
- **문법 강조 문자 누락 수정** — 권한 프롬프트·메시지에서 Ruby `?`, Erlang `$`, Perl `$` 기호 뒤 한 글자가 사라지던 문제.
- **전체화면 트랜스크립트 흔들림 수정** — 슬래시 명령·`@`파일 제안 목록이 열리고 닫힐 때 한 줄씩 튀던 문제.
- **플러그인 경로 보안 2건** — 역슬래시가 든 경로가 macOS·Linux에서 심볼릭 링크 격리 검사를 우회하던 문제, 이름이 점 두 개로 시작하는 플러그인 디렉토리가 루트 밖으로 잘못 판정되던 문제.
- **로그인·세션 안정화** — VS Code·SDK 세션이 토큰 갱신 중 종료되면 재로그인을 요구하던 문제, `--bg` 세션이 유휴 타임아웃 직전 메시지 도착 시 턴 도중 종료되던 문제.
- **중첩 레포 clean filter 실행 방지** — Claude Code 자체 git status·diff 조사가 작업 트리 안 중첩 레포의 clean filter를 실행하던 문제.
- **MCP `http` 전송 폴백** — 구형 HTTP+SSE만 지원하는 서버에 아예 연결되지 않던 문제. MCP 스펙대로 SSE로 폴백한다.
- **비대화형 세션의 `cd` 유지** — `-p` + stream-json 입력·Agent SDK·클라우드 세션에서 새 사용자 메시지마다 셸 작업 디렉토리가 초기화되던 문제.
- **Windows 샌드박스 파일 접근 수정** — AppContainer·제한 토큰 샌드박스 안에서 Read·Write·Edit가 모든 파일을 거부하던 문제.
- **2키 단축키 대기 시간 3초로 연장** — tmux처럼 두 번째 키가 1초 넘게 늦으면 조용히 취소되던 문제. 이제 시간이 초과되면 알림을 띄운다.
- **`/workflows` 상세 화면 개선** — 도구 호출을 실행 중·실패·완료로 표시하고, subagent의 할 일 목록을 보여주며, Enter로 호출 입력·결과를 펼친다.
- **프롬프트 도중 슬래시 명령 개선** — 단일 제안 대신 목록으로 보여주고(전체화면 밖에선 Tab으로 열기), 플러그인 스킬을 이름만으로 찾는다.

## 🔑 이번 버전의 핵심 키워드
**"캐시와 resume을 붙잡는 버전"** — 프롬프트 캐시가 깨지던 경로와 중단된 세션 복구를 집중적으로 고치고, 플러그인 로딩·경로 검사를 정리했다.

---

# 📜 원문 (한글 번역본)

> 원문 ChangeLog를 원래 순서 그대로 한 줄도 빠짐없이 번역한 문서입니다.

- Claude Desktop과 Cowork가 Claude apps 게이트웨이를 통해 보내는 텔레메트리에 `user.email`과 `user.groups`를 추가해 터미널 세션과 맞췄다
- `--plugin-dir`를 플러그인 폴더로 지정하는 것을 지원한다. 매니페스트가 있는 각 하위 폴더가 로드되고, 실행 중 추가·삭제된 하위 폴더도 반영된다
- 디스크에 저장되는 도구 결과에 1GB 상한을 두었다. 저장된 파일이 잘린 경우 대화 내 미리보기가 이를 알린다
- foreground로 띄운 subagent를 resume할 때 도구 목록과 시스템 프롬프트 앞부분이 바뀌어 해당 에이전트의 프롬프트 캐시 재사용이 깨지던 문제를 고쳤다
- 에이전트 팀메이트와 resume된 subagent가 이후 턴에서 `SubagentStart` 훅 컨텍스트와 미리 로드한 스킬을 프롬프트 앞부분 밖으로 옮겨 프롬프트 캐시 재사용이 깨지던 문제를 고쳤다
- 도구 실행 중 이전 프로세스가 죽은 뒤의 resume을 고쳤다. 마지막 프롬프트를 다시 쓰지 않으며, 중단된 도구 호출은 유지되고 중단됨으로 표시된다
- `/model opusplan[1m]`이 "Model not found"로 거부되던 문제를 고쳤다
- 권한 프롬프트와 메시지의 문법 강조 코드에서 Ruby `?`, Erlang `$`, Perl `$` 기호 뒤 한 글자가 가끔 빠지던 문제를 고쳤다
- 슬래시 명령이나 `@`파일 제안 목록이 열리고 닫힐 때마다 전체화면 트랜스크립트가 한 줄씩 튀던 문제를 고쳤다
- 역슬래시가 포함된 플러그인 경로가 macOS와 Linux에서 심볼릭 링크 격리 검사를 우회하던 문제를 고쳤다
- 이름이 점 두 개로 시작하는 플러그인 디렉토리가 플러그인 루트 밖이라고 잘못 거부되던 문제를 고쳤다
- 토큰 갱신 중 세션이 닫히면 VS Code와 SDK 세션이 가끔 재로그인을 요구하던 문제를 고쳤다
- Remote Control 세션이 응답의 마지막 메시지보다 먼저 턴 종료 신호를 보내 마지막 부분이 도착하기 전에 Claude 앱에서 응답이 끝난 것처럼 보이던 문제를 고쳤다
- 유휴 타임아웃 직전에 메시지가 도착하면 백그라운드(`--bg`) 세션이 턴 도중 종료되던 문제를 고쳤다
- Claude Code 자체의 git status·diff 조사가 작업 트리 안 중첩 레포가 설정한 clean filter를 실행하던 문제를 고쳤다
- advisor 도구와 그 지침이 요청마다 요청의 모델로 다시 결정되던 문제를 고쳤다. 이제 한 번만 결정하고, 바뀌면 대화에 알린다
- 커넥터가 노출하지 않는 커넥터 도구 이름을 artifact publish가 받아들이던 문제를 고쳤다. 선언된 도구가 하나도 없으면 publish를 거부하고, 일부만 없으면 경고한다
- 관리 설정이 스킬만 플러그인으로 제한할 때 `/add-dir <subdirectory>`가 하위 디렉토리의 에이전트 로드를 거부하고, 에이전트만 제한됐을 때는 에이전트를 로드하겠다고 하던 문제를 고쳤다
- tmux 안에서처럼 두 번째 키가 1초 넘게 늦게 도착하면 2키 단축키가 조용히 취소되던 문제를 고쳤다. 이제 3초를 기다리고 시간이 초과되면 알림을 표시한다
- fork된 스킬(`context: fork`)이 킥오프 프롬프트를, 그리고 `--forward-subagent-text`와 함께 쓸 때 텍스트 턴을 stream-json의 진행 이벤트로 스트리밍하지 않던 문제를 고쳤다
- 심볼릭 링크 루프처럼 OS가 검사할 수 없는 플러그인 기본 컴포넌트 폴더가 조용히 건너뛰어지던 문제를 고쳤다. 이제 `/plugin`에서 에러 코드와 함께 표시된다
- Claude apps 게이트웨이의 OTLP 텔레메트리 중계가 몇 건의 페이로드를 형식 오류나 크기 초과로 거부한 뒤 컬렉터로의 전달을 30초간 전부 멈추던 문제를 고쳤다
- `/plugin`의 Discover/Browse와 `claude plugin list --json --available`이 메타데이터가 `plugin.json`에만 있는 마켓플레이스 플러그인의 설명·표시 이름을 보여주지 않던 문제를 고쳤다
- 관리 설정이 지정한 Claude apps 게이트웨이에 로그인한 세션에서 `/login`을 다시 실행하면 "no gateway URL is configured"가 뜨던 문제를 고쳤다
- 설정 파일을 쓰지 못했는데도 `/model`이 모델을 "saved as your default"라고 하던 문제를 고쳤다. 이제 저장 실패 사실과 이유를 알린다
- Remote Control에서 실행한 `/clear`가 SessionStart 훅과 열려 있는 터미널 대화상자를 기다린 뒤에야 끝나던 문제를 고쳤다
- `/config` 대화상자가 탭을 전환할 때 높이가 바뀌던 문제를 고쳤다
- 컨테이너 재시작 후 workflow 실행을 resume하는 것을 고쳤다. 실행 저널이 없는 resume은 이제 모든 에이전트를 다시 실행하지 않고 명확한 에러로 실패한다
- `claude-api` 스킬의 에러 코드 참조를 고쳤다. 모델 접근 실패는 403이 아니라 404, 지원되지 않는 beta 헤더는 400을 반환한다
- 비대화형 세션(`-p` + stream-json 입력, Agent SDK, 클라우드 세션)이 새 사용자 메시지마다 셸 작업 디렉토리를 초기화하던 문제를 고쳤다. 이제 `cd`가 턴을 넘어 유지된다
- `http`로 설정된 MCP 서버가 구형 HTTP+SSE 전송만 지원할 때 아예 연결되지 않던 문제를 고쳤다. 이제 MCP 스펙대로 SSE로 폴백한다
- 클라우드 세션의 일부 claude.ai 커넥터가 claude.ai에서 연결돼 있는데도 인증이 필요한 것처럼 보이던 문제를 고쳤다(지원되지 않는 요청에 HTTP 401로 답하는 서버)
- 커넥터 승인이나 로그인 링크가 사용자를 기다리는 동안 원격 세션이 샌드박스 컨테이너를 계속 살려두던 문제를 고쳤다
- resume된 세션이 "background task didn't finish" 알림에 짧은 상태 줄 대신 긴 모델용 복구 지침을 보여주던 문제를 고쳤다
- Windows: AppContainer나 제한 토큰 샌드박스 안에서 실행할 때 Read·Write·Edit가 모든 파일을 "symlink resolution changed after permission was checked"로 거부하던 문제를 고쳤다
- 대형 레포에서 `--worktree` 시작을 개선했다. 새 worktree를 병렬로 체크아웃한다(git 2.32+)
- `/workflows`의 에이전트 상세를 개선했다. 도구 호출을 실행 중·실패·완료로 표시하고, subagent에 할 일 목록이 있으면 보여주며, Enter로 나열된 호출을 입력·결과와 함께 펼친다
- 프롬프트 도중 입력하는 슬래시 명령을 개선했다. 이제 단일 제안 대신 목록으로 표시되고(전체화면 밖에서는 Tab으로 열기), 플러그인 스킬을 이름만으로도 찾는다
- 로그인이 필요한 원격 MCP 서버를 개선했다. 실제로 인증하기 전에는 Claude Code가 OAuth 클라이언트를 등록하지 않는다
- 파일을 많이 읽은 긴 세션의 resume 시간을 개선했다
- 크기 제한을 넘는 이미지를 디코딩할 수 없을 때의 에러를 개선했다. 제한만 알리는 대신 원인과 해결 방법을 알린다
- Artifact 도구가 다른 사람이 쓴 artifact를 읽을 때를 개선했다. 요약이 해당 페이지를 신뢰할 수 없는 콘텐츠로 다루고, 안에 심어진 지시를 전달하지 않고 표시한다
- `.claude` 폴더 권한 옵션의 문구를 실제 허용 범위에 맞게 수정했다. 세션 동안 프로젝트의 `.claude` 폴더(또는 `~/.claude`) 안 파일을 편집하는 것을 허용한다는 뜻이다
- 관리 설정에 `forceLoginGatewayUrl`이 있는 머신을 `forceLoginMethod: "gateway"`와 마찬가지로 시작부터 Claude apps 게이트웨이 세션으로 바꿨다. 남아 있는 claude.ai 로그인이나 API 키는 사용하지 않는다
- 이미지 처리를 런타임 내장 이미지 지원으로 바꿨다. CLI가 더 이상 네이티브 이미지 모듈을 임시 디렉토리에 풀지 않는다
- 플러그인 표시 메타데이터를 Installed 탭과 `claude plugin details`에서 `plugin.json`보다 마켓플레이스 항목을 우선하도록 바꾸고, 빠진 값은 `plugin.json`에서 채우도록 했다
- Claude apps 게이트웨이 세션이 게이트웨이 관리 설정의 `OTEL_EXPORTER_OTLP_ENDPOINT`가 지정한 컬렉터로 OpenTelemetry를 게이트웨이 중계 대신 직접 내보내도록 바꿨다. 컬렉터가 지정되지 않은 세션은 계속 중계를 쓴다
- [VSCode] 일정 기간 사용하지 않은 세션의 자동 보관을 추가했다(새 "Archive inactive sessions" 설정, 기본 14일)
- [VSCode] 대화를 10분 넘게 열어둔 뒤 Reload Window나 재시작을 하면 사이드바 채팅이 빈 화면으로 돌아오던 문제를 고쳤다
- [VSCode] "Remote Control is active" 메시지에서 타임라인 점이 글자 아래에 놓이던 문제를 고쳤다

---

## 🎯 챌린지

이번 버전에서 내 환경에 적용해볼 만한 항목입니다.

### 1. `--plugin-dir` 폴더 지정으로 로컬 스킬 묶음 관리
- **파일**: `~/.claude/settings.json`
- **근거**: 이번 버전부터 `--plugin-dir`가 플러그인 하나가 아니라 플러그인 **폴더**를 받는다. 현재 `enabledPlugins`에 마켓플레이스 플러그인 8개가 등록돼 있고 그중 6개는 꺼져 있다 — 자주 손보는 로컬 스킬을 `~/.claude/local-plugins/` 한 폴더에 모아두면 실행 중 추가·삭제도 즉시 반영돼 마켓플레이스를 거치지 않고 실험할 수 있다.
- **난이도**: ★★☆ (약 15분)

### 2. 프롬프트 캐시가 깨지던 subagent 경로 재확인
- **파일**: `~/.claude/CLAUDE.md` (§5 Model Routing)
- **근거**: 이번 버전이 `SubagentStart` 훅 컨텍스트·미리 로드한 스킬이 프롬프트 앞부분 밖으로 밀려 캐시 재사용이 깨지던 문제를 고쳤다. 모든 subagent를 Opus로 돌리는 설정(§5)에서는 캐시 미스 비용이 가장 크게 나오는 지점이다. `superpowers:subagent-driven-development`로 여러 에이전트를 띄우는 작업 한 번을 돌려 statusline 토큰 수를 이전과 비교해두면, 앞으로 캐시가 다시 깨질 때 바로 알아챌 수 있다.
- **난이도**: ★☆☆ (약 10분)

### 3. `/model opusplan[1m]` 사용 가능 여부 확인
- **파일**: `~/.claude/settings.json` (`model` 키)
- **근거**: 현재 `"model": "opus[1m]"`로 고정돼 있다. 이번 버전에서 `opusplan[1m]`이 "Model not found"로 거부되던 문제가 풀렸으므로, 계획 단계는 가볍게 두고 실행만 Opus로 돌리는 조합을 이제 실제로 쓸 수 있다. §7의 기획→구현 라우팅과 맞는 조합인지 한 번 시험해볼 값이 있다.
- **난이도**: ★☆☆ (약 5분)

### 4. 중첩 레포 clean filter 영향 점검
- **파일**: `~/EOS-H5/` 하위 레포들 (`update-repos.sh` 대상)
- **근거**: Claude Code의 git status·diff 조사가 작업 트리 안 중첩 레포의 clean filter를 실행하던 문제가 고쳐졌다. EOS-H5는 루트 아래 여러 git 레포가 중첩된 구조라 이 수정의 직접 대상이다. `/update-repos` 실행 후 git 상태 조회가 이전보다 빨라졌는지, 예상치 못한 필터 실행이 사라졌는지 확인해두면 이후 배포 작업 시간이 줄어든다.
- **난이도**: ★★☆ (약 15분)
