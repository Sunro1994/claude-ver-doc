# Claude Code v2.1.235

> 작성일: 2026-08-19

---

# 📋 요약본

## 🎉 신기능 (1건)
- **`spellcheck` 설정 추가** — 프롬프트 입력창에 오타를 밑줄로 표시한다. 선택 설정이며, 시스템에 설치된 `aspell`·`hunspell`·`ispell` 중 하나를 사용한다.

## 🛠️ 개선/수정 (18건)
- **language server 재연결 시 캐시 무효화** — 세션 중 언어 서버가 끊기거나 다시 붙을 때 전체 프롬프트 캐시가 통째로 날아가던 문제를 고쳤다.
- **마크다운 리스트 정렬** — 깊이 3 이상 중첩 리스트가 어긋나던 문제를 고쳤고, 줄바꿈된 리스트 항목에 hanging indent를 넣었다.
- **프롬프트 하이라이트 위치 어긋남** — 여러 줄 프롬프트에서 슬래시 명령·키워드·멘션 하이라이트가 한 글자 이상 밀려 보이던 문제를 고쳤다.
- **권한 창 Shift+Tab** — 권한 창 코멘트 입력란에서 Shift+Tab을 누르면 창만 닫히지 않고 편집을 승인하고 세션 전체 편집 권한까지 부여되던 문제를 고쳤다.
- **Agent 도구의 없는 기본값 광고** — general-purpose 에이전트가 없는 세션에서 `subagent_type`을 생략하면 이제 사용 가능한 에이전트 목록이 담긴 명확한 에러가 나온다.
- **노트북 셀 승인 창** — 노트북이나 셀을 읽지 못했을 때 기존 셀 내용을 조용히 빼먹던 문제를 고쳤다. 이제 이유를 표시한다.
- **응답 중 슬래시 명령 표시** — Claude가 답하는 중 실행한 슬래시 명령이 실제 문자 대신 HTML 엔티티로 보이던 문제를 고쳤다.
- **자동 업데이트 재시작 안내** — 백그라운드 자동 업데이트 후 "Update installed" 재시작 안내가 프롬프트 하단에 안 뜨던 문제를 고쳤다.
- **작업 목록 펼침 상태 유지** — 열린 작업이 남은 세션을 재개·재실행하면 `ctrl+t` 확장 목록이 항상 접힌 채 시작되던 문제를 고쳤다.
- **클라우드 세션 메모리·CPU** — `/ultrareview`·`/autofix-pr` 같은 클라우드 세션이 백그라운드로 돌 때 이벤트 스트림을 갱신마다 다시 스캔·렌더하지 않는다.
- **권한 창 정합성** — 표시 문구와 "don't ask again" 옵션이 실제 부여 범위와 항상 일치한다. 내용을 다 보여줄 수 없으면 "don't ask again"을 제공하지 않는다.
- **내장 `grep` 개선** — 네이티브 macOS/Linux 빌드에서 병적인 패턴이 메모리를 소진하는 대신 바로 실패하고, `-m N`과 `-A/-C` 조합이 올바른 컨텍스트를 출력한다.
- **컨텍스트 한계 에러 안내** — auto-compact가 꺼져 있으면 그 사실을 알려주고 `/config`로 다시 켜도록 안내한다.
- **Vim 모드 상태 유지** — 상세 트랜스크립트(ctrl+o) 토글이나 패널 종료 시 NORMAL 모드와 커서 위치가 유지된다.
- **다이얼로그 빠른 입력** — 방향키와 Enter를 연달아 누르면 이전에 하이라이트됐던 항목이 아니라 이동한 항목이 선택된다.
- **`SendMessage` 크기 초과 거부** — 세션 간 전달에 너무 큰 메시지를 조용히 버리지 않고 처음부터 거부한다.
- **Remote Control 게이트웨이 체크** — `claude rc`가 대화형 시작과 동일한 엔터프라이즈 게이트웨이 가용성 검사를 적용한다.
- **[VSCode] 탭 포커스 튐** — Claude 패널이 여러 개인 창을 복원·리로드할 때 포커스가 저절로 다른 Claude 탭으로 옮겨가던 문제를 고쳤다.

## 🔑 이번 버전의 핵심 키워드
**"권한 창과 입력창의 신뢰 회복"** — 승인 범위·표시 문구·커서 위치가 실제 동작과 어긋나던 UI 정합성 결함을 대거 정리한 안정화 릴리스다.

---

# 📜 원문 (한글 번역본)

> 원문 ChangeLog를 원래 순서 그대로 한 줄도 빠짐없이 번역한 문서입니다.

- 프롬프트 입력창에 입력하는 동안 틀린 단어에 밑줄을 표시하는 선택적 `spellcheck` 설정을 추가했다. 설치된 `aspell`·`hunspell`·`ispell`을 사용한다.
- 세션 중 language server가 끊기거나 다시 연결될 때 전체 프롬프트 캐시가 무효화되던 문제를 고쳤다.
- 중첩 마크다운 리스트 항목이 깊이 3 이상에서 어긋나던 문제를 고쳤고, 터미널 UI에서 줄바꿈된 리스트 항목에 hanging indent를 추가했다.
- 일부 여러 줄 프롬프트에서 프롬프트 입력 하이라이트(슬래시 명령·키워드·멘션)가 한 글자 이상 밀려 보이던 문제를 고쳤다.
- 권한 프롬프트의 코멘트 입력란에서 Shift+Tab을 누르면 입력란이 닫히는 대신 편집이 승인되고 세션 전체 편집 권한이 부여되던 문제를 고쳤다.
- general-purpose 에이전트를 쓸 수 없는 세션에서 Agent 도구가 general-purpose 기본값을 광고하던 문제를 고쳤다. 그 환경에서 `subagent_type`을 생략하면 이제 사용 가능한 에이전트를 나열한 명확한 에러가 반환된다.
- 노트북이나 셀을 읽을 수 없을 때 노트북 셀 삭제/교체 승인 다이얼로그가 기존 셀 내용을 조용히 생략하던 문제를 고쳤다. 이제 다이얼로그가 이유를 표시한다.
- Claude가 응답하는 중 실행된 슬래시 명령이 실제 문자 대신 HTML 엔티티로 표시되던 문제를 고쳤다.
- 백그라운드 자동 업데이트 후 프롬프트 하단에 "Update installed" 재시작 안내가 표시되지 않던 문제를 고쳤다.
- 열린 작업이 남아 있는 세션을 재개하거나 재실행할 때 확장 작업 목록(`ctrl+t`)이 항상 접힌 상태로 시작하던 문제를 고쳤다.
- `/ultrareview`·`/autofix-pr` 같은 클라우드 세션이 백그라운드로 실행될 때의 메모리·CPU 사용을 개선했다 — 갱신마다 이벤트 스트림을 다시 스캔·렌더하지 않는다.
- 권한 다이얼로그를 개선했다: 표시 문구와 "don't ask again" 옵션이 이제 부여 범위와 항상 일치하며, 내용을 완전히 표시할 수 없을 때는 "don't ask again"을 제공하지 않는다.
- 네이티브 macOS/Linux 빌드의 내장 `grep`을 개선했다: 병적인 패턴이 메모리를 소진하는 대신 빠르게 실패하고, `-m N`을 `-A/-C`와 함께 쓸 때 올바른 컨텍스트를 출력한다.
- auto-compact가 꺼져 있을 때 그 사실을 알리고 `/config`에서 다시 켜도록 안내하게 컨텍스트 한계 에러를 개선했다.
- Vim 모드: 상세 트랜스크립트(ctrl+o)를 토글하거나 패널을 닫을 때 NORMAL 모드와 커서 위치가 유지된다.
- 다이얼로그: 방향키와 Enter를 빠르게 연속 입력하면 이전에 하이라이트되어 있던 항목이 아니라 이동한 항목이 선택된다.
- `SendMessage`가 세션 간 전달에 너무 큰 메시지를 조용히 버리지 않고 처음부터 거부한다.
- Remote Control: `claude rc`가 대화형 시작과 동일한 엔터프라이즈 게이트웨이 가용성 검사를 적용한다.
- [VSCode] Claude 패널이 여러 개인 창을 복원하거나 리로드할 때 열린 Claude 탭 사이로 포커스가 저절로 튀던 문제를 고쳤다.

---

## 🎯 챌린지

이번 버전에서 내 환경에 적용해볼 만한 항목입니다.

### 1. `spellcheck` 켜기 (한국어 문서 작업 대비)
- **파일**: `~/.claude/settings.json`
- **근거**: 이번 버전 신기능. changelog·보고서·changelog 로그를 한국어 평서체로 자주 쓰는 환경이라 영문 식별자·커밋 메시지 오타를 입력 단계에서 잡을 수 있다. `brew install aspell` 로 백엔드를 먼저 깔고 `"spellcheck": true` 를 추가한다.
- **난이도**: ★☆☆ (약 7분)

### 2. auto-compact 상태 확인 후 명시적으로 고정
- **파일**: `~/.claude/settings.json`
- **근거**: 컨텍스트 한계 에러가 이제 auto-compact 꺼짐 여부를 알려준다. `opus[1m]` 로 긴 세션을 도는 환경이라 이 설정이 현재 어느 상태인지 `/config` 에서 확인하고 의도한 값으로 고정해 두면 장시간 작업 중 갑작스러운 컨텍스트 중단을 예측 가능하게 만든다.
- **난이도**: ★☆☆ (약 5분)

### 3. 권한 프롬프트 오승인 사고 점검
- **파일**: `~/.claude/settings.json`, `~/.claude/settings.local.json`
- **근거**: 권한 창 코멘트 입력란의 Shift+Tab 이 세션 전체 편집 권한을 부여하던 버그가 이번에 고쳐졌다. 그 버그로 의도치 않게 들어간 광범위 `allow` 항목이 남아 있을 수 있으니 두 파일의 `permissions.allow` 를 훑어 기억에 없는 항목을 지운다. `deploy-guard.sh` hook 이 막는 push 경로와 겹치는 항목이 있는지도 함께 본다.
- **난이도**: ★★☆ (약 15분)

### 4. `subagent_type` 명시 여부 점검
- **파일**: `~/.claude/CLAUDE.md`
- **근거**: general-purpose 가 없는 세션에서 `subagent_type` 생략이 이제 에러가 된다. CLAUDE.md §5 는 subagent 모델(Opus)만 규정하고 타입은 규정하지 않으니, subagent 호출 시 타입을 항상 명시한다는 한 줄을 §5 에 추가해 두면 병렬 dispatch 가 중간에 죽는 일을 막는다.
- **난이도**: ★☆☆ (약 8분)
