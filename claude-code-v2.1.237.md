# Claude Code v2.1.237

> 작성일: 2026-08-22

---

# 📋 요약본

## 🎉 신기능 (1건)
- **내장 "Concise" 출력 스타일** — 결과를 먼저 말하고 서론·해설을 생략하는 출력 스타일이 기본 제공된다. 작업의 철저함은 그대로 유지된다.
  - `/config` 의 Output style 항목에서 선택한다.
  - 별도 파일을 만들 필요 없이 바로 쓸 수 있다.

## 🛠️ 개선/수정 (1건)
- **LLM 게이트웨이·커스텀 base URL 세션의 prompt caching 수정** — LLM 게이트웨이나 커스텀 base URL(`ANTHROPIC_BASE_URL`)을 쓰는 세션에서 프롬프트 캐시가 동작하지 않던 문제를 고쳤다. 같은 환경에서 반복 요청 비용과 지연이 줄어든다.

## 🔑 이번 버전의 핵심 키워드
**"짧게 말하는 기본 스타일, 게이트웨이 환경 캐시 정상화"** — 출력 스타일 하나가 내장으로 들어오고, 프록시 경유 세션의 캐시 누락이 잡혔다.

---

# 📜 원문 (한글 번역본)

> 원문 ChangeLog를 원래 순서 그대로 한 줄도 빠짐없이 번역한 문서입니다.

- LLM 게이트웨이 또는 커스텀 base URL을 사용하는 세션의 prompt caching 문제를 수정했다.
- 내장 "Concise" 출력 스타일을 추가했다: Claude가 결과를 먼저 제시하고 서론과 진행 설명을 생략하지만, 작업 자체는 동일하게 철저히 수행한다. `/config` 의 Output style 에서 선택한다.

---

## 🎯 챌린지

이번 버전에서 내 환경에 적용해볼 만한 항목입니다.

### 1. Concise 출력 스타일을 기본값으로 고정
- **파일**: `~/.claude/settings.json`
- **근거**: 이번 버전에 내장된 `Concise` 스타일은 CLAUDE.md §8 "짧고 단호. 불필요한 형용사 제거"와 방향이 같다. `outputStyle` 키를 `"Concise"` 로 넣어두면 세션마다 `/config` 로 고르지 않아도 된다. 적용 후 새 세션에서 응답 서론이 사라졌는지 확인한다.
- **난이도**: ★☆☆ (약 5분)

### 2. Concise 와 §9 설명 눈높이 충돌 여부 점검
- **파일**: `~/.claude/CLAUDE.md`
- **근거**: §9는 전문용어를 "한 번 풀어서 설명"하라고 요구하는데 `Concise` 는 해설을 줄인다. 두 지시가 부딪히므로 §9에 "Concise 스타일에서도 용어 풀이 1줄은 유지한다" 같은 예외 문장을 한 줄 추가해 규칙을 명시한다.
- **난이도**: ★☆☆ (약 10분)

### 3. 커스텀 base URL 사용 여부 확인 후 캐시 수정 수혜 판단
- **파일**: `~/.claude/settings.json`
- **근거**: prompt caching 수정은 `ANTHROPIC_BASE_URL` 등 게이트웨이 설정을 쓰는 세션에만 적용된다. 현재 `settings.json` 의 `env` 블록에 해당 키가 없으므로 이 수정은 무관하다 — 사내 프록시나 Bedrock 경유를 쓸 계획이 있으면 그때 이 버전 이상을 쓰도록 메모만 남긴다.
- **난이도**: ★☆☆ (약 5분)
