# Claude Code v2.1.199

> 작성일: 2026-07-05

---

# 📋 요약본

## 🎉 신기능 (3건)
- **스택 슬래시-스킬 다중 로드** — `/skill-a /skill-b do XYZ` 처럼 앞에 나열한 슬래시-스킬을 첫 개만이 아니라 최대 5개까지 모두 로드한다.
- **429 속도 제한 자동 재시도** — 사용 한도와 무관한 일시적 429 오류가 구독자에게는 턴 실패 대신 백오프와 함께 자동 재시도된다.
- **재시도 워치독 강화** — `CLAUDE_CODE_RETRY_WATCHDOG` 가 비-용량(non-capacity) 일시 오류의 기본 재시도를 300으로 올리고 `CLAUDE_CODE_MAX_RETRIES` 의 15 상한을 해제한다.

## 🛠️ 개선/수정 (21건)
- **SSL 인증서 오류 즉시 실패** — TLS 검사 프록시·누락된 `NODE_EXTRA_CA_CERTS`·만료 인증서 오류가 재시도를 소진하지 않고 수정 힌트와 함께 즉시 실패한다.
- **스트리밍 부분 응답 보존** — 부분 출력 후 스트림 도중 overloaded/서버 오류가 나도 부분 응답을 "미완료" 안내와 함께 유지한다.
- **서브에이전트 부분 작업 반환** — 속도 제한·서버 오류로 잘린 서브에이전트가 조용히 실패하지 않고 부분 작업을 부모에게 돌려준다.
- **서브에이전트 오류 정직 보고** — 서브에이전트가 API 오류(예: 사용 한도 도달)를 성공으로 위장하지 않고 부모에게 오류로 보고한다.
- **Linux 백그라운드 데몬 자폭 수정** — 손상된 워커 레코드로 약 50초마다 데몬과 실행 중인 모든 에이전트를 죽이던 문제 해결.
- **macOS SSH 콜드 스타트 수정** — SSH로 백그라운드 에이전트 시작 시 "Could not switch to audit session" 실패(2.1.196 회귀) 해결.
- **`claude stop` 존중** — stop이 백그라운드 재생성과 경합해 조용히 무효화되지 않는다.
- **백그라운드 진행 표시기 멈춤 수정** — 긴 명령 실행 중 진행 표시기가 수 분간 정지하던 문제 해결.
- **저메모리 세션 안내** — 메모리 부족 시 일반 오류 대신 낮은 메모리를 알리고 자원 확보를 제안한다.
- **원격 세션 깜빡임 수정** — 백그라운드 에이전트 완료 시 Working/Idle 사이 깜빡임 제거.
- **유휴 서브에이전트 요약 행** — 다른 에이전트가 작업 중일 때 유휴 에이전트가 사라지지 않고 펼침 가능한 요약 행으로 접힌다.
- **`/model`·`/fast` 대상 안내** — 서브에이전트를 보는 중 입력해도 리드 선택기가 조용히 열리지 않고 리드에 적용된다는 안내를 표시한다.
- **훅 stderr 노출** — `SessionStart`·`Setup`·`SubagentStart` 훅이 종료 코드 2일 때 stderr를 숨기지 않고 트랜스크립트에 표시한다.
- **`daemon` 서브커맨드 실행** — `claude --dangerously-skip-permissions daemon <subcommand>` 를 채팅 프롬프트로 취급하지 않고 서브커맨드로 실행한다.
- **`SendMessage` 오라우팅 방지** — 재생성 에이전트가 이전 에이전트 이름을 재사용하면 불일치를 감지해 대상 재지정을 요청한다.
- **트랜스크립트 비대화 방지** — 새 메시지 없는 세션을 열거나 재개해도 트랜스크립트 파일이 불필요하게 커지지 않는다.
- **`/color` 유지** — `←`·`/background` 로 백그라운드 전환 시 에이전트 뷰 행에서 `/color` 가 사라지지 않는다.
- **설정 파일 백업 후 리셋** — 손상된 설정을 시작 복구 대화상자에서 리셋할 때 파괴 전에 먼저 파일을 백업한다.
- **Chrome 재연결 반복 수정** — 서로 다른 빌드·설정 디렉터리 세션에서 재연결 페이지를 반복해 열던 문제 해결.
- **플랜 모드 브라우저 확인** — 상태를 바꾸는 브라우저 도구 호출은 확인을 요청하고, 읽기 전용 `browser_batch` 호출은 올바르게 자동 허용한다.
- **PR 링크 표시 간소화** — `claude agents` 세션 행이 풀 리퀘스트 링크를 중복된 "PR" 레이블 없이 `#N` 으로 표시한다.

## 🔑 이번 버전의 핵심 키워드
**"서브에이전트·백그라운드 에이전트가 조용히 실패하지 않는다"** — 부분 결과 보존과 재시도 강화로 자동화·병렬 실행의 신뢰성을 끌어올린 버전.

---

# 📜 원문 (한글 번역본)

> 원문 ChangeLog를 원래 순서 그대로 한 줄도 빠짐없이 번역한 문서입니다.

- `/skill-a /skill-b do XYZ` 처럼 스택으로 쌓은 슬래시-스킬 호출이 이제 첫 번째만이 아니라 앞쪽 스킬을 모두(최대 5개) 로드한다.
- TLS 검사 프록시, 누락된 `NODE_EXTRA_CA_CERTS`, 만료된 인증서로 인한 SSL 인증서 오류가 실질적 안내를 보여주기 전에 재시도를 소진하던 문제 수정 — 이제 수정 힌트와 함께 즉시 실패한다.
- API가 부분 출력 이후 스트림 도중 overloaded/서버 오류를 방출할 때 스트리밍 응답이 폐기되던 문제 수정 — 이제 부분 응답을 "미완료 응답" 안내와 함께 보존한다.
- 속도 제한(rate limit)이나 서버 오류로 잘린 서브에이전트가 부분 작업을 부모에게 반환하지 않고 조용히 실패하던 문제 수정.
- 서브에이전트가 API 오류(예: 사용 한도 도달)를 성공 결과로 보고하던 문제 수정 — 이제 오류를 부모 에이전트에 보고한다.
- Linux에서 백그라운드-에이전트 데몬이 비정상 종료로 손상된 워커 레코드가 남은 뒤 약 50초마다 자기 자신과 실행 중인 모든 에이전트를 죽이던 문제 수정.
- macOS에서 SSH를 통한 백그라운드 에이전트 콜드 스타트가 "Could not switch to audit session" 으로 실패하던 문제 수정 (2.1.196 회귀).
- `claude stop` 이 백그라운드-에이전트 재생성(respawn)과 경합해 조용히 무효화되던 문제 수정 — 이제 재생성이 stop을 존중한다.
- 백그라운드 작업이 오래 걸리는 명령을 실행하는 동안 진행 표시기가 수 분간 멈춰 있던 문제 수정.
- 메모리가 부족한 머신의 백그라운드 세션이 일반 오류를 표시하던 문제 수정 — 이제 낮은 메모리를 알리고 자원 확보를 제안한다.
- 백그라운드 에이전트가 완료될 때 에이전트 뷰에서 원격 세션이 Working과 Idle 사이를 잠깐 깜빡이던 문제 수정.
- 다른 서브에이전트가 여전히 작업 중일 때 유휴 서브에이전트가 에이전트 패널에서 사라지던 문제 수정 — 이제 잉여 유휴 에이전트는 펼칠 수 있는 요약 행으로 접힌다.
- 서브에이전트를 보는 중에 `/model` 이나 `/fast` 를 입력하면 조용히 리드(lead)의 모델 선택기가 열리던 문제 수정 — 이제 명령이 리드에 적용된다는 안내가 표시된다.
- `SessionStart`, `Setup`, `SubagentStart` 훅이 종료 코드 2로 끝날 때 stderr를 조용히 숨기던 문제 수정 — 이제 오류가 트랜스크립트에 표시된다.
- `claude --dangerously-skip-permissions daemon <subcommand>` 이 서브커맨드를 실행하지 않고 채팅 프롬프트로 취급되던 문제 수정.
- 재생성된 에이전트가 이전 에이전트의 이름을 재사용할 때 `SendMessage` 가 조용히 잘못 라우팅되던 문제 수정 — 이제 도구가 불일치를 감지하고 호출자에게 대상 재지정을 요청한다.
- 새 메시지가 없는 세션을 열거나 재개할 때 트랜스크립트 파일이 불필요하게 커지던 문제 수정.
- `←` 또는 `/background` 로 세션을 백그라운드로 보낼 때 에이전트 뷰 행에서 `/color` 가 사라지던 문제 수정.
- 시작 복구 대화상자에서 손상된 설정 파일을 리셋하면 복구 불가능하게 파괴되던 문제 수정 — 이제 먼저 파일을 백업한다.
- 서로 다른 빌드나 설정 디렉터리에서 세션을 실행할 때 Chrome의 Claude가 재연결 페이지를 반복해서 열던 문제 수정.
- 플랜 모드가 상태를 바꾸는 브라우저 도구 호출에 대해 확인을 요청하지 않던 문제 수정 — 읽기 전용 `browser_batch` 호출은 이제 올바르게 자동 허용된다.
- (사용 한도와 무관한) 일시적 서버 속도 제한 오류(429)가 이제 구독자에게는 턴을 실패시키지 않고 백오프와 함께 자동으로 재시도된다.
- `CLAUDE_CODE_RETRY_WATCHDOG` 가 이제 비-용량(non-capacity) 일시적 오류의 기본 재시도 횟수를 300으로 올리고 `CLAUDE_CODE_MAX_RETRIES` 의 15 상한을 해제한다.
- `claude agents` 세션 행이 이제 풀 리퀘스트 링크를 중복된 "PR" 레이블 없이 순수 `#N` 형태로 표시한다.

---

## 🎯 챌린지

이번 버전에서 내 환경에 적용해볼 만한 항목입니다.

### 1. 재시도 워치독 환경변수 켜기
- **파일**: `~/.claude/settings.json` (`env` 블록 추가)
- **근거**: 이번 버전의 `CLAUDE_CODE_RETRY_WATCHDOG`·`CLAUDE_CODE_MAX_RETRIES` 강화 항목. 당신은 CLAUDE.md §5에서 "신뢰성 우선"을 명시했고 긴 워크플로우와 cron(`claude -p --model opus`, `claude-changelog-sync`)을 돌린다. 이 변수를 켜면 사용 한도와 무관한 일시적 오류로 턴이 죽는 일이 크게 줄어든다.
- **난이도**: ★☆☆ (약 5분)

### 2. SessionStart 훅 오류를 표면화
- **파일**: `~/agent-infra/hooks/session-start-retro-alert.sh`
- **근거**: 이번 버전에서 `SessionStart`·`Setup`·`SubagentStart` 훅이 종료 코드 2일 때 stderr를 트랜스크립트에 보여주도록 고쳐졌다. 이 훅의 오류 경로가 지금 `exit 0` 으로 실패를 삼키거나 stdout으로만 로그하면 여전히 안 보인다. 실패 시 메시지를 `>&2` 로 쓰고 `exit 2` 하도록 바꾸면 회고 알림 로직이 깨졌을 때 바로 드러난다.
- **난이도**: ★★☆ (약 10~15분)

### 3. 손수 튜닝한 settings.json 백업본 떠두기
- **파일**: `~/.claude/settings.json` → `~/.claude/settings.json.bak`
- **근거**: 이번 버전은 시작 복구 대화상자가 손상된 설정 파일을 복구 불가능하게 파괴하던 문제를 "리셋 전 백업"으로 고쳤다. 당신의 settings.json은 훅 다수·statusLine·플러그인이 얽힌 손수 튜닝 파일이라 손상 시 복구 비용이 크다. 수동 백업 사본을 하나 떠두면 최악의 경우에도 원복할 수 있다.
- **난이도**: ★☆☆ (약 2분)
