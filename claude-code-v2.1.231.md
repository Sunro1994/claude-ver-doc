# Claude Code v2.1.231

> 작성일: 2026-08-14

---

# 📋 요약본

## 🛠️ 개선/수정 (1건)
- **MCP OAuth 로그인 실패 수정** — 사전 등록된 OAuth 클라이언트를 쓰는 서버(예: Slack)에서 리디렉션 URI 불일치로 로그인이 실패하던 문제를 고쳤다. 서버가 미리 등록해 둔 리디렉션 주소와 Claude Code가 보내던 주소가 달라 인증이 거부됐다. 이제 Slack 계열 MCP 서버 연결이 정상 동작한다.

## 🔑 이번 버전의 핵심 키워드
**"Slack 같은 사전 등록 OAuth MCP 서버 로그인 정상화"** — 기능 추가 없이 MCP 인증 회귀 하나만 잡은 패치 버전.

---

# 📜 원문 (한글 번역본)

> 원문 ChangeLog를 원래 순서 그대로 한 줄도 빠짐없이 번역한 문서입니다.

- Slack처럼 사전 등록된 OAuth 클라이언트를 사용하는 서버에서 리디렉션 URI 불일치(redirect URI mismatch)로 MCP OAuth 로그인이 실패하던 문제를 수정했다.

---

## 🎯 챌린지

이번 버전에서 내 환경에 적용해볼 만한 항목입니다.

### 1. MCP 서버 인증 상태 점검
- **파일**: `~/.claude.json` (MCP 서버 설정), 확인 명령 `claude mcp list`
- **근거**: 이번 버전이 고친 건 MCP OAuth 로그인이다. 현재 `google-sheets` MCP 서버가 연결 대기 상태로 잡혀 있으니, 인증이 살아 있는지 한 번 확인해 둔다. 끊겨 있으면 `/mcp` 로 재인증한다.
- **난이도**: ★☆☆ (약 5분)

### 2. Claude Code 버전 올리기
- **파일**: 터미널에서 `claude update` 실행 후 `claude --version` 으로 확인
- **근거**: 이 수정은 v2.1.231 에 들어 있다. 구버전에 머물면 사전 등록 OAuth 를 쓰는 MCP 서버는 계속 로그인에 실패한다. 버전만 올리면 끝나는 작업이다.
- **난이도**: ★☆☆ (약 5분)

### 3. MCP 인증 실패 시 재시도 절차 메모 남기기
- **파일**: `~/.claude/projects/-/memory/reference-mcp-oauth-recovery.md` + `MEMORY.md` 한 줄 추가
- **근거**: OAuth 오류는 원인이 눈에 안 보여 매번 헤맨다. "`claude mcp list` 로 상태 확인 → `/mcp` 로 재인증 → 그래도 실패하면 버전 확인" 순서를 메모로 남기면 다음에 같은 증상에서 시간을 아낀다.
- **난이도**: ★☆☆ (약 10분)
