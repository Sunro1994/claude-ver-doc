# Claude Code v2.1.205

> 작성일: 2026-07-09

---

# 📋 요약본

## 🎉 신기능 (2건)
- **세션 트랜스크립트 변조 차단 규칙** — auto 모드에 세션 기록(transcript) 파일을 건드리지 못하게 막는 규칙이 추가됐다. 에이전트가 자기 대화 기록을 조작하는 것을 원천 봉쇄한다.
- **`/doctor` 전체 셋업 점검** — `/doctor`가 문제를 진단하고 직접 고치는 전체 셋업 점검 도구로 바뀌었다. `/checkup`이 그 별칭(alias)이다.

## 🛠️ 개선/수정 (21건)
- **`--json-schema` 검증** — 스키마가 잘못됐을 때 조용히 비구조화 출력이 나오던 문제와, `format` 키워드를 쓴 스키마가 거부되던 문제를 고쳤다.
- **`--max-turns` 한계 메시지 유실** — Claude가 작업 중일 때 보낸 메시지가 턴이 `--max-turns` 한계에서 끝나면 조용히 사라지던 문제를 고쳤다.
- **Windows worktree 삭제 사고** — worktree 안에 NTFS junction 또는 디렉터리 심링크가 있으면 worktree 밖 파일까지 지우던 문제를 고쳤다.
- **백그라운드 에이전트 상태 고착** — `SendMessage`로 재개한 뒤에도 에이전트 목록에서 "failed"/"completed"로 계속 표시되던 문제를 고쳤다.
- **백그라운드 잡 상태 되돌림** — 에이전트 턴에 읽을 수 있는 텍스트가 없을 때 목록에서 "needs input"이 다시 "working"으로 뒤집히던 문제를 고쳤다.
- **`claude attach` 오류** — 백그라운드 에이전트가 업그레이드 재시작 중일 때 `claude attach`가 기다리지 않고 에러를 내던 문제를 고쳤다.
- **세션↔PR 연결 누락** — 출력이 30K 인라인 한계를 넘긴 Bash 호출에서 생성된 PR이 세션-PR 연결에서 빠지던 문제를 고쳤다.
- **`claude mcp add-from-claude-desktop` 멈춤** — 서버 이름에 지원되지 않는 문자가 있으면 멈추던 문제를 고쳤다. 이제 잘못된 이름은 보고되고 나머지 서버는 정상 임포트된다.
- **플러그인 LSP 초기화 실패 전파** — 초기화에 실패한 플러그인 LSP 서버가 같은 확장자를 처리하는 다른 플러그인의 정상 LSP 서버까지 막던 문제를 고쳤다.
- **Windows 실행 디렉터리 삭제 크래시** — Claude를 실행한 디렉터리가 명령 실행 중 삭제·잠김·언마운트될 때 나던 Windows 크래시를 고쳤다.
- **파일 워처 크래시** — 디렉터리 스캔이 진행 중일 때 파일 워처가 닫히면 나던 크래시를 고쳤다.
- **프로젝트 verify 스킬 재작성** — 문서화된 명령이 바뀐 경우에만 다시 써야 하는데 매 세션마다 프로젝트 verify 스킬이 재작성되던 문제를 고쳤다.
- **에이전트 뷰 렌더링 오차** — 잡 목록이 화면을 살짝 넘칠 때 에이전트 뷰가 한 줄 위로 렌더링돼 헤더가 잘리던 문제를 고쳤다.
- **원격 제어 패널 상태 지연** — 웹·모바일 Remote Control 패널의 백그라운드 태스크가 낡은 "Running" 상태로 남던 문제를, 멤버십 변경마다 전체 태스크 상태를 전달하도록 고쳤다.
- **auto 모드 `rm -rf` 확인** — 문맥에서 값을 해석할 수 없는 변수에 `rm -rf`를 실행하기 전에 auto 모드가 먼저 묻도록 개선했다.
- **자동 업데이트 메모리 절감** — 자동 업데이트 바이너리 다운로드가 메모리에 버퍼링하지 않고 디스크로 스트리밍하도록 바뀌어, 업데이터 최대 메모리 사용량을 약 400 MB 줄였다.
- **백그라운드 알림 명시** — 백그라운드 태스크 알림이 "사람의 입력이 없었음"을 명시하게 돼, 트랜스크립트에 조작된 승인이 실제 승인으로 취급되는 것을 막는다.
- **에이전트 뷰 PR 연결 확대** — 기존 PR을 편집·병합·코멘트·push한 세션도 이제 `claude agents`에서 그 PR을 연결한다.
- **에이전트 뷰 표시 개선** — 각 행이 원시 도구 호출 텍스트 대신 색상이 입혀진 상태 단어와 분류기가 작성한 헤드라인을 보여주고, peek을 열면 차단된 세션의 정확한 요청을 포함한 전체 상태가 표시된다.
- **예약 MCP 서버 이름** — Claude Desktop 창 이름 변경에 앞서 "Claude Browser" 이름을 ("Claude Preview"와 함께) 예약했다. 사용자가 설정한 MCP 서버는 이제 두 이름 중 어느 것으로도 등록할 수 없다.
- **Cowork VM 모드 로그인 오류** — CLI 2.1.203+ 에서 Cowork VM 모드 로컬 에이전트 세션이 "Not logged in · Please run /login"으로 시작 실패하던 문제를 고쳤다.

## 🔑 이번 버전의 핵심 키워드
**"백그라운드 에이전트 신뢰성과 트랜스크립트 무결성"** — 에이전트 목록 상태 정확도, PR 연결, 트랜스크립트·승인 조작 차단에 집중한 안정화 버전.

---

# 📜 원문 (한글 번역본)

> 원문 ChangeLog를 원래 순서 그대로 한 줄도 빠짐없이 번역한 문서입니다.

- auto 모드에 세션 트랜스크립트 파일 변조를 차단하는 규칙을 추가했다.
- `--json-schema`가 스키마가 잘못됐을 때 조용히 비구조화 출력을 내던 문제와, `format` 키워드를 사용한 스키마가 거부되던 문제를 고쳤다.
- Claude가 작업 중일 때 보낸 메시지가 턴이 `--max-turns` 한계에서 끝나면 조용히 유실되던 문제를 고쳤다.
- Windows worktree 삭제 시, worktree 내부에 NTFS junction 또는 디렉터리 심링크가 있으면 worktree 밖 파일까지 지우던 문제를 고쳤다.
- 백그라운드 에이전트가 `SendMessage`로 재개된 후에도 에이전트 목록에서 "failed" 또는 "completed"로 계속 표시되던 문제를 고쳤다.
- 에이전트의 턴에 읽을 수 있는 텍스트가 없을 때, 에이전트 목록에서 백그라운드 잡이 "needs input"에서 다시 "working"으로 뒤집히던 문제를 고쳤다.
- 백그라운드 에이전트가 업그레이드 재시작 중일 때 `claude attach`가 복귀를 기다리지 않고 에러를 내던 문제를 고쳤다.
- 출력이 30K 인라인 한계를 초과한 Bash 호출에서 생성된 PR이 세션-PR 연결에서 누락되던 문제를 고쳤다.
- 서버 이름에 지원되지 않는 문자가 포함됐을 때 `claude mcp add-from-claude-desktop`이 멈추던 문제를 고쳤다. 이제 잘못된 이름은 보고되고 나머지 서버는 계속 임포트된다.
- 초기화에 실패한 플러그인 LSP 서버가, 같은 파일 확장자를 처리하는 다른 플러그인의 정상 LSP 서버를 막던 문제를 고쳤다.
- Claude가 실행된 디렉터리가 명령 실행 중 삭제·잠김·언마운트될 때 발생하던 Windows 크래시를 고쳤다.
- 디렉터리 스캔이 진행 중일 때 파일 워처가 닫히면 발생하던 크래시를 고쳤다.
- 문서화된 명령이 변경됐을 때만 다시 써야 하는데도 매 세션마다 프로젝트 verify 스킬이 재작성되던 문제를 고쳤다.
- 잡 목록이 화면을 살짝 넘칠 때 에이전트 뷰가 한 줄 위로 렌더링돼 헤더가 잘리던 문제를 고쳤다.
- 웹·모바일 Remote Control 패널의 백그라운드 태스크가 낡은 "Running" 상태로 표시되던 문제를, 멤버십 변경마다 전체 태스크 상태를 전달하도록 고쳤다.
- auto 모드를 개선해, 문맥에서 값을 해석할 수 없는 변수에 `rm -rf`를 실행하기 전에 먼저 묻도록 했다.
- 자동 업데이트 바이너리 다운로드가 이제 메모리에 버퍼링하지 않고 디스크로 스트리밍해, 업데이터의 최대 메모리 사용량을 약 400 MB 줄였다.
- 백그라운드 태스크 알림이 이제 사람의 입력이 없었음을 명시적으로 밝혀, 트랜스크립트에 조작된 승인이 실제 승인으로 처리되는 것을 방지한다.
- 에이전트 뷰 개선: 기존 PR을 편집·병합·코멘트·push한 세션도 이제 `claude agents`에서 해당 PR을 연결한다.
- 에이전트 뷰 개선: 각 행이 원시 도구 호출 텍스트 대신 색상이 입혀진 상태 단어와 분류기가 작성한 헤드라인을 표시하고, peek을 열면 차단된 세션의 정확한 요청을 포함한 전체 상태가 함께 열린다.
- `/doctor`가 이제 문제를 진단하고 고칠 수 있는 전체 셋업 점검 기능이 됐다. `/checkup`이 그 별칭이다.
- Claude Desktop 창(pane) 이름 변경에 앞서 "Claude Browser" MCP 서버 이름을 ("Claude Preview"와 함께) 예약했다. 사용자가 설정한 MCP 서버는 이제 두 이름 중 어느 것으로도 등록할 수 없다.
- CLI 2.1.203+ 에서 Cowork VM 모드 로컬 에이전트 세션이 "Not logged in · Please run /login"으로 시작에 실패하던 문제를 고쳤다.

---

## 🎯 챌린지

이번 버전에서 내 환경에 적용해볼 만한 항목입니다.

### 1. `/doctor` 전체 셋업 점검 실행
- **파일**: `~/.claude/settings.json` (점검 대상)
- **근거**: 이번 버전에서 `/doctor`(`/checkup`)가 진단만 하는 게 아니라 문제를 직접 고치는 전체 점검 도구가 됐다. 현재 `settings.json`에는 `UserPromptSubmit`·`PreToolUse`·`PostToolUse`·`SessionStart/End`·`SubagentStop` 등 `$HOME/agent-infra/hooks/*.sh`를 가리키는 커스텀 hook이 10개 가까이 걸려 있어, hook 경로 유효성·설정 정합성을 한 번에 검진하기 좋다.
- **난이도**: ★☆☆ (약 5분)

### 2. 예약 MCP 서버 이름 충돌 사전 점검
- **파일**: `~/.claude/settings.json`, 프로젝트별 `.mcp.json`
- **근거**: 이번 버전이 "Claude Browser"·"Claude Preview"를 예약 이름으로 막았다. 현재 `playwright` MCP를 쓰고 있으니 설정 파일에서 이 두 이름을 쓰는 서버가 없는지 grep으로 확인해, 향후 Desktop 창 이름 변경 시 등록 실패를 미리 배제한다.
- **난이도**: ★☆☆ (약 5분)

### 3. `claude -p` 자동화에 `--json-schema` 신뢰 도입
- **파일**: `claude-changelog-sync` / `claude-ver-dashboard` 스킬 스크립트
- **근거**: 이번 버전이 잘못된 스키마를 조용히 통과시키던 버그와 `format` 키워드 거부를 고쳤다. 이 스킬들은 `claude -p --model opus`로 생성물을 만드는데, 다운스트림에서 파싱하는 구조화 출력이 있다면 이제 스키마를 강제해도 실패가 조용히 묻히지 않으니 안전하게 도입할 수 있다.
- **난이도**: ★★☆ (약 15분)
