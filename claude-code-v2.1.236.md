# Claude Code v2.1.236

> 작성일: 2026-08-20

---

# 📋 요약본

## 🎉 신기능 (3건)
- **`ANTHROPIC_DEFAULT_MODEL` 환경변수** — 새 세션이 시작할 모델을 지정한다. `/model` 로 고른 값이 이 설정을 덮어쓰고 재시작 후에도 유지된다. 기존 `ANTHROPIC_MODEL` 과 다른 점 — `ANTHROPIC_MODEL` 은 항상 강제되지만 이건 "출발점"만 정한다.
- **`SendMessage` 의 `notify_when_idle`** — 같은 머신의 다른 Claude Code 세션에게 "다음에 한가해지면 한 번 알려달라"고 부탁하는 옵션. 직접 켜야 동작하고, 알림은 1회만 오고, 반복 확인(polling) 없이 동작한다. macOS·Linux 지원.
- **[VSCode] 대화 기록 스크린리더 지원** — 답변·권한 요청·오류·상태 변화를 소리로 알려주고, 턴마다 제목(heading)으로 이동할 수 있다.

## 🛠️ 개선/수정 (28건)
- **샌드박스 읽기 차단 규칙 강화 (macOS)** — `**/.env` 같은 와일드카드 읽기-차단 규칙이 읽기 허용 영역 안에서도 우선 적용된다. 매칭된 디렉터리 내용까지 덮고, 차단된 파일 이름을 바꿔서 우회할 수 없다.
- **세션 작업 디렉터리 삭제 후 기능 깨짐 수정** — 클립보드 복사, 백그라운드 정리 작업, 백그라운드 세션, 로컬 MCP 로그가 세션이 이동해 들어간 디렉터리가 삭제되면 망가지던 문제(2.1.229 이후).
- **전체화면 렌더러 영구 실패 수정** — 한 번 시작에 실패하면 이후 실행마다 종료되던 문제. 이제 기존(classic) 렌더러로 대체 동작한다.
- **`/model` 선택창 높이 수정** — 터미널보다 길게 그려지던 문제. 이제 창에 들어가는 만큼만 보여주고 나머지는 스크롤로 본다.
- **`SendMessage` 태그 오류 처리** — 닫는 태그가 깨져서 본문이 요약(summary) 칸에 들어가면 호출이 거부되던 문제 수정.
- **하위 프로세스 시작 실패 시 예외 수정** — WSL에서 Windows 연동이 꺼진 채 `powershell.exe` 를 실행할 때 등(2.1.234 회귀).
- **전체화면 모드 새 메시지 표시 지연 수정** — 터미널 크기를 바꾼 뒤 새로 보낸 메시지가 다음 갱신까지 안 보이던 문제.
- **전체화면 모드 잔상·재그리기 수정** — 여러 줄 입력을 지운 뒤 프롬프트 위에 빈 띠가 남던 문제, 터미널 크기를 바꿨다 되돌렸을 때 창이 다시 안 그려지던 문제.
- **관리 설정(managed-settings) 승인창 수정** — 시작할 때 창이 안 뜨는데도 첫 키 입력을 승인으로 먹던 문제.
- **tmux 탭 제목 튐 수정 (iTerm tmux 연동)** — 960ms마다 계속 다시 쓰던 것을 글자가 바뀔 때만 쓰도록 변경.
- **클라우드 환경 목록 오류 메시지 개선** — 목록이 비었거나 형식이 깨졌을 때의 불명확한 오류 수정.
- **Fable 5 첫 사용 크레딧 안내 수정** — Remote Control 사용 시 60초간 응답이 없으면 대체 모델이 자동 선택되던 문제.
- **스피너 팁 미표시 수정** — `~/.claude.json` 의 guest-pass 보상 캐시가 깨져 있으면 팁이 안 나오고 백그라운드 오류가 반복되던 문제.
- **스킬 즉시 재적용(hot-reload) 오류 수정** — SDK·VS Code 세션에서 작업 디렉터리가 삭제된 뒤 스킬이 바뀔 때마다 오류가 나던 문제(2.1.229+).
- **자체 호스팅 러너 세션 수정** — 유휴·종료·시작 타임아웃으로 반납된 세션이 세션 종료 후 훅이 끝나기 전에 다른 러너에서 재개되던 문제.
- **Clawd 마스코트 렌더링 수정** — iTerm2 특정 글꼴 크기에서 눈·발이 어긋나 보이던 문제.
- **세션 요약(recap) 폭주 수정** — 자동 요약과 `/recap` 모두 400자로 제한하고 단어 경계에서 자른다.
- **시작 성능 개선** — 세션 카운터를 백그라운드에서 기록한다.
- **auto 모드 개선 — `Monitor` 허용 규칙** — auto 모드가 켜져 있는 동안 `Monitor` 허용 규칙을 잠시 제외해, Monitor 명령도 Bash 명령과 같은 방식으로 검토된다.
- **auto 모드 개선 — Bedrock·Vertex AI·Foundry·텔레메트리 비활성 환경** — 분류기가 Claude API와 같은 기본값(심각도 점수 분류 포함)을 쓴다.
- **auto 모드 개선 — git 상태 확인** — 레포의 `status.showUntrackedFiles=no` 설정 때문에 작업 트리를 깨끗하다고 잘못 보고하던 문제 차단.
- **`/model` 선택창 강조 방식 변경** — 가장 최신 모델의 이름만 강조해, 강조가 임의 항목 묶음이 아니라 신규 릴리스를 표시하게 했다.
- **`/goal` 자동 체크인** — 오래 걸리는 백그라운드 작업 뒤에 목표가 밀려 있는 유휴 세션이 30분(이후 1시간, 2시간) 뒤 자동으로 확인한다.
- **`/usage` 개선** — Team·Enterprise 멤버에게 사용 크레딧 지출 행을 표시하고, 아직 지출이 없으면 0% 상태의 상한 행을 보여준다.
- **print/SDK 모드 SIGTERM 처리** — 종료 전에 중단된 턴이나 가짜 도구 거부 기록을 남기지 않는다. 실행 중인 명령은 그대로 종료되고 종료 코드는 143 유지.
- **슬래시 명령 오타 처리** — 오타나 이 세션에서 못 쓰는 명령에 Enter를 치면 가장 비슷한 명령을 실행하지 않고 그 사실을 알린다. 접두사·별칭은 그대로 실행.
- **Remote Control 오프라인 표시** — CLI가 종료되거나 터미널이 닫히면 몇 초 안에 세션을 오프라인으로 표시한다.
- **`SendMessage` 폭주 차단** — 짧은 시간에 몰아 보낸 메시지가 대상 세션의 받은함 한도를 넘으면 앞단에서 거부한다. 전에는 실제로 버려졌는데 "보냈다"고 보고했다.
- **프롬프트 테두리 세션 제목 칩 정렬** — 하단 바(footer) 오른쪽 끝에 맞춤.
- **오른쪽 정렬 항목 여백 통일** — 목표 표시, 세션 상태, 백그라운드 에이전트 상태, 잘린 알림이 프롬프트 영역과 같은 오른쪽 여백을 쓴다.

## 🔑 이번 버전의 핵심 키워드
**"세션 간 협업과 화면 안정화"** — 세션끼리 알림을 주고받고(`notify_when_idle`), 전체화면·`/model`·tmux 렌더링 잔상을 대거 정리한 버전.

---

# 📜 원문 (한글 번역본)

> 원문 ChangeLog를 원래 순서 그대로 한 줄도 빠짐없이 번역한 문서입니다.

- `ANTHROPIC_DEFAULT_MODEL` 환경변수 추가 — 새 세션이 시작할 모델을 정한다. `/model` 로 고른 값은 여전히 이를 덮어쓰고 재시작 후에도 유지된다(`ANTHROPIC_MODEL` 과 다른 점).
- 세션 간 `SendMessage` 에 `notify_when_idle` 추가 — 이 머신의 다른 Claude Code 세션에게 다음에 유휴 상태가 될 때 한 번 알려달라고 요청한다. 직접 켜야 하고(opt-in), 1회성이며, 폴링이 없다(macOS·Linux).
- 샌드박스 — macOS에서 와일드카드 읽기 차단 규칙(예: `**/.env`)이 읽기 허용 영역 안에서도 우선 적용되고, 매칭된 디렉터리의 내용까지 포함하며, 차단된 파일 이름을 바꿔서 우회할 수 없다.
- 세션이 이동해 들어간 디렉터리가 삭제된 뒤 클립보드 복사, 백그라운드 정리 작업, 백그라운드 세션, 로컬 MCP 로그가 깨지던 문제 수정(2.1.229부터).
- 전체화면 렌더러가 한 번 시작 실패하면 이후 실행마다 종료되며 영구히 실패하던 문제 수정 — 이제 기존(classic) 렌더러로 대체 동작한다.
- `/model` 선택창이 터미널보다 높게 렌더링되던 문제 수정 — 이제 창에 맞는 개수만 표시하고 나머지는 스크롤로 접근한다.
- 닫는 태그가 잘못되어 메시지 본문이 요약(summary) 필드에 들어간 경우 `SendMessage` 호출이 거부되던 문제 수정.
- 하위 프로세스 시작 실패 시 처리되지 않은 promise rejection 이 발생하던 문제 수정 — 예: Windows 연동이 비활성화된 WSL에서의 `powershell.exe`(2.1.234 회귀).
- 터미널 크기 변경 후 전체화면 모드에서 새로 보낸 메시지가 다음 갱신까지 표시되지 않던 문제 수정.
- 전체화면 모드에서 여러 줄 프롬프트를 지운 뒤 프롬프트 위에 빈 띠가 남던 문제, 터미널 크기를 바꿨다 되돌렸을 때 창이 다시 그려지지 않던 문제 수정.
- 관리 설정(managed-settings) 승인 프롬프트가 시작 시 표시되지 않으면서도 첫 키 입력을 승인으로 받던 문제 수정.
- tmux(iTerm tmux 연동)에서 터미널 탭 제목이 튀던 문제 수정 — 이제 960ms마다 애니메이션하는 대신 텍스트가 변할 때만 제목을 쓴다.
- 클라우드 환경 목록이 비어 있거나 형식이 잘못된 상태로 돌아올 때의 불명확한 오류 수정.
- Remote Control 사용 시 Fable 5 최초 사용 크레딧 프롬프트가 60초간 응답이 없으면 대체 모델을 자동 선택하던 문제 수정.
- `~/.claude.json` 에 캐시된 guest-pass 보상이 잘못된 형식일 때 스피너 팁이 전혀 나타나지 않고 백그라운드 오류가 반복되던 문제 수정.
- 세션 작업 디렉터리가 삭제된 뒤 SDK·VS Code 세션에서 스킬이 변경될 때마다 스킬 hot-reload 가 오류를 내던 문제 수정(2.1.229+).
- 유휴·회수·시작 타임아웃으로 반납된 자체 호스팅 러너 세션이 세션 종료 후 훅이 끝나기 전에 다른 러너에서 재개되는 일이 있던 문제 수정.
- iTerm2 일부 글꼴 크기에서 Clawd 마스코트의 눈과 발이 고르지 않게 렌더링되던 문제 수정.
- 간헐적으로 폭주하던 세션 요약 수정 — 요약 텍스트(자동 및 `/recap`)를 400자로 제한하고 단어 경계에서 자른다.
- 시작 성능 개선 — 세션 카운터를 백그라운드에서 기록한다.
- auto 모드 개선 — auto 모드가 활성인 동안 `Monitor` 허용 규칙을 제외해, Monitor 명령이 Bash 명령과 동일하게 검토된다.
- Bedrock·Vertex AI·Foundry 및 텔레메트리 비활성 시 auto 모드 개선 — 분류기가 Claude API와 동일한 기본값(심각도 점수 분류 포함)을 사용한다.
- auto 모드 개선 — git 상태 확인이 레포의 `status.showUntrackedFiles=no` 설정에 속아 작업 트리를 깨끗하다고 보고할 수 없게 되었다.
- `/model` 선택창이 가장 최신 모델의 이름만 강조하도록 변경 — 강조가 목록의 임의 부분집합이 아니라 신규 릴리스를 표시하게 한다.
- `/goal` — 오래 걸리는 백그라운드 작업 뒤에 목표가 밀려 있는 유휴 세션이 사용자가 돌아오기를 기다리지 않고 30분 뒤(이후 1시간, 2시간) 자동으로 확인한다.
- `/usage` 가 Team·Enterprise 멤버에게 사용 크레딧 지출 행을 표시하고, 아무것도 지출되지 않은 상태에서는 0%인 상한 행을 표시한다.
- print/SDK 모드의 SIGTERM 이 종료 전에 중단된 턴이나 합성된 도구 거부를 기록하지 않는다 — 실행 중 명령은 여전히 종료되고 프로세스는 종료 코드 143으로 끝난다.
- 슬래시 명령 오타나 이 세션에서 사용할 수 없는 명령에 Enter를 누르면 가장 비슷한 퍼지 매치를 실행하는 대신 그 사실을 알린다 — 접두사와 별칭은 여전히 실행된다.
- Remote Control 이 CLI 종료 또는 터미널 종료 시 몇 초 안에 세션을 오프라인으로 표시한다.
- `SendMessage` 가 짧은 시간에 몰린 메시지가 대상 세션 받은함의 허용량을 넘길 경우, 보냈다고 보고하고 실제로는 버리는 대신 앞단에서 추가 메시지를 거부한다.
- 프롬프트 테두리의 세션 제목 칩을 하단 바 오른쪽 끝에 정렬.
- 오른쪽 정렬 항목(목표 표시, 세션 상태, 백그라운드 에이전트 상태)과 잘린 알림이 프롬프트 영역의 나머지와 일관된 오른쪽 여백을 공유하도록 변경.
- [VSCode] 대화 기록 스크린리더 지원 추가 — 답변·권한 요청·오류·상태 변화의 실시간 안내와 턴별 제목 내비게이션.

---

## 🎯 챌린지

이번 버전에서 내 환경에 적용해볼 만한 항목입니다.

### 1. 샌드박스 읽기 차단으로 시크릿 파일 보호
- **파일**: `~/.claude/settings.json`
- **근거**: 이번 버전에서 macOS 와일드카드 읽기-차단 규칙이 허용 영역 안에서도 우선 적용되고 이름 변경으로 우회되지 않게 강화됐다. `**/.env`, `**/*.pem` 같은 deny 규칙을 넣으면 CLAUDE.md §6 의 "deploy 전 시크릿 유출 직접 확인" 부담을 도구 차원에서 줄인다. 지금 settings.json 에는 샌드박스 읽기 차단 규칙이 없다.
- **난이도**: ★☆☆ (약 10분)

### 2. `ANTHROPIC_DEFAULT_MODEL` 로 시작 모델 분리
- **파일**: `~/.claude/settings.json`
- **근거**: 현재 `"model": "opus[1m]"` 로 고정돼 있어 세션 중 `/model` 로 바꾼 값과 설정값의 관계가 헷갈린다. `env` 에 `ANTHROPIC_DEFAULT_MODEL` 을 두면 "새 세션 출발점"만 정하고 `/model` 선택은 재시작 후에도 유지된다 — 임시로 다른 모델을 쓸 때 설정 파일을 건드릴 필요가 없어진다.
- **난이도**: ★☆☆ (약 5분)

### 3. 스피너 팁 캐시 깨짐 점검
- **파일**: `~/.claude.json`
- **근거**: guest-pass 보상 캐시가 깨져 있으면 팁이 안 뜨고 백그라운드 오류가 반복된다. 이번 버전이 이를 고쳤지만, 이미 깨진 캐시가 남아 있는지 한 번 확인해두면 statusLine 플러그인(token-optimizer)과 겹치는 잡음의 원인을 지울 수 있다.
- **난이도**: ★☆☆ (약 5분)

### 4. `notify_when_idle` 로 세션 완료 알림 통합
- **파일**: `~/.claude/settings.json` 의 `hooks.Stop`
- **근거**: 지금은 `Stop` 훅에서 `osascript` 로 macOS 알림을 띄운다. 여러 세션을 병렬로 돌릴 때(CLAUDE.md §7.2) 다른 세션이 언제 끝나는지는 알 수 없다. `SendMessage` 의 `notify_when_idle` 을 쓰면 폴링 없이 1회 알림을 받을 수 있어 병렬 dispatch 후 대기 패턴이 단순해진다. 훅과 병행 가능하다.
- **난이도**: ★★☆ (약 15분)
