# Claude Code v2.1.270

> 작성일: 2026-09-14

---

# 📋 요약본

## 🛠️ 개선/수정 (1건)
- **Bash 읽기 전용 git 명령 권한 재요청 회귀 수정** — 세션을 한동안 켜둔 뒤 `git status`, `git log` 같은 읽기 전용 git 명령이 갑자기 권한 승인을 다시 묻던 문제를 고쳤다. 2.1.269에서 생긴 회귀다. 이제 긴 세션에서도 읽기 전용 git 명령은 처음 허용한 그대로 조용히 실행된다.

## 🔑 이번 버전의 핵심 키워드
**"긴 세션에서도 읽기 전용 git은 다시 묻지 않는다"** — 2.1.269 회귀 하나만 되돌린 핫픽스 버전.

---

# 📜 원문 (한글 번역본)

> 원문 ChangeLog를 원래 순서 그대로 한 줄도 빠짐없이 번역한 문서입니다.

- 세션이 한동안 실행된 뒤 Bash의 읽기 전용 git 명령이 예기치 않게 권한을 요청하던 문제를 수정했습니다 (2.1.269에서 발생한 회귀)

---

## 🎯 챌린지

이번 버전에서 내 환경에 적용해볼 만한 항목입니다.

### 1. 읽기 전용 git 명령 allowlist 고정
- **파일**: `~/.claude/settings.json`
- **근거**: 이번 수정은 회귀를 되돌린 것일 뿐, 권한 허용 자체가 세션 메모리에 의존한다. `permissions.allow`에 `Bash(git status:*)`, `Bash(git log:*)`, `Bash(git diff:*)`, `Bash(git branch:*)`를 명시해두면 세션 길이·버전과 무관하게 프롬프트가 사라진다. 현재 `settings.json`에는 `permissions` 키 자체가 없다.
- **난이도**: ★☆☆ (약 10분)

### 2. `deploy-guard.sh` 가 읽기 전용 git을 그냥 통과시키는지 확인
- **파일**: `~/.claude/hooks/deploy-guard.sh`
- **근거**: `PreToolUse` 훅이 모든 `Bash` 호출을 가로챈다. 이번 버전이 고친 건 harness 쪽 권한 판정이지 훅이 아니다. 훅이 `git status`·`git log` 같은 읽기 명령에서 지연이나 추가 확인을 유발하지 않는지, `push` 차단 로직에만 걸리는지 직접 실행해 확인한다.
- **난이도**: ★★☆ (약 15분)

### 3. `/fewer-permission-prompts` 로 실제 프롬프트 유발 명령 수집
- **파일**: `~/.claude/settings.json` (또는 프로젝트 `.claude/settings.json`)
- **근거**: 추측으로 allowlist를 짜는 대신 지난 transcript에서 실제로 권한을 물었던 명령을 뽑아낸다. git 외에 반복 확인을 유발하던 읽기 전용 명령도 같이 잡힌다. 1번 챌린지를 먼저 하면 중복 항목은 걸러진다.
- **난이도**: ★★☆ (약 15분)
