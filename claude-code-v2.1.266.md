# Claude Code v2.1.266

> 작성일: 2026-09-10

---

# 📋 요약본

## 🛠️ 개선/수정 (1건)
- **`CLAUDE_CODE_USE_GATEWAY` 단독 설정 시 게이트웨이 강제 로그인 회귀 수정** — LLM 게이트웨이·프록시 환경에서 2.1.265가 깨뜨린 동작을 되돌렸다.
  - 원래 이 환경변수는 `ANTHROPIC_BASE_URL` 과 `ANTHROPIC_AUTH_TOKEN` 이 **둘 다** 설정된 경우에만 의미가 있었고, 그 외에는 무시됐다.
  - 2.1.265에서는 이 변수만 있어도 Cloud 게이트웨이 로그인을 강제했다. 그래서 API 키·`apiKeyHelper`·커스텀 인증 헤더와 함께 쓰던 설정은 모든 요청이 "Not signed in to the Cloud gateway" 로 실패했다.
  - 이제 변수 단독으로는 다시 무시된다. 사용자가 설정을 바꿀 필요는 없다.

## 🔑 이번 버전의 핵심 키워드
**"게이트웨이 인증 회귀 원복"** — 직전 버전에서 프록시·API 키 사용자를 전부 막던 인증 강제를 되돌린 단일 핫픽스 릴리스.

---

# 📜 원문 (한글 번역본)

> 원문 ChangeLog를 원래 순서 그대로 한 줄도 빠짐없이 번역한 문서입니다.

## 2.1.266

- LLM 게이트웨이 및 프록시 구성에 영향을 주던 2.1.265 회귀를 수정했다. 문서화되지 않은 환경변수 `CLAUDE_CODE_USE_GATEWAY` 는 이전까지 `ANTHROPIC_BASE_URL` 과 `ANTHROPIC_AUTH_TOKEN` 이 모두 설정되지 않는 한 무시됐으나, 2.1.265에서는 이 변수 자체가 Cloud 게이트웨이 로그인을 강제하기 시작했다. 그 결과 이 변수를 API 키, `apiKeyHelper`, 또는 커스텀 인증 헤더와 함께 설정한 구성은 모든 요청이 "Not signed in to the Cloud gateway" 로 실패했다. 이제 이 변수는 단독으로는 다시 무시된다. 설정 변경은 필요 없다.

---

## 🎯 챌린지

이번 버전에서 내 환경에 적용해볼 만한 항목입니다.

### 1. 게이트웨이 환경변수가 셸에 남아 있는지 확인
- **파일**: `~/.zshrc`, `~/.claude/settings.json`
- **근거**: 이번 수정은 `CLAUDE_CODE_USE_GATEWAY` 가 단독으로 켜져 있을 때 인증이 전부 실패하던 문제다. `env | grep -E 'CLAUDE_CODE_USE_GATEWAY|ANTHROPIC_BASE_URL|ANTHROPIC_AUTH_TOKEN'` 로 셸에 남은 값이 있는지 보고, 쓰지 않는 변수는 `~/.zshrc` 에서 지운다. 현재 `settings.json` 에는 `env` 블록이 없어 셸 쪽만 보면 된다.
- **난이도**: ★☆☆ (약 5분)

### 2. 버전 고정 없이 자동 업데이트를 쓰는지 점검
- **파일**: `~/.claude/settings.json`
- **근거**: 2.1.265처럼 하루 만에 원복되는 회귀가 실제로 나온다. 인증·프록시 설정을 쓰는 환경이라면 회귀 버전을 그대로 받는 위험이 있다. `claude --version` 으로 현재 버전을 확인하고, 문제가 생겼을 때 되돌릴 수 있게 정상 동작한 버전 번호를 메모해 둔다.
- **난이도**: ★☆☆ (약 5분)

### 3. 인증 실패 시 원인 구분 메모 추가
- **파일**: `~/.claude/CLAUDE.md`
- **근거**: "Not signed in to the Cloud gateway" 는 실제로 로그인이 안 된 것이 아니라 환경변수 조합이 잘못된 경우에도 나온다. 이 문구가 뜨면 먼저 `CLAUDE_CODE_USE_GATEWAY` 계열 환경변수를 의심하라는 한 줄을 §6 Deploy 정책 근처에 남겨두면 다음에 같은 증상에서 시간을 안 뺏긴다.
- **난이도**: ★☆☆ (약 5분)
