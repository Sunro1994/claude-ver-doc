# Claude Code v2.1.201

> 작성일: 2026-07-05

---

# 📋 요약본

## 🛠️ 개선/수정 (1건)
- **Sonnet 5 세션의 하네스 리마인더 전달 방식 변경** — Claude Sonnet 5 세션에서 하네스(harness = Claude Code 실행 층, 툴 결과·주의 안내를 대화에 끼워 넣는 부분) 리마인더를 더 이상 대화 중간 `system` 역할 메시지로 넣지 않는다. 즉 대화 도중 `system` 역할로 주입되던 안내가 사라져, 대화 흐름과 컨텍스트가 더 깔끔해진다. (Opus 세션이 아닌 Sonnet 5 세션에만 적용.)

## 🔑 이번 버전의 핵심 키워드
**"Sonnet 5는 대화 중간 system 리마인더를 더 이상 쓰지 않는다"** — 하네스 안내 주입 방식을 정리해 컨텍스트를 깨끗하게 유지한다.

---

# 📜 원문 (한글 번역본)

> 원문 ChangeLog를 원래 순서 그대로 한 줄도 빠짐없이 번역한 문서입니다.

- Claude Sonnet 5 세션은 이제 하네스 리마인더에 대화 중간(mid-conversation) `system` 역할을 사용하지 않는다.

---

## 🎯 챌린지

이번 버전에서 내 환경에 적용해볼 만한 항목입니다.

### 1. 매 턴 실행되는 hook의 컨텍스트 주입량 점검
- **파일**: `~/.claude/settings.json`
- **근거**: 이번 버전은 Sonnet 5에서 대화 중간 `system` 리마인더를 걷어내 컨텍스트를 아끼는 방향이다. 같은 원리로, 매 `UserPromptSubmit`·`PreToolUse` 마다 도는 커스텀 hook(컨텍스트 경고, `persona-drift-warn.sh`, 읽기 이력 체크)이 실제로 임계 조건에서만 메시지를 내보내는지 확인하면 불필요한 주입을 줄일 수 있다. 컨텍스트 경고 hook이 `fill_pct >= 80` 에서만 stderr를 내는지, 다른 hook이 조용히 통과(`exit 0`)하는지 점검한다.
- **난이도**: ★★☆ (약 15분)

### 2. Sonnet 5 세션에서 커스텀 리마인더 노출 확인
- **파일**: `~/.claude/settings.json` (`hooks` 블록)
- **근거**: 이 변경은 Sonnet 5 전용이라 Opus 고정(CLAUDE.md §5) 환경엔 직접 영향이 없다. 하지만 Sonnet 5로 한 세션을 돌려 `persona-drift-warn.sh` 같은 커스텀 리마인더가 여전히 정상 노출되는지 미리 확인해 두면, 향후 Sonnet 5를 부분 채택할 때 안내가 묻히는 사고를 막는다. 확인 후 결과를 hook 주석에 1줄 기록한다.
- **난이도**: ★☆☆ (약 8분)
