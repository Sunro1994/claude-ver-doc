# Claude Code v2.1.272

> 작성일: 2026-09-15

---

# 📋 요약본

## 🛠️ 개선/수정 (1건)
- **버그 수정 및 안정성 개선** — 개별 항목이 공개되지 않은 유지보수 릴리스. 세부 변경 내역 없이 전반적인 버그 수정과 동작 안정성 향상만 명시됐다.

## 🔑 이번 버전의 핵심 키워드
**"조용한 안정화 릴리스"** — 새 기능 없이 버그 수정과 안정성만 다진 버전이다.

---

# 📜 원문 (한글 번역본)

> 원문 ChangeLog를 원래 순서 그대로 한 줄도 빠짐없이 번역한 문서입니다.

- 버그 수정 및 안정성 개선

---

## 🎯 챌린지

이번 버전에서 내 환경에 적용해볼 만한 항목입니다.

### 1. Stop 훅 알림에 프로젝트 이름 넣기
- **파일**: `~/.claude/settings.json`
- **근거**: 지금 Stop 훅은 "작업이 완료되었습니다" 만 띄운다. EOS-H5·poplus·개인 프로젝트를 여러 세션으로 동시에 돌리는 환경이라 어느 세션이 끝났는지 알림만 보고는 구분이 안 된다. `display notification` 문구에 `$(basename "$PWD")` 를 넣어 프로젝트명을 붙인다.
- **난이도**: ★☆☆ (약 5분)

### 2. 비활성 플러그인 4개 정리
- **파일**: `~/.claude/settings.json`
- **근거**: `enabledPlugins` 에 `false` 로 꺼둔 항목이 5개(`andrej-karpathy-skills`·`token-optimizer`·`understand-anything`·`vercel`·`playground`·`swift-lsp`·`clangd-lsp`)다. 그런데 `statusLine` 은 꺼진 `token-optimizer` 플러그인 캐시 경로의 스크립트를 실행하고 있어, 플러그인 캐시가 정리되면 상태줄이 깨진다. 스크립트를 `~/.claude/` 아래로 복사해 경로를 옮기거나 플러그인을 다시 켠다.
- **난이도**: ★★☆ (약 15분)

### 3. 안정화 릴리스 확인용 버전 고정 확인
- **파일**: `~/.claude/settings.json`
- **근거**: 이번은 기능 변화 없는 안정화 버전이라 업데이트 리스크가 낮다. `cleanupPeriodDays: 30` 으로 트랜스크립트가 30일 후 삭제되는데, 라이브 점검 회차 기록을 세션 로그에서 되짚는 일이 잦다면 60~90일로 늘려둔다.
- **난이도**: ★☆☆ (약 5분)
