# Claude Code v2.1.269

> 작성일: 2026-09-12

---

# 📋 요약본

## 🎉 신기능 (8건)
- **`claude plugin eval`** — 플러그인의 평가(eval) 스위트를 Claude Code 상대로 돌려 점수가 매겨진 재현 가능한 결과를 낸다. JSON + HTML 리포트로 출력. `claude plugin eval --help` 참고.
- **`/output-style [name]`** — 출력 스타일을 목록으로 보고 바꾼다. Remote Control, 클라우드, 기타 headless 세션에서도 동작한다.
- **Bash 편집 diff (`bashEditDiffEnabled`)** — Bash 도구가 파일을 수정하면 그 명령이 바꾼 파일의 diff 를 Bash 결과에 붙여준다.
- **`OTEL_METRICS_INCLUDE_REPOSITORY`** — OpenTelemetry 메트릭·이벤트에 `vcs.*` 저장소 속성을 태깅한다. `OTEL_LOG_TOOL_DETAILS` 를 같이 켜면 커밋 이벤트에 `vcs.ref.head.*` 가 붙는다.
- **`CLAUDE_CODE_GATEWAY_MODEL_DISCOVERY_TIMEOUT_MS`** — LLM 게이트웨이의 `/v1/models` 탐색 타임아웃을 늘린다(기본 3초).
- **`/focus` 스피너 팁** — 프롬프트·한 줄 작업 요약·응답만 보여주는 뷰를 제안하는 팁이 스피너에 뜬다.
- **`CLAUDE_CODE_WORKFLOW_MAX_CONCURRENT_AGENTS` (1–256)** — Workflow 도구의 실행당 동시 에이전트 한도를 올린다. 추론 부하가 큰 fan-out 용.
- **[VSCode] 에이전트 맵·Hooks 다이얼로그·Permission rules 다이얼로그** — 푸터의 "N agents" 알약을 누르면 서브에이전트 맵(에이전트별 카드, Stop agent, 읽기 전용 트랜스크립트)이 열린다. Hooks 와 권한 규칙을 명령 메뉴 다이얼로그에서 user/project/local 설정 단위로 보고 추가·수정·삭제할 수 있다(관리형·플러그인·세션 항목은 읽기 전용).

## 🛠️ 개선/수정 (다수)
- **프롬프트 캐시** — 출력 토큰 한계로 잘렸다 자동 재개된 다음 턴에서 캐시가 부분 무효화되던 문제, 생각 중간에 끊고 세션을 재개할 때 이전 컨텍스트 재전송 방식이 바뀌어 캐시 재사용이 나빠지던 문제 수정. 클라우드 세션은 첫 요청 전 서버 설정을 잠깐 기다려 캐시 미스를 줄인다.
- **권한 규칙** — `!` 로 시작하는 deny/ask 규칙이 그것을 쓴 설정 소스 밖까지 적용되던 문제 수정(이제 자기 소스 안에서만 적용, 맨 `!` 부정은 무시). `Edit()` deny 규칙과 쓰기 경로 검사가 Bash `tee` 대상 파일에 적용되지 않던 문제 수정 — `Bash(tee:*)` 허용 규칙이 작업 디렉토리 밖 대상까지 덮지 않는다.
- **터미널 입력·렌더링** — kitty 프로토콜 터미널의 F1/F2/F4, st 의 Delete, rxvt-unicode 의 Alt+화살표, WezTerm 의 Shift+문장부호 회귀(2.1.247) 수정. 시작 시 터미널 응답(`^[[?1;2c`, `22c` 등)이 프롬프트에 잡문자로 들어가던 문제, 전체화면 리사이즈 후 상·하단 행이 비던 문제, 외부 편집기 복귀 후 화면이 두 번 그려지던 문제 수정.
- **압축(compaction) 후 git 상태** — 세션 시작 시점이 아니라 현재 상태를 알려준다.
- **`/goal` 안정성** — API 오류·네트워크 끊김·토큰 한도로 조용히 멈추던 것이 백오프 재시도 또는 사유를 밝힌 일시정지로 바뀐다(사용량 한도 해제까지 대기 포함).
- **`/btw`** — 곁다리 질문 답변에 지어낸 도구 호출·출력이 섞이던 문제 수정. 이제 쓰지 말라고 지시하고, 나오면 "실행되지 않음"으로 표시한다.
- **커밋 귀속(attribution)** — 귀속 리마인더가 CLAUDE.md·메모리의 "커밋/PR 귀속 금지" 규칙을 덮어쓰던 문제 수정. 관리형 설정이 정한 줄은 그대로 적용.
- **다국어 처리** — 일본어·중국어·태국어 등 띄어쓰기 없는 언어에서 프롬프트 제안이 버려지던 문제 수정. 한·중·일 텍스트의 제안 필터링 개선(혼합 문자·단일 단어 제안 유지, 메타·평가성 텍스트 제거).
- **플러그인 보안·수명** — 세션용으로 푼 플러그인 아카이브를 다른 로컬 사용자가 읽을 수 있던 문제, 아카이브의 world-writable 비트가 남던 문제, 재추출 후 옛 파일이 살아남던 문제 수정. `shutdown` 파라미터를 거부하는 LSP 서버(rust-analyzer 등)가 세션 종료 후에도 떠 있던 문제 수정.
- **백그라운드·재개 세션** — 백그라운드 에이전트가 도는 중에도 원격·headless 세션이 "입력 대기 중"으로 보고하던 문제, 재개된 headless 세션이 모델 전환·재시도 중 턴 응답을 잃던 문제, 백그라운드 작업 기록의 이스케이프 코드·줄바꿈·과대 텍스트가 작업 목록과 알림에 새어나오던 문제 수정.
- **긴 세션 응답성** — 트랜스크립트 갱신 때 접힌 도구 사용 요약을 만들려고 대화 전체를 다시 처리하지 않는다.
- **`/diff`·`/ultrareview --post`** — `/diff` 패널이 로딩 상태 없이 한 번에 완성된 상태로 열린다. `/ultrareview --post` 는 결과가 오면 두 번째 클라우드 세션을 띄우지 않고 바로 PR 코멘트를 달고 링크를 출력한다.
- **웹·Slack(Claude Tag)** — 클라우드 세션에서 큐에 넣은 메시지를 Claude 가 읽기 전에 회수 가능. 일회성 예약 루틴 중복 실행, 서브에이전트 루틴의 조기 완료 판정, 서브폴더 작업 시 파일 링크 404 수정. Slack 은 실패 알림 재시도, 미승인 모델 전환 거부, 표 원시 텍스트 게시, 공유 범위 오표기 등 수정.

## 🔑 이번 버전의 핵심 키워드
**"캐시·권한·터미널 입력의 구멍을 메우고, 플러그인 평가와 Workflow 동시성을 열었다"** — 눈에 띄는 신기능보다 매일 쓰는 경로의 신뢰성 보강이 본질인 버전.

---

# 📜 원문 (한글 번역본)

> 원문 ChangeLog를 원래 순서 그대로 한 줄도 빠짐없이 번역한 문서입니다.

- `claude plugin eval` 추가: 플러그인의 eval 스위트를 Claude Code 상대로 실행해 점수화된 재현 가능한 결과(JSON + HTML 리포트)를 얻는다. `claude plugin eval --help` 참고
- `/output-style [name]` 추가: 출력 스타일을 나열하고 전환한다. Remote Control, 클라우드 및 기타 headless 세션에서도 가능
- Bash 도구가 파일 편집을 처리할 때, 그 Bash 명령이 변경한 파일들의 diff 를 Bash 도구 결과에 추가(설정 `bashEditDiffEnabled`)
- `OTEL_METRICS_INCLUDE_REPOSITORY` 추가: OpenTelemetry 메트릭·이벤트에 `vcs.*` 저장소 속성을 태깅. `OTEL_LOG_TOOL_DETAILS` 와 함께 쓰면 커밋 이벤트에 `vcs.ref.head.*` 가 붙는다
- `CLAUDE_CODE_GATEWAY_MODEL_DISCOVERY_TIMEOUT_MS` 추가: LLM 게이트웨이의 `/v1/models` 탐색 타임아웃 연장(기본 3초)
- 프롬프트, 한 줄 작업 요약, 응답만 보여주는 뷰를 위해 `/focus` 를 제안하는 스피너 팁 추가
- `CLAUDE_CODE_WORKFLOW_MAX_CONCURRENT_AGENTS` (1–256) 추가: 추론 부하가 큰 fan-out 을 위해 Workflow 도구의 실행당 동시 에이전트 한도를 올린다
- 출력 토큰 한계로 응답이 잘렸다가 자동 재개된 다음 턴에 프롬프트 캐시가 부분적으로 무효화되던 문제 수정
- Claude 를 생각 도중에 중단한 뒤 세션을 재개하면 이전 컨텍스트 재전송 방식이 바뀌어 프롬프트 캐시 재사용이 나빠질 수 있던 경우 수정
- kitty 프로토콜 터미널에서 F1/F2/F4 가 동작하지 않던 문제, st 의 Delete, rxvt-unicode 에서 Alt+화살표가 Escape 로 동작하던 문제, WezTerm 에서 Shift+문장부호가 비시프트 키로 입력되던 문제(2.1.247 회귀) 수정
- 백그라운드 에이전트가 아직 실행 중인데 원격·headless 세션이 "waiting for your input" 으로 보고하던 문제 수정(`CLAUDE_CODE_BG_TASKS_REPORT_RUNNING=0` 으로 이전 동작 복원 가능)
- 일부 터미널에서 시작 시 터미널의 기능 질의 응답(`^[[?1;2c`)이 잡문자로 표시되던 문제 수정
- 터미널 리사이즈 후 전체화면에서 트랜스크립트 상단·하단 행이 비던 문제 수정
- `!` 로 시작하는 deny 또는 ask 권한 규칙이 그것을 작성한 설정 소스 너머까지 적용되던 문제 수정. 이제 그런 규칙은 자기 소스 안에서만 적용되고, 맨 `!` 부정은 무시된다
- 압축(compaction) 후 Claude 에게 전달되는 git 상태 수정: 이제 세션 시작 시점이 아니라 현재 상태다
- 원격 세션 재개 시 동기화된 플러그인 MCP 서버가 연결되지 않던 문제 수정
- 재개된 headless 세션이 턴 도중 모델이 전환되거나 요청이 재시도될 때 그 턴의 응답을 잃던 문제 수정
- 작업 재개 시 백그라운드 작업의 디스크 기록에서 터미널 이스케이프 코드·줄바꿈·과대 텍스트가 작업 목록과 작업 알림에 흘러들던 문제 수정
- CMYK JPEG 이미지가 "cannot decode" 로 첨부되지 않던 문제 수정. 이제 다른 JPEG 처럼 변환·리사이즈된다
- scheme 없이 설정된 gRPC 텔레메트리 엔드포인트에 대해 관리형 설정 승인 다이얼로그가 수집기(collector)를 명시하지 않던 문제 수정
- 플러그인 `headersHelper` 동의 프롬프트가 다른 호스트로 오독될 수 있는 URL 경로를 보여주던 문제 수정
- 플러그인 오류가 `@` 로 시작하는 폴더명을 가진 상대 Windows 경로 자리에 `[redacted URL]` 을 보여주던 문제 수정
- 터미널의 네이티브 커서가 켜져 있을 때 권한 규칙, 자동 모드 규칙, 디렉토리 추가, 세션 이름 변경, 피드백 검토 텍스트 필드에서 커서가 보이지 않던 문제 수정
- `/fork` 영수증을 1초 이내 간격으로 반복 클릭하면, 현재 도구가 끝나기를 기다리는 동안 세션이 바로 백그라운드로 내려가지 않던 문제 수정
- `shutdown` 파라미터를 거부하는 플러그인 LSP 서버(예: rust-analyzer)가 세션 종료 시 살아남던 문제 수정. 이제 `shutdown` 이 실패해도 `exit` 를 보낸다
- 귀속(attribution) 리마인더가 커밋·PR 귀속을 금지하는 CLAUDE.md 또는 메모리 규칙을 덮어쓰던 문제 수정. 관리형 설정이 정한 줄은 계속 적용된다
- 일본어·중국어·태국어 등 단어 사이에 공백이 없는 언어의 텍스트에서 프롬프트 제안이 누락되던 문제 수정
- GNOME Terminal 과 Konsole 중 동기화된 출력(synchronized output)을 지원하지 않는 버전에서 터미널 이름만 보고 지원한다고 가정하던 문제 수정
- `--output-format stream-json` 결과의 `permission_denials` 가 경로 범위 deny 규칙으로 차단된 Read·Edit·Write 호출을 빠뜨리던 문제 수정
- SDK 또는 데스크톱 앱으로 실행된 세션이 다른 세션의 에이전트 목록에서 알 수 없는 상태로 표시되던 문제 수정
- 기본 Opus 모델에 접근할 수 없는 계정의 Bedrock·Vertex·Foundry·게이트웨이 배포에서 `/insights` 가 실패하던 문제 수정. 이제 그런 환경에서는 세션 모델을 쓴다
- 다른 Claude Code 프로세스가 같은 순간에 로그인을 갱신할 때 조직 정책 제한이 세션에 로드되지 않던 문제 수정
- Bedrock·Vertex·게이트웨이를 쓰는 Claude Desktop 세션이 턴 종료 시 맥락형 "what Claude needs" 알림 문구를 받지 못하던 문제 수정
- 갱신된 설정이 서버 URL 의 쿼리 파라미터 순서만 바꿨을 때 MCP 서버가 재연결되던 문제 수정
- 이름이나 설명에 줄바꿈이 있거나 터미널보다 넓은 백그라운드 에이전트를 볼 때 프롬프트 박스 상단 테두리가 여러 줄로 갈라지던 문제 수정
- 자동 압축이 요약할 완전한 이전 대화 교환을 찾지 못했을 때 세션이 "Prompt is too long" 에서 영구히 멈추던 문제 수정(주로 매우 큰 프롬프트를 쓰는 Agent SDK 세션)
- API 오류·네트워크 끊김·토큰 한도 이후 `/goal` 실행이 조용히 멈추던 문제 수정: 이제 백오프로 재시도하거나, 멈추고 이유를 말한다. 사용량 한도가 초기화될 때까지 대기하는 경우 포함
- 첫 요청 전에 서버 설정을 잠깐 기다려 클라우드 세션의 프롬프트 캐시 미스를 수정
- 지어낸 도구 호출과 출력이 들어간 `/btw` 답변 수정: 이제 곁다리 질문에 그런 것을 쓰지 말라고 지시하고, 나타나면 실행되지 않았다고 표시한다
- `CLAUDE_CODE_RESUME_INTERRUPTED_TURN` 이 6시간보다 전에(또는 `CLAUDE_CODE_RESUME_INTERRUPTED_TURN_MAX_AGE_MS` 설정 시 그보다 오래 전에) API 오류로 실패한 턴을 다시 실행하던 문제 수정
- 관리형 설정으로 활성화된 조직 플러그인이 headless 세션과 Claude Desktop(이 CLI 버전이 Desktop 에 번들되면)에서 로드되지 않던 문제 수정. 다음 세션부터 로드된다
- 세션용으로 추출된 플러그인 아카이브를 다른 로컬 사용자가 읽을 수 있던 문제, 추출된 파일이 아카이브의 world-writable 비트를 유지하던 문제, 재추출 후 오래된 파일이 남던 문제 수정
- `Edit()` deny 규칙과 쓰기 경로 검사가 Bash `tee` 명령이 쓰는 파일에 적용되지 않던 문제 수정. `Bash(tee:*)` 허용 규칙이 더 이상 작업 디렉토리 밖의 대상까지 덮지 않는다
- 느린 연결(ssh, 브라우저 터미널)에서 시작 시 `22c` 같은 잡문자나 터미널의 색상·버전 응답이 프롬프트에 입력되던 문제 수정
- rxvt-unicode 에서 전체화면을 나가거나 다시 들어간 뒤 터미널의 블록 커서가 인터페이스 아래 보이던 문제 수정
- rxvt-unicode 전체화면 모드에서 외부 편집기에서 돌아온 뒤 커서 블록이 계속 보이던 문제 수정
- 전체화면이 아닌 모드에서 외부 편집기(Ctrl+G)에서 돌아온 뒤 인터페이스가 두 번 그려지던 문제 수정
- Konsole 에서 외부 편집기에서 돌아온 뒤 인터페이스가 두 번 그려지던 문제 수정
- Windows: 백그라운드로 보낸 PowerShell 도구 명령이 Claude Code 종료 시 중단되던 문제 수정
- `/diff` 패널 개선: 로딩 상태를 먼저 보여주지 않고 한 번에 완전히 렌더링된 상태로 열린다
- 일본어·중국어·한국어 텍스트의 프롬프트 제안 필터링 개선: 혼합 문자와 단일 단어 제안은 유지되고, 메타적·평가적 텍스트는 영어에서처럼 버려진다
- Skill 도구의 "Unknown skill" 오류 개선: 짧은 이름이 정확히 하나의 플러그인 스킬과 일치하면 그 플러그인 스킬의 전체 이름을 알려준다
- SSH 및 인식되지 않는 터미널에서의 키보드 지원 개선: kitty 키보드 질의에 응답하는 터미널(foot, Alacritty 0.16+ 등)은 이제 Shift+Enter 와 Ctrl+Shift 단축키를 받는다
- 긴 세션의 응답성 개선: 트랜스크립트 갱신 시 접힌 도구 사용 요약을 만들기 위해 대화 전체를 다시 처리하지 않는다
- 텔레메트리를 끈 1st-party 세션 개선: 대화 도중 연결을 마친 `alwaysLoad` MCP 서버를 도구 검색 왕복 없이 다음 턴에 바로 쓸 수 있다
- `/ultrareview --post` 변경: 결과가 도착하면 두 번째 클라우드 세션을 시작하는 대신 PR 코멘트를 직접 달고 코멘트 링크를 출력한다
- 세션 스크래치패드에 저장하는 아티팩트 데이터베이스 읽기를 작업 폴더 승인 없이 진행하도록 변경
- 클라우드 세션에서 claude.ai 로부터 동기화된 스킬의 이름을 `anthropic-skills:<name>` 으로 변경(Claude Desktop 과 일치). 다른 것이 그 이름을 쓰지 않으면 짧은 이름도 계속 동작
- [VSCode] 에이전트 맵 추가: 푸터의 "N agents" 알약을 누르면 세션 서브에이전트 맵이 열린다. 에이전트별 카드, Stop agent, 읽기 전용 트랜스크립트 포함
- [VSCode] 명령 메뉴에 Hooks 다이얼로그 추가: user·project·local 설정의 훅을 보고 추가·수정·삭제한다. 관리형·플러그인·세션 훅은 읽기 전용
- [VSCode] Focus 뷰의 도구 호출 그룹 아래에 실행 중인 서브에이전트의 라이브 진행 행 추가
- [VSCode] Permission rules 다이얼로그 추가: 권한 규칙을 나열하고 user·project·local 설정에서 추가·삭제한다. 시작 옵션·세션 전용·관리형 규칙은 읽기 전용
- [VSCode] Switch account 화면에 Cancel 버튼 추가: 현재 계정 그대로 세션으로 돌아간다
- [VSCode] Focus 뷰가 예약 작업 등 전달된 평문 프롬프트로 시작된 턴을 이전 턴의 일부로 보여주던 문제 수정
- [VSCode] 패널이 좁을 때 푸터의 프롬프트 캐시 시계가 분 표시를 숨기던 문제 수정
- [VSCode] 설정 파일이나 `environmentVariables` 설정으로 `CLAUDE_CONFIG_DIR` 를 지정했을 때 세션 목록이 기본 폴더의 세션을 계속 들고 있던 문제 수정
- [VSCode] 늦게 로딩을 마친 플랜 미리보기가 코멘트 박스를 숨기거나 옛 플랜을 보여주던 문제 수정
- [VSCode] 플랜 미리보기가 Claude 탭이 닫힌 뒤 아무데도 가지 않는 코멘트를 받던 문제 수정
- [VSCode] 마지막 응답 뒤 압축되고 닫힌 세션의 프롬프트 캐시 시계와 재개 안내 수정. 다시 열면 cold 로 표시된다
- [VSCode] Remote Control 이 켜진 상태에서 확장에서 이름을 바꾼 세션이 claude.ai/code 에서 옛 이름을 유지하던 문제 수정
- [VSCode] 터미널에서 설정을 기본값으로 되돌린 뒤에도 "Enable Remote Control for all sessions" 토글이 마지막 위치를 유지하던 문제 수정
- [VSCode] 창 리로드 후 복원된 Claude 탭이 클릭 전까지 세션 목록에서 열린 것으로 집계되지 않고, 그 행이 두 번째 탭을 열던 문제 수정
- [VSCode] Switch account 로 같은 계정에 다시 로그인하면 탭이 닫아둔 사용량 한도 경고를 잊던 문제 수정
- [VSCode] 긴 턴 도중 이름을 바꾼 세션이 창 리로드 후 생성된 이름으로 되돌아가던 문제 수정
- [VSCode] 계정이 해당 한도를 잃은 뒤에도 사이드바 사용량 미터가 모델별 주간 한도 행을 남겨두던 문제 수정
- [VSCode] 새 채팅 뷰가 시작되는 중에 키보드 단축키로 보낸 @-멘션이 키 입력 한참 뒤에 입력창에 삽입되던 드문 경우 수정
- [VSCode] Claude 사이드바를 연 직후 Account & usage 헤더가 잠시 뒤 나타나면서 세션 목록이 아래로 튀던 문제 수정
- [VSCode] 사용자가 아닌 다른 사람을 위해 쓰는 문서와 메시지 개선: 이제 Claude 가 그 독자를 대상으로 쓰고 응답 상단에 독자를 밝힌다
- [VSCode] 슬래시 명령 메뉴, @-멘션 메뉴, 출력 스타일 선택기, Send/Stop 버튼, 권한·질문 카드, 온보딩 체크리스트의 스크린 리더·키보드 접근성 개선
- [VSCode] 메시지 박스의 현재 파일 칩 변경: Hide 토글 대신 X 로 제거한다
- [VSCode] 세션 탭 우클릭 메뉴와 편집기 제목 표시줄 "..." 메뉴에서 Claude Code 항목 제거. 메뉴가 열린 탭에 대해 동작할 수 없었다
- [Claude Code on the web] 클라우드 세션에서 큐에 넣은 메시지를 Claude 가 읽기 전에 회수하는 기능 추가: 큐에서 제거하거나 Esc 또는 위 화살표를 누르면 텍스트가 메시지 박스로 돌아온다
- [Claude Code on the web] Claude Code 가 쓸 수 있는 모델을 제한하는 조직에서 클라우드 세션의 `/model default` 가 이후 모든 메시지를 실패시키던 문제 수정
- [Claude Code on the web] 일회성 예약 루틴이 일시적 서버 오류 후 가끔 두 번 실행되던 문제 수정
- [Claude Code on the web] 서브에이전트를 쓰는 루틴 실행이 너무 일찍 완료로 처리되어 실제 실패 후 재시도를 건너뛰거나 중복 실행을 시작할 수 있던 문제 수정
- [Claude Code on the web] Claude 가 저장소의 하위 폴더에서 작업했을 때 클라우드 세션 트랜스크립트의 파일 링크가 GitHub 404 를 열던 문제 수정
- [Claude Code on the web] Cloud environments 관리 페이지 변경: 도달할 수 없는 Show more 컨트롤 뒤로 표를 5행으로 제한하지 않고 모든 환경을 나열한다
- [Claude Code on the web] Free 플랜 사용자의 claude.ai/code 변경: 진행 방법이 없는 "Disabled by org admin" 페이지 대신 업그레이드 경로가 있는 요금제 페이지를 연다
- [Claude Tag] 관리자 설정의 GitHub 설치에서 Connect all 또는 Disconnect 전에 확인 다이얼로그 추가. 조직 전체에 영향을 주는 실수를 막는다
- [Claude Tag] Slack 이 잠시 rate limit 을 걸면 실패 알림이 유실되어 스레드가 가끔 조용해지던 문제 수정. 이제 알림을 재시도한다
- [Claude Tag] Claude 가 조직이 활성화하지 않은 모델로의 전환을 수락한 뒤 조용히 대체 모델로 답하던 문제 수정. 이제 거절하고 관리자가 활성화할 수 있다고 말한다
- [Claude Tag] Claude 가 같은 메시지에 파일을 첨부했을 때 표가 원시 파이프 텍스트로 게시되던 문제 수정. 이제 표는 일반 응답으로 게시되고 파일은 평문 캡션과 함께 뒤따른다
- [Claude Tag] Claude 가 활성화되지 않은 채널의 최상위에서 `@Claude !restart` 가 무관한 대화를 시작하던 문제 수정. 이제 재시작할 것이 없다고 비공개로 답한다
- [Claude Tag] Slack 접근 설정의 플러그인 행이 라벨 없는 원시 ID 만 보여주고 끄는 방법이 없던 문제 수정. 이제 이름을 보여주고 그것을 관리하는 번들로 링크한다
- [Claude Tag] Slack 에서 시작된 세션의 공유 배너와 Share 다이얼로그가 조직 전체가 링크를 열 수 있다고 주장하던 문제 수정. 이제 Slack 채널의 대상 범위를 명시한다
- [Claude Tag] 관리자 설정 페이지와 Slack 채널 선택기의 로드 시간 개선. 채널이 많거나 워크스페이스를 여러 개 연결한 조직에서 특히 체감된다
- [Claude Tag] Slack 채널의 예약 루틴 개선: 루틴 실행이 항상 새 최상위 채널 메시지를 올리는 대신 기존 스레드에 답할 수 있다
- [Claude Tag] Claude 의 라이브 진행 체크리스트 타임스탬프 개선: 고정된 UTC 시간 대신 각 읽는 사람의 현지 시간과 갱신 경과 시간을 보여준다

---

## 🎯 챌린지

이번 버전에서 내 환경에 적용해볼 만한 항목입니다.

### 1. Bash 편집 diff 켜기
- **파일**: `~/.claude/settings.json`
- **근거**: `"bashEditDiffEnabled": true` 를 추가하면 `sed`·`tee`·스크립트가 파일을 바꿨을 때 그 diff 가 Bash 결과에 바로 붙는다. CLAUDE.md §3 "Surgical Changes" 와 §12 "지시 범위 준수" 를 검증하는 수단 — 의도하지 않은 파일 변경을 그 자리에서 잡는다.
- **난이도**: ★☆☆ (약 5분)

### 2. `tee` 쓰기 경로 방어 확인
- **파일**: `~/.claude/settings.json`, `~/.claude/hooks/deploy-guard.sh`
- **근거**: 이번 버전에서 `Bash(tee:*)` 허용 규칙이 작업 디렉토리 밖까지 덮지 않도록 바뀌었다. 현재 `permissions` 에 넓은 `Bash(...)` 허용이 있는지 확인하고, 있으면 경로를 좁힌다. deploy-guard 훅이 Bash 만 가로채므로 쓰기 경로는 권한 규칙이 마지막 방어선이다.
- **난이도**: ★★☆ (약 15분)

### 3. Workflow 동시 에이전트 한도 조정
- **파일**: `~/.claude/settings.json` 의 `env` 블록
- **근거**: CLAUDE.md §7.2 는 독립 task 병렬 dispatch 를 기본으로 둔다. `CLAUDE_CODE_WORKFLOW_MAX_CONCURRENT_AGENTS` 를 환경에 맞게(예: `"8"`) 명시하면 다화면 구현 fan-out 이 기본 한도에 묶이지 않는다. 값은 작게 시작해 올린다.
- **난이도**: ★☆☆ (약 5분)

### 4. 커밋 귀속 금지 규칙 재확인
- **파일**: `~/.claude/CLAUDE.md`
- **근거**: 귀속 리마인더가 CLAUDE.md 의 귀속 금지 규칙을 덮어쓰던 버그가 고쳐졌다. 현재 CLAUDE.md §6 에는 커밋 귀속(Co-Authored-By) 관련 명시가 없다 — 원하는 방향(붙임/안 붙임)을 §6 에 한 줄로 적으면 이제 그대로 적용된다.
- **난이도**: ★☆☆ (약 5분)
