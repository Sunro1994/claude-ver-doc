# Claude Code v2.1.179

> 작성일: 2026-06-17

---

# 📋 요약본

## 🛠️ 개선/수정 (9건)
- **스트림 중단 시 응답 보존** — 중간에 연결이 끊겨도 부분 응답이 사라지지 않고 보존된다. 스피너가 "running tool" 상태로 멈추는 현상도 해결.
- **WSL2 마우스 휠 스크롤 복구** — Windows Terminal 및 VS Code 환경의 WSL2 에서 마우스 휠 스크롤이 동작하지 않던 회귀 버그 (2.1.172 도입) 수정.
- **샌드박스 glob 거대화 차단** — `denyRead`/`allowRead` glob 이 큰 디렉토리 트리를 대상으로 할 때 Bash tool description 이 비대해져 Linux 세션이 마비되던 문제 수정.
- **피드백 설문 오인식 수정** — 턴 종료 직후 입력한 한 자리 숫자가 즉시 세션 평점으로 캡처되던 문제 수정.
- **welcome 화면 배너 정리** — 여러 프로모션 배너가 쌓여 표시되던 문제 수정. 세션당 최대 1개만 노출.
- **Ctrl+O 서브에이전트 트랜스크립트 표시** — 서브에이전트 보기 중 Ctrl+O 로 해당 트랜스크립트가 보이지 않던 문제 수정.
- **프롬프트 입력창 포커스 복귀** — 서브에이전트/푸터 패널에서 프롬프트 입력창 클릭 시 포커스가 돌아오지 않던 문제 수정.
- **원격 세션 백그라운드 작업 상태 정상화** — 턴 사이에 백그라운드 작업이 "still running" 상태로 멈춰 보이던 문제 수정.
- **원격 세션 플러그인 로딩 성능 개선** — 원격 세션에서 플러그인 로드 속도 향상.

## 🔑 이번 버전의 핵심 키워드
**"순수 버그 픽스 — 스트림·입력·원격 세션 안정성 보강"** — 신기능 없이 기존 UX 회귀와 엣지 케이스를 잡는 정비 릴리스.

---

# 📜 원문 (한글 번역본)

> 원문 ChangeLog를 원래 순서 그대로 한 줄도 빠짐없이 번역한 문서입니다.

- 스트림 중간 연결 끊김 수정: 부분 응답이 raw error 대신 보존되며, 스피너가 "running tool" 에서 멈추지 않음
- WSL2 환경의 Windows Terminal 및 VS Code 에서 마우스 휠 스크롤 수정 (2.1.172 회귀)
- 큰 디렉토리 트리에 대한 샌드박스 `denyRead`/`allowRead` glob 이 Bash tool description 을 비대화시켜 Linux 세션을 사용 불가하게 만들던 문제 수정
- 턴 완료 직후 한 자리 숫자 답변을 세션 평점으로 즉시 캡처하던 피드백 설문 수정
- welcome 화면이 여러 프로모션 배너를 쌓아 표시하던 문제 수정 — 세션당 최대 1개만 노출
- 서브에이전트 보기 중 Ctrl+O 가 해당 서브에이전트의 트랜스크립트를 보여주지 않던 문제 수정
- 서브에이전트/푸터 패널에서 프롬프트 입력 클릭 시 포커스가 돌아오지 않던 문제 수정
- 원격 세션 백그라운드 작업이 턴 사이에 "still running" 상태로 멈춰 보이던 문제 수정
- 원격 세션의 플러그인 로딩 성능 개선

---

## 🎯 챌린지

이번 버전에서 내 환경에 적용해볼 만한 항목입니다.

### 1. 샌드박스 글로브 검토
- **파일**: `~/.claude/settings.json`
- **근거**: 이번 버전이 큰 트리에 걸린 `denyRead`/`allowRead` glob 이 Bash tool description 을 비대화시키는 문제를 수정했다. 현재 settings.json 에는 sandbox 설정이 보이지 않지만, 만약 추가할 계획이 있다면 좁은 경로로 한정하는 패턴을 사전에 정리해두는 것이 안전하다.
- **난이도**: ★☆☆ (약 5분)

### 2. 플러그인 로딩 측정 베이스라인 기록
- **파일**: `~/agent-infra/notes/plugin-load-baseline.md` (신규)
- **근거**: 이번 버전이 원격 세션 플러그인 로딩 성능을 개선했다. 현재 5개 플러그인 (`superpowers`, `token-optimizer`, `understand-anything`, `vercel`, 비활성 `andrej-karpathy-skills`) 이 enabled 상태이므로, 업데이트 전후 세션 시작 시간을 기록해두면 다음 회귀 시 비교 근거가 된다.
- **난이도**: ★☆☆ (약 5분)

### 3. Ctrl+O 단축키 동작 재확인
- **파일**: `~/.claude/keybindings.json`
- **근거**: 서브에이전트 트랜스크립트 보기가 이번 버전에서 복구됐다. `subagent-driven-development` 와 `qa-agent` 를 자주 쓰는 환경이므로, Ctrl+O 가 의도대로 동작하는지 한 번 확인하고 필요하면 keybindings 에 명시적으로 고정해두면 좋다.
- **난이도**: ★☆☆ (약 5분)

### 4. 피드백 설문 입력 습관 점검
- **파일**: 해당 없음 (사용자 습관)
- **근거**: 턴 종료 직후 숫자 한 자만 입력하면 세션 평점으로 잡히던 문제가 수정됐다. 그래도 다음 프롬프트가 숫자로 시작할 일이 잦다면 (예: `1.`, `2단계`) 첫 줄에 공백이나 단어를 먼저 두는 습관을 유지하는 게 안전하다.
- **난이도**: ★☆☆ (약 5분)
